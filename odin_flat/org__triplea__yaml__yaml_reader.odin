package game

// Port of org.triplea.yaml.YamlReader (Java @UtilityClass).
// Phase A: type stub only. Utility class — no instance fields.
// Nested InvalidYamlFormatException is in a separate file.

Yaml_Reader :: struct {}

