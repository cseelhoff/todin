package game

// Empty shell type for org.triplea.map.data.elements.Production$Rule.
// The Java class is a namespace for Cost/Result inner classes. Renamed
// to Map_Data_Production_Rule to avoid clashing with the engine's
// authoritative Production_Rule type.
Map_Data_Production_Rule :: struct {}
