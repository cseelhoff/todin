diff --git a/game-app/game-core/src/main/java/games/strategy/engine/random/PlainRandomSource.java b/game-app/game-core/src/main/java/games/strategy/engine/random/PlainRandomSource.java
index 4a1488ba6..e799066a9 100644
--- a/game-app/game-core/src/main/java/games/strategy/engine/random/PlainRandomSource.java
+++ b/game-app/game-core/src/main/java/games/strategy/engine/random/PlainRandomSource.java
@@ -12,8 +12,19 @@ import org.apache.commons.math3.random.RandomGenerator;
 public final class PlainRandomSource implements IRandomSource {
   private final Object lock = new Object();
 
+  // triplea-port-bootstrap: fixedSeed: pin RNG for snapshot characterization runs.
+  /** When non-null, every new {@code PlainRandomSource} uses this seed. */
+  public static volatile Long fixedSeed = null;
+
   @GuardedBy("lock")
-  private final RandomGenerator random = new MersenneTwister();
+  private final RandomGenerator random;
+
+  public PlainRandomSource() {
+    Long seed = fixedSeed;
+    this.random = (seed != null)
+        ? new MersenneTwister(seed)
+        : new MersenneTwister();
+  }
 
   @Override
   public int[] getRandom(final int max, final int count, final String annotation) {
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/AbstractProAi.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/AbstractProAi.java
index f979e80ca..4263982c1 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/AbstractProAi.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/AbstractProAi.java
@@ -20,6 +20,7 @@ import games.strategy.triplea.ai.pro.logging.ProLogger;
 import games.strategy.triplea.ai.pro.simulate.ProDummyDelegateBridge;
 import games.strategy.triplea.ai.pro.simulate.ProSimulateTurnUtils;
 import games.strategy.triplea.ai.pro.util.ProBattleUtils;
+import games.strategy.triplea.ai.pro.util.ProPurTrace;
 import games.strategy.triplea.ai.pro.util.ProMatches;
 import games.strategy.triplea.ai.pro.util.ProOddsCalculator;
 import games.strategy.triplea.ai.pro.util.ProPurchaseUtils;
@@ -50,6 +51,7 @@ import java.util.UUID;
 import lombok.Getter;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashSet;
 
 /** Pro AI. */
 public abstract class AbstractProAi extends AbstractAi {
@@ -186,6 +188,7 @@ public abstract class AbstractProAi extends AbstractAi {
       }
       ProLogger.info("Starting simulation for purchase phase");
 
+      if (ProPurTrace.ENABLED) dumpCaucasus(data, player, "PRE_COPYDATA_LIVE");
       // Setup data copy and delegates
       final GameData dataCopy = copyData(data);
       if (dataCopy == null) {
@@ -201,10 +204,12 @@ public abstract class AbstractProAi extends AbstractAi {
       final GameSequence sequence = dataCopy.getSequence();
       final int nextStepIndex = sequence.getStepIndex() + 1;
       final List<GameStep> gameSteps = getGameStepsForPlayer(dataCopy, playerCopy, nextStepIndex);
+      if (ProPurTrace.ENABLED) dumpCaucasus(dataCopy, player, "00_after_copyData");
       for (final GameStep step : gameSteps) {
         sequence.setRoundAndStep(sequence.getRound(), step.getDisplayName(), step.getPlayerId());
         final String stepName = step.getName();
         ProLogger.info("Simulating phase: " + stepName);
+        if (ProPurTrace.ENABLED) dumpCaucasus(dataCopy, player, "BEFORE_" + stepName);
         if (GameStep.isNonCombatMoveStepName(stepName)) {
           proData.initializeSimulation(this, dataCopy, playerCopy);
           final Map<Territory, ProTerritory> factoryMoveMap =
@@ -271,6 +276,24 @@ public abstract class AbstractProAi extends AbstractAi {
     return dataCopy;
   }
 
+  private static void dumpCaucasus(GameData d, GamePlayer player, String tag) {
+    if (!"Russians".equals(player.getName())) return;
+    if (d == null || d.getMap() == null) return;
+    for (Territory t : d.getMap().getTerritories()) {
+      if (!"Caucasus".equals(t.getName())) continue;
+      java.util.TreeMap<String, Integer> b = new java.util.TreeMap<>();
+      int n = 0;
+      for (Unit u : t.getUnitCollection().getUnits()) {
+        n++;
+        String k = (u.getOwner() == null ? "-" : u.getOwner().getName())
+            + ":" + (u.getType() == null ? "?" : u.getType().getName());
+        b.merge(k, 1, Integer::sum);
+      }
+      System.out.printf("SIM_CAUCASUS tag=%s addr=%d n=%d breakdown=%s%n",
+          tag, System.identityHashCode(t), n, b);
+    }
+  }
+
   private static List<GameStep> getGameStepsForPlayer(
       GameData gameData, GamePlayer gamePlayer, int startStep) {
     int stepIndex = 0;
@@ -521,7 +544,7 @@ public abstract class AbstractProAi extends AbstractAi {
 
     // Calculate battle results
     final ProBattleResult result =
-        calc.calculateBattleResults(proData, unitTerritory, attackers, defenders, new HashSet<>());
+        calc.calculateBattleResults(proData, unitTerritory, attackers, defenders, new LinkedHashSet<>());
     ProLogger.debug(player.getName() + " sub attack TUVSwing=" + result.getTuvSwing());
     return result.getTuvSwing() > 0;
   }
@@ -538,3 +561,4 @@ public abstract class AbstractProAi extends AbstractAi {
     }
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProCombatMoveAi.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProCombatMoveAi.java
index 3c6f8c306..ee3370246 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProCombatMoveAi.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProCombatMoveAi.java
@@ -41,6 +41,8 @@ import java.util.TreeMap;
 import java.util.function.Predicate;
 import java.util.stream.Collectors;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Pro combat move AI. */
 public class ProCombatMoveAi {
@@ -98,7 +100,7 @@ public class ProCombatMoveAi {
 
     // Determine which territories can be held and remove any that aren't worth attacking
     clearedTerritories = new ArrayList<>();
-    final Set<Territory> possibleTransportTerritories = new HashSet<>();
+    final Set<Territory> possibleTransportTerritories = new LinkedHashSet<>();
     for (final ProTerritory patd : attackOptions) {
       clearedTerritories.add(patd.getTerritory());
       if (!patd.getAmphibAttackMap().isEmpty()) {
@@ -132,6 +134,20 @@ public class ProCombatMoveAi {
     // Check if any subs in contested territory that's not being attacked
     checkContestedSeaTerritories();
 
+    if (Boolean.getBoolean("plan") && "Russians".equals(player.getName())) {
+      Map<Territory, ProTerritory> _am = territoryManager.getAttackOptions().getTerritoryMap();
+      for (Map.Entry<Territory, ProTerritory> _e : _am.entrySet()) {
+        if (_e.getValue().getUnits().isEmpty()) continue;
+        for (Unit _u : _e.getValue().getUnits()) {
+          Territory _from = proData.getUnitTerritory(_u);
+          System.out.printf("PLAN_U player=%s t=%s unit=%s from=%s uuid=%s%n",
+              player.getName(), _e.getKey().getName(),
+              _u.getType()==null?"?":_u.getType().getName(),
+              _from==null?"<nil>":_from.getName(), _u.getId());
+        }
+      }
+    }
+
     // Calculate attack routes and perform moves
     doMove(territoryManager.getAttackOptions().getTerritoryMap(), moveDel, data, player);
 
@@ -402,7 +418,7 @@ public class ProCombatMoveAi {
         territoryManager.getAttackOptions().getTerritoryMap();
 
     // Determine which territories to try and hold
-    final Set<Territory> territoriesToCheck = new HashSet<>();
+    final Set<Territory> territoriesToCheck = new LinkedHashSet<>();
     for (final ProTerritory patd : prioritizedTerritories) {
       final Territory t = patd.getTerritory();
       territoriesToCheck.add(t);
@@ -600,7 +616,7 @@ public class ProCombatMoveAi {
         }
 
         // Find all territories units are attacking from that are adjacent to territory
-        final Set<Territory> attackFromTerritories = new HashSet<>();
+        final Set<Territory> attackFromTerritories = new LinkedHashSet<>();
         for (final Unit u : patd.getMaxUnits()) {
           attackFromTerritories.add(proData.getUnitTerritory(u));
         }
@@ -701,7 +717,7 @@ public class ProCombatMoveAi {
         territoryManager.getDefendOptions().getTerritoryMap();
 
     // Remove units that have already attacked
-    final Set<Unit> alreadyAttackedWithUnits = new HashSet<>();
+    final Set<Unit> alreadyAttackedWithUnits = new LinkedHashSet<>();
     for (final ProTerritory t : attackMap.values()) {
       alreadyAttackedWithUnits.addAll(t.getUnits());
       alreadyAttackedWithUnits.addAll(t.getAmphibAttackMap().keySet());
@@ -722,7 +738,7 @@ public class ProCombatMoveAi {
       if (!patd.getTerritory().isWater() && !patd.getTransportTerritoryMap().isEmpty()) {
 
         // Find all transports for each unload territory
-        final Map<Territory, List<Unit>> territoryTransportAndBombardMap = new HashMap<>();
+        final Map<Territory, List<Unit>> territoryTransportAndBombardMap = new LinkedHashMap<>();
         for (final Unit u : patd.getTransportTerritoryMap().keySet()) {
           final Territory unloadTerritory = patd.getTransportTerritoryMap().get(u);
           if (territoryTransportAndBombardMap.containsKey(unloadTerritory)) {
@@ -864,7 +880,7 @@ public class ProCombatMoveAi {
       attackMap.values().forEach(proTerritory -> proTerritory.getBombers().clear());
 
       // Get all units that have already moved
-      final Set<Unit> alreadyAttackedWithUnits = new HashSet<>();
+      final Set<Unit> alreadyAttackedWithUnits = new LinkedHashSet<>();
       for (final ProTerritory t : attackMap.values()) {
         alreadyAttackedWithUnits.addAll(t.getUnits());
         alreadyAttackedWithUnits.addAll(t.getAmphibAttackMap().keySet());
@@ -1213,7 +1229,7 @@ public class ProCombatMoveAi {
         int sameTargetBombersCount = 0;
         final List<Unit> existingAttackingBombers = attackMap.get(t).getBombers();
         if (Properties.getDamageFromBombingDoneToUnitsInsteadOfTerritories(data.getProperties())) {
-          final Set<Unit> sameTargetBombers = new HashSet<>();
+          final Set<Unit> sameTargetBombers = new LinkedHashSet<>();
           for (final Unit target : targetUnits) {
             neededDamageUnits += target.getHowMuchMoreDamageCanThisUnitTake(t);
             final Predicate<Unit> canBombTarget =
@@ -1268,11 +1284,11 @@ public class ProCombatMoveAi {
     }
 
     // Loop through all units and determine attack options
-    final Map<Unit, Set<Territory>> unitAttackOptions = new HashMap<>();
+    final Map<Unit, Set<Territory>> unitAttackOptions = new LinkedHashMap<>();
     for (final Map.Entry<Unit, Set<Territory>> unitAttackEntry : unitAttackMap.entrySet()) {
 
       // Find number of attack options
-      final Set<Territory> canAttackTerritories = new HashSet<>();
+      final Set<Territory> canAttackTerritories = new LinkedHashSet<>();
       for (final ProTerritory attackTerritoryData : prioritizedTerritories) {
         if (unitAttackEntry.getValue().contains(attackTerritoryData.getTerritory())) {
           canAttackTerritories.add(attackTerritoryData.getTerritory());
@@ -1533,12 +1549,12 @@ public class ProCombatMoveAi {
     if (!Properties.getTransportCasualtiesRestricted(data.getProperties())) {
 
       // Loop through all my transports and see which territories they can attack from current list
-      final Map<Unit, Set<Territory>> transportAttackOptions = new HashMap<>();
+      final Map<Unit, Set<Territory>> transportAttackOptions = new LinkedHashMap<>();
       for (final Map.Entry<Unit, Set<Territory>> transportAttackEntry :
           transportAttackMap.entrySet()) {
 
         // Find number of attack options
-        final Set<Territory> canAttackTerritories = new HashSet<>();
+        final Set<Territory> canAttackTerritories = new LinkedHashSet<>();
         for (final ProTerritory attackTerritoryData : prioritizedTerritories) {
           if (transportAttackEntry.getValue().contains(attackTerritoryData.getTerritory())) {
             canAttackTerritories.add(attackTerritoryData.getTerritory());
@@ -1578,7 +1594,7 @@ public class ProCombatMoveAi {
     }
 
     // Loop through all my transports and see which can make amphib attack
-    final Map<Unit, Set<Territory>> amphibAttackOptions = new HashMap<>();
+    final Map<Unit, Set<Territory>> amphibAttackOptions = new LinkedHashMap<>();
     for (final ProTransport proTransportData : transportMapList) {
 
       // If already used to attack then ignore
@@ -1587,7 +1603,7 @@ public class ProCombatMoveAi {
       }
 
       // Find number of attack options
-      final Set<Territory> canAmphibAttackTerritories = new HashSet<>();
+      final Set<Territory> canAmphibAttackTerritories = new LinkedHashSet<>();
       for (final ProTerritory attackTerritoryData : prioritizedTerritories) {
         if (proTransportData.getTransportMap().containsKey(attackTerritoryData.getTerritory())) {
           canAmphibAttackTerritories.add(attackTerritoryData.getTerritory());
@@ -1639,7 +1655,7 @@ public class ProCombatMoveAi {
                 final Set<Territory> territoriesToMoveTransport =
                     data.getMap()
                         .getNeighbors(t, ProMatches.territoryCanMoveSeaUnits(player, false));
-                final Set<Territory> loadFromTerritories = new HashSet<>();
+                final Set<Territory> loadFromTerritories = new LinkedHashSet<>();
                 for (final Unit u : amphibUnitsToAdd) {
                   loadFromTerritories.add(proData.getUnitTerritory(u));
                 }
@@ -1698,14 +1714,14 @@ public class ProCombatMoveAi {
     }
 
     // Get all units that have already moved
-    final Set<Unit> alreadyAttackedWithUnits = new HashSet<>();
+    final Set<Unit> alreadyAttackedWithUnits = new LinkedHashSet<>();
     for (final ProTerritory t : attackMap.values()) {
       alreadyAttackedWithUnits.addAll(t.getUnits());
       alreadyAttackedWithUnits.addAll(t.getAmphibAttackMap().keySet());
     }
 
     // Loop through all my bombard units and see which can bombard
-    final Map<Unit, Set<Territory>> bombardOptions = new HashMap<>();
+    final Map<Unit, Set<Territory>> bombardOptions = new LinkedHashMap<>();
     for (final Unit u : bombardMap.keySet()) {
 
       // If already used to attack then ignore
@@ -1714,7 +1730,7 @@ public class ProCombatMoveAi {
       }
 
       // Find number of bombard options
-      final Set<Territory> canBombardTerritories = new HashSet<>();
+      final Set<Territory> canBombardTerritories = new LinkedHashSet<>();
       for (final ProTerritory patd : prioritizedTerritories) {
         final List<Unit> defendingUnits = patd.getMaxEnemyDefenders(player);
         final boolean hasDefenders =
@@ -1939,7 +1955,7 @@ public class ProCombatMoveAi {
       final Set<Unit> combinedUnits = new HashSet<>(attackEntry.getValue().getMaxUnits());
       combinedUnits.addAll(attackEntry.getValue().getMaxAmphibUnits());
       ProLogger.trace("  --- My max units ---");
-      final Map<String, Integer> printMap = new HashMap<>();
+      final Map<String, Integer> printMap = new LinkedHashMap<>();
       for (final Unit unit : combinedUnits) {
         if (printMap.containsKey(unit.toStringNoOwner())) {
           printMap.put(unit.toStringNoOwner(), printMap.get(unit.toStringNoOwner()) + 1);
@@ -1949,7 +1965,7 @@ public class ProCombatMoveAi {
       }
       writeProLog(printMap);
       ProLogger.trace("  --- My max bombard units ---");
-      final Map<String, Integer> printBombardMap = new HashMap<>();
+      final Map<String, Integer> printBombardMap = new LinkedHashMap<>();
       for (final Unit unit : attackEntry.getValue().getMaxBombardUnits()) {
         if (printBombardMap.containsKey(unit.toStringNoOwner())) {
           printBombardMap.put(
@@ -1961,7 +1977,7 @@ public class ProCombatMoveAi {
       writeProLog(printBombardMap);
       final List<Unit> units3 = attackEntry.getValue().getUnits();
       ProLogger.trace("  --- My actual units ---");
-      final Map<String, Integer> printMap3 = new HashMap<>();
+      final Map<String, Integer> printMap3 = new LinkedHashMap<>();
       for (final Unit unit : units3) {
         if (printMap3.containsKey(unit.toStringNoOwner())) {
           printMap3.put(unit.toStringNoOwner(), printMap3.get(unit.toStringNoOwner()) + 1);
@@ -1971,7 +1987,7 @@ public class ProCombatMoveAi {
       }
       writeProLog(printMap3);
       ProLogger.trace("  --- Enemy units ---");
-      final Map<String, Integer> printMap2 = new HashMap<>();
+      final Map<String, Integer> printMap2 = new LinkedHashMap<>();
       final List<Unit> units2 = attackEntry.getValue().getMaxEnemyDefenders(player);
       for (final Unit unit : units2) {
         if (printMap2.containsKey(unit.toStringNoOwner())) {
@@ -1982,7 +1998,7 @@ public class ProCombatMoveAi {
       }
       writeProLog(printMap2);
       ProLogger.trace("  --- Enemy Counter Attack Units ---");
-      final Map<String, Integer> printMap4 = new HashMap<>();
+      final Map<String, Integer> printMap4 = new LinkedHashMap<>();
       final List<Unit> units4 = attackEntry.getValue().getMaxEnemyUnits();
       for (final Unit unit : units4) {
         if (printMap4.containsKey(unit.toStringNoOwner())) {
@@ -1993,7 +2009,7 @@ public class ProCombatMoveAi {
       }
       writeProLog(printMap4);
       ProLogger.trace("  --- Enemy Counter Bombard Units ---");
-      final Map<String, Integer> printMap5 = new HashMap<>();
+      final Map<String, Integer> printMap5 = new LinkedHashMap<>();
       final Set<Unit> units5 = attackEntry.getValue().getMaxEnemyBombardUnits();
       for (final Unit unit : units5) {
         if (printMap5.containsKey(unit.toStringNoOwner())) {
@@ -2028,3 +2044,4 @@ public class ProCombatMoveAi {
     return isAdjacentToAlliedFactory || !usesMoreThanHalfOfRange;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProData.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProData.java
index 37441e0b2..68b9dfa2f 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProData.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProData.java
@@ -23,6 +23,8 @@ import javax.annotation.Nullable;
 import lombok.Getter;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Pro AI data. */
 @Getter
@@ -33,13 +35,13 @@ public final class ProData {
   private double minWinPercentage = 75;
   private @Nullable Territory myCapital = null;
   private List<Territory> myUnitTerritories = new ArrayList<>();
-  private Map<Unit, Territory> unitTerritoryMap = new HashMap<>();
+  private Map<Unit, Territory> unitTerritoryMap = new java.util.LinkedHashMap<>();  // triplea-port-bootstrap: LinkedHashMap for stable iter
   private IntegerMap<UnitType> unitValueMap = new IntegerMap<>();
   private @Nullable ProPurchaseOptionMap purchaseOptions = null;
   // If we purchased units this turn that consume other units, these are the units selected to be
   // consumed. These are already located at the factory locations, so should not be moved. In the
   // future, we could add logic about moving such units to factory territories from elsewhere.
-  private final Set<Unit> unitsToBeConsumed = new HashSet<>();
+  private final Set<Unit> unitsToBeConsumed = new java.util.LinkedHashSet<>();  // triplea-port-bootstrap: LinkedHashMap for stable iter
   private double minCostPerHitPoint = Double.MAX_VALUE;
 
   private AbstractProAi proAi;
@@ -90,7 +92,7 @@ public final class ProData {
   }
 
   private static Map<Unit, Territory> newUnitTerritoryMap(final GameState data) {
-    final Map<Unit, Territory> unitTerritoryMap = new HashMap<>();
+    final Map<Unit, Territory> unitTerritoryMap = new LinkedHashMap<>();
     for (final Territory t : data.getMap().getTerritories()) {
       for (final Unit u : t.getUnits()) {
         unitTerritoryMap.put(u, t);
@@ -113,3 +115,4 @@ public final class ProData {
     return moveMap.computeIfAbsent(t, k -> new ProTerritory(t, this));
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProNonCombatMoveAi.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProNonCombatMoveAi.java
index f75dd27ce..600279ca7 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProNonCombatMoveAi.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProNonCombatMoveAi.java
@@ -57,6 +57,7 @@ import java.util.stream.Collectors;
 import java.util.stream.Stream;
 import org.apache.commons.lang3.mutable.MutableObject;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
 
 /** Pro non-combat move AI. */
 class ProNonCombatMoveAi {
@@ -91,6 +92,9 @@ class ProNonCombatMoveAi {
 
     // Find the max number of units that can move to each allied territory
     territoryManager.populateDefenseOptions(new ArrayList<>());
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emit(
+        "01_after_populateDefenseOptions",
+        territoryManager.getDefendOptions().getTerritoryMap());
 
     // Note: On maps that have a single move phase, this function may be called with this true.
     boolean isCombatMove = GameStepPropertiesHelper.isCombatMove(data);
@@ -98,19 +102,30 @@ class ProNonCombatMoveAi {
     // Find number of units in each move territory that can't move and all infra units
     findUnitsThatCantMove(purchaseTerritories, proData.getPurchaseOptions().getLandOptions());
     final Map<Unit, Set<Territory>> infraUnitMoveMap = findInfraUnitsThatCanMove();
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emit(
+        "02_after_findUnitsThatCantMove",
+        territoryManager.getDefendOptions().getTerritoryMap());
 
     // Try to have one land unit in each territory that is bordering an enemy territory
     final List<Territory> movedOneDefenderToTerritories =
         moveOneDefenderToLandTerritoriesBorderingEnemy();
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emit(
+        "03_after_moveOneDefender",
+        territoryManager.getDefendOptions().getTerritoryMap());
 
     // Determine max enemy attack units and if territories can be held
     territoryManager.populateEnemyAttackOptions(
         movedOneDefenderToTerritories, territoryManager.getDefendTerritories());
     determineIfMoveTerritoriesCanBeHeld();
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emit(
+        "04_after_determineCanHold",
+        territoryManager.getDefendOptions().getTerritoryMap());
 
     // Prioritize territories to defend
     Map<Territory, ProTerritory> factoryMoveMap = initialFactoryMoveMap;
     final List<ProTerritory> prioritizedTerritories = prioritizeDefendOptions(factoryMoveMap);
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emitList(
+        "05_after_prioritize", prioritizedTerritories);
 
     // Determine which territories to defend and how many units each one needs
     final Territory myCapital = proData.getMyCapital();
@@ -119,6 +134,9 @@ class ProNonCombatMoveAi {
       enemyDistanceToMyCapital =
           ProUtils.getClosestEnemyLandTerritoryDistance(data, player, myCapital);
       moveUnitsToDefendTerritories(isCombatMove, prioritizedTerritories, enemyDistanceToMyCapital);
+      games.strategy.triplea.ai.pro.util.ProNcmTrace.emit(
+          "06_after_moveUnitsToDefend",
+          territoryManager.getDefendOptions().getTerritoryMap());
     }
 
     // Copy data in case capital defense needs increased
@@ -158,6 +176,8 @@ class ProNonCombatMoveAi {
         }
 
         moveUnitsToBestTerritories(isCombatMove);
+        games.strategy.triplea.ai.pro.util.ProNcmTrace.emit(
+            "07_after_moveUnitsToBest", moveMap);
 
         // Check if capital has local land superiority
         ProLogger.info(
@@ -182,6 +202,8 @@ class ProNonCombatMoveAi {
 
     // Determine where to move infra units
     factoryMoveMap = moveInfraUnits(isCombatMove, factoryMoveMap, infraUnitMoveMap);
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emit(
+        "08_after_moveInfra", territoryManager.getDefendOptions().getTerritoryMap());
 
     // Log a warning if any units not assigned to a territory (skip infrastructure for now)
     for (final Unit u : territoryManager.getDefendOptions().getUnitMoveMap().keySet()) {
@@ -246,6 +268,7 @@ class ProNonCombatMoveAi {
                   .or(proData.getUnitsToBeConsumed()::contains));
       proTerritory.addCantMoveUnits(cantMoveUnits);
     }
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emit("02A_after_allied_defender", moveMap);
 
     // Add all units that only have 1 move option and can't be transported
     for (final Iterator<Unit> it = unitMoveMap.keySet().iterator(); it.hasNext(); ) {
@@ -260,13 +283,23 @@ class ProNonCombatMoveAi {
         }
       }
     }
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emit("02B_after_single_option", moveMap);
 
     // Check if purchase units are known yet
+    if (games.strategy.triplea.ai.pro.util.ProNcmTrace.ENABLED && "Russians".equals(player.getName())) {
+      System.out.printf("PHC pname=Russians has_pt=%b pt_size=%d%n",
+          purchaseTerritories != null,
+          purchaseTerritories == null ? 0 : purchaseTerritories.size());
+    }
     if (purchaseTerritories != null) {
       // Add all units that will be purchased
       for (final ProPurchaseTerritory ppt : purchaseTerritories.values()) {
         for (final ProPlaceTerritory placeTerritory : ppt.getCanPlaceTerritories()) {
           final Territory t = placeTerritory.getTerritory();
+          if (games.strategy.triplea.ai.pro.util.ProNcmTrace.ENABLED && "Russians".equals(player.getName())) {
+            System.out.printf("PHC place t=%s n_units=%d in_move_map=%b%n",
+                t.getName(), placeTerritory.getPlaceUnits().size(), moveMap.get(t) != null);
+          }
           if (moveMap.get(t) != null) {
             moveMap.get(t).addCantMoveUnits(placeTerritory.getPlaceUnits());
           }
@@ -297,7 +330,7 @@ class ProNonCombatMoveAi {
     Map<Unit, Set<Territory>> unitMoveMap = territoryManager.getDefendOptions().getUnitMoveMap();
 
     // Add all units that are infra
-    final Map<Unit, Set<Territory>> infraUnitMoveMap = new HashMap<>();
+    final Map<Unit, Set<Territory>> infraUnitMoveMap = new LinkedHashMap<>();
     for (final Iterator<Unit> it = unitMoveMap.keySet().iterator(); it.hasNext(); ) {
       final Unit u = it.next();
       if (ProMatches.unitCanBeMovedAndIsOwned(player).test(u)
@@ -653,7 +686,7 @@ class ProNonCombatMoveAi {
         CollectionUtils.getMatches(
             seaFactories, ProMatches.territoryHasInfraFactoryAndIsOwnedLandAdjacentToSea(player));
     final Predicate<Territory> canMoveSeaUnits = ProMatches.territoryCanMoveSeaUnits(player, true);
-    final Set<Territory> territoriesToCheck = new HashSet<>(seaFactories);
+    final Set<Territory> territoriesToCheck = new LinkedHashSet<>(seaFactories);
     for (final Territory t : seaFactories) {
       territoriesToCheck.addAll(data.getMap().getNeighbors(t, canMoveSeaUnits));
     }
@@ -740,7 +773,7 @@ class ProNonCombatMoveAi {
           prioritizedTerritories.subList(0, numToDefend);
 
       // Loop through all units and determine defend options
-      final Map<Unit, Set<Territory>> unitDefendOptions = new HashMap<>();
+      final Map<Unit, Set<Territory>> unitDefendOptions = new LinkedHashMap<>();
       for (final Unit unit : unitMoveMap.keySet()) {
         // Find number of move options
         final Set<Territory> canDefendTerritories = new LinkedHashSet<>();
@@ -861,10 +894,10 @@ class ProNonCombatMoveAi {
       // Loop through all my transports and see which territories they can defend from current list
       final List<Unit> alreadyMovedTransports = new ArrayList<>();
       if (!Properties.getTransportCasualtiesRestricted(data.getProperties())) {
-        final Map<Unit, Set<Territory>> transportDefendOptions = new HashMap<>();
+        final Map<Unit, Set<Territory>> transportDefendOptions = new LinkedHashMap<>();
         for (final Unit unit : transportMoveMap.keySet()) {
           // Find number of defend options
-          final Set<Territory> canDefendTerritories = new HashSet<>();
+          final Set<Territory> canDefendTerritories = new LinkedHashSet<>();
           for (final ProTerritory attackTerritoryData : territoriesToTryToDefend) {
             if (transportMoveMap.get(unit).contains(attackTerritoryData.getTerritory())) {
               canDefendTerritories.add(attackTerritoryData.getTerritory());
@@ -899,7 +932,7 @@ class ProNonCombatMoveAi {
       }
 
       // Loop through all my transports and see which can make amphib move
-      final Map<Unit, Set<Territory>> amphibMoveOptions = new HashMap<>();
+      final Map<Unit, Set<Territory>> amphibMoveOptions = new LinkedHashMap<>();
       for (final ProTransport proTransportData : transportMapList) {
         // If already used to defend then ignore
         if (alreadyMovedTransports.contains(proTransportData.getTransport())) {
@@ -907,7 +940,7 @@ class ProNonCombatMoveAi {
         }
 
         // Find number of amphib move options
-        final Set<Territory> canAmphibMoveTerritories = new HashSet<>();
+        final Set<Territory> canAmphibMoveTerritories = new LinkedHashSet<>();
         for (final ProTerritory attackTerritoryData : territoriesToTryToDefend) {
           if (proTransportData.getTransportMap().containsKey(attackTerritoryData.getTerritory())) {
             canAmphibMoveTerritories.add(attackTerritoryData.getTerritory());
@@ -963,7 +996,7 @@ class ProNonCombatMoveAi {
                 final Set<Territory> territoriesToMoveTransport =
                     data.getMap()
                         .getNeighbors(t, ProMatches.territoryCanMoveSeaUnits(player, isCombatMove));
-                final Set<Territory> loadFromTerritories = new HashSet<>();
+                final Set<Territory> loadFromTerritories = new LinkedHashSet<>();
                 for (final Unit u : amphibUnitsToAdd) {
                   loadFromTerritories.add(unitTerritoryMap.get(u));
                 }
@@ -1019,7 +1052,7 @@ class ProNonCombatMoveAi {
       // Determine if all defenses are successful
       boolean areSuccessful = true;
       boolean containsCapital = false;
-      final Set<Territory> territoriesToCheck = new HashSet<>();
+      final Set<Territory> territoriesToCheck = new LinkedHashSet<>();
       for (final ProTerritory patd : territoriesToTryToDefend) {
         final Territory t = patd.getTerritory();
         territoriesToCheck.add(t);
@@ -1215,9 +1248,9 @@ class ProNonCombatMoveAi {
 
     while (true) {
       ProLogger.info("Move units to best value territories");
-      final Set<Territory> territoriesToDefend = new HashSet<>();
-      final Map<Unit, Set<Territory>> currentUnitMoveMap = new HashMap<>(unitMoveMap);
-      final Map<Unit, Set<Territory>> currentTransportMoveMap = new HashMap<>(transportMoveMap);
+      final Set<Territory> territoriesToDefend = new LinkedHashSet<>();
+      final Map<Unit, Set<Territory>> currentUnitMoveMap = new LinkedHashMap<>(unitMoveMap);
+      final Map<Unit, Set<Territory>> currentTransportMoveMap = new LinkedHashMap<>(transportMoveMap);
       final List<ProTransport> currentTransportMapList = new ArrayList<>(transportMapList);
 
       // Reset lists
@@ -1270,7 +1303,7 @@ class ProNonCombatMoveAi {
             }
 
             // Find best territory to move transport
-            final Set<Territory> loadFromTerritories = new HashSet<>();
+            final Set<Territory> loadFromTerritories = new LinkedHashSet<>();
             for (final Unit u : amphibUnitsToAdd) {
               loadFromTerritories.add(unitTerritoryMap.get(u));
             }
@@ -1475,7 +1508,7 @@ class ProNonCombatMoveAi {
         // Move towards best loading territory if route is safe
         for (final ProTerritory patd : priorizitedLoadTerritories) {
           boolean movedTransport = false;
-          final Set<Territory> cantHoldTerritories = new HashSet<>();
+          final Set<Territory> cantHoldTerritories = new LinkedHashSet<>();
           while (true) {
             final Predicate<Territory> match =
                 ProMatches.territoryCanMoveSeaUnitsThrough(player, isCombatMove)
@@ -2258,7 +2291,7 @@ class ProNonCombatMoveAi {
   private Map<Territory, ProTerritory> buildFactoryMoveMap(
       final Map<Territory, ProTerritory> moveMap,
       final Map<Unit, Set<Territory>> infraUnitMoveMap) {
-    final Map<Territory, ProTerritory> factoryMoveMap = new HashMap<>();
+    final Map<Territory, ProTerritory> factoryMoveMap = new LinkedHashMap<>();
     for (final Iterator<Unit> it = infraUnitMoveMap.keySet().iterator(); it.hasNext(); ) {
       final Unit u = it.next();
       // Only check factory units
@@ -2326,7 +2359,7 @@ class ProNonCombatMoveAi {
       final Map<Territory, ProTerritory> moveMap,
       final MoveValidator validator) {
     // First, determine which unit types can be consumed during purchase phase.
-    Set<UnitType> consumables = new HashSet<>();
+    Set<UnitType> consumables = new LinkedHashSet<>();
     for (ProPurchaseOption option : proData.getPurchaseOptions().getAllOptions()) {
       // Skip construction purchase options, since these can be placed without factory.
       if (option.isConsumesUnits() && !option.isConstruction()) {
@@ -2470,7 +2503,7 @@ class ProNonCombatMoveAi {
       combinedUnits.addAll(proTerritory.getMaxAmphibUnits());
       combinedUnits.addAll(proTerritory.getCantMoveUnits());
       ProLogger.trace("  --- My max units ---");
-      final Map<String, Integer> printMap = new HashMap<>();
+      final Map<String, Integer> printMap = new LinkedHashMap<>();
       for (final Unit unit : combinedUnits) {
         if (printMap.containsKey(unit.toStringNoOwner())) {
           printMap.put(unit.toStringNoOwner(), printMap.get(unit.toStringNoOwner()) + 1);
@@ -2482,7 +2515,7 @@ class ProNonCombatMoveAi {
         ProLogger.trace("    " + printMap.get(key) + " " + key);
       }
       ProLogger.trace("  --- My max amphib units ---");
-      final Map<String, Integer> printMap5 = new HashMap<>();
+      final Map<String, Integer> printMap5 = new LinkedHashMap<>();
       for (final Unit unit : proTerritory.getMaxAmphibUnits()) {
         if (printMap5.containsKey(unit.toStringNoOwner())) {
           printMap5.put(unit.toStringNoOwner(), printMap5.get(unit.toStringNoOwner()) + 1);
@@ -2495,7 +2528,7 @@ class ProNonCombatMoveAi {
       }
       final List<Unit> units3 = proTerritory.getUnits();
       ProLogger.trace("  --- My actual units ---");
-      final Map<String, Integer> printMap3 = new HashMap<>();
+      final Map<String, Integer> printMap3 = new LinkedHashMap<>();
       for (final Unit unit : units3) {
         if (unit == null) {
           continue;
@@ -2510,7 +2543,7 @@ class ProNonCombatMoveAi {
         ProLogger.trace("    " + printMap3.get(key) + " " + key);
       }
       ProLogger.trace("  --- Enemy units ---");
-      final Map<String, Integer> printMap2 = new HashMap<>();
+      final Map<String, Integer> printMap2 = new LinkedHashMap<>();
       final List<Unit> units2 = proTerritory.getMaxEnemyUnits();
       for (final Unit unit : units2) {
         if (printMap2.containsKey(unit.toStringNoOwner())) {
@@ -2523,7 +2556,7 @@ class ProNonCombatMoveAi {
         ProLogger.trace("    " + printMap2.get(key) + " " + key);
       }
       ProLogger.trace("  --- Enemy bombard units ---");
-      final Map<String, Integer> printMap4 = new HashMap<>();
+      final Map<String, Integer> printMap4 = new LinkedHashMap<>();
       final Set<Unit> units4 = proTerritory.getMaxEnemyBombardUnits();
       for (final Unit unit : units4) {
         if (printMap4.containsKey(unit.toStringNoOwner())) {
@@ -2538,3 +2571,4 @@ class ProNonCombatMoveAi {
     }
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProPoliticsAi.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProPoliticsAi.java
index a8a1455bf..484cf4502 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProPoliticsAi.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProPoliticsAi.java
@@ -19,6 +19,7 @@ import java.util.Iterator;
 import java.util.List;
 import java.util.Map;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
 
 /** Pro politics AI. */
 class ProPoliticsAi {
@@ -53,8 +54,8 @@ class ProPoliticsAi {
     ProLogger.trace("Valid War options: " + validWarActions);
 
     // Divide war actions into enemy and neutral
-    final Map<PoliticalActionAttachment, List<GamePlayer>> enemyMap = new HashMap<>();
-    final Map<PoliticalActionAttachment, List<GamePlayer>> neutralMap = new HashMap<>();
+    final Map<PoliticalActionAttachment, List<GamePlayer>> enemyMap = new LinkedHashMap<>();
+    final Map<PoliticalActionAttachment, List<GamePlayer>> neutralMap = new LinkedHashMap<>();
     for (final PoliticalActionAttachment action : validWarActions) {
       final List<GamePlayer> warPlayers = new ArrayList<>();
       for (final PoliticalActionAttachment.RelationshipChange relationshipChange :
@@ -99,7 +100,7 @@ class ProPoliticsAi {
               + attackOptions);
 
       // Find attack options per war action
-      final Map<PoliticalActionAttachment, Double> attackPercentageMap = new HashMap<>();
+      final Map<PoliticalActionAttachment, Double> attackPercentageMap = new LinkedHashMap<>();
       for (final PoliticalActionAttachment action : enemyMap.keySet()) {
         int count = 0;
         final List<GamePlayer> enemyPlayers = enemyMap.get(action);
@@ -190,3 +191,4 @@ class ProPoliticsAi {
     }
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProPurchaseAi.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProPurchaseAi.java
index 16a0aee81..eb7030b9e 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProPurchaseAi.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProPurchaseAi.java
@@ -52,6 +52,8 @@ import java.util.Set;
 import java.util.function.Predicate;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Pro purchase AI. */
 class ProPurchaseAi {
@@ -92,7 +94,7 @@ class ProPurchaseAi {
     if (player.getRepairFrontier() != null
         && Properties.getDamageFromBombingDoneToUnitsInsteadOfTerritories(data.getProperties())) {
       ProLogger.debug("Factories can be damaged");
-      final Map<Unit, Territory> unitsThatCanProduceNeedingRepair = new HashMap<>();
+      final Map<Unit, Territory> unitsThatCanProduceNeedingRepair = new LinkedHashMap<>();
       for (final Territory fixTerr : rfactories) {
         if (!Matches.territoryIsOwnedAndHasOwnedUnitMatching(
                 player, Matches.unitCanProduceUnitsAndCanBeDamaged())
@@ -126,7 +128,7 @@ class ProPurchaseAi {
           if (diff > 0) {
             final IntegerMap<RepairRule> repairMap = new IntegerMap<>();
             repairMap.add(repairRule, diff);
-            final Map<Unit, IntegerMap<RepairRule>> repair = new HashMap<>();
+            final Map<Unit, IntegerMap<RepairRule>> repair = new LinkedHashMap<>();
             repair.put(fixUnit, repairMap);
             pusRemaining -= diff;
             ProLogger.debug(
@@ -183,7 +185,7 @@ class ProPurchaseAi {
 
       // Find strategic value for each territory
       ProLogger.info("Find strategic value for place territories");
-      final Set<Territory> territoriesToCheck = new HashSet<>();
+      final Set<Territory> territoriesToCheck = new LinkedHashSet<>();
       for (final ProPurchaseTerritory t : purchaseTerritories.values()) {
         for (final ProPlaceTerritory ppt : t.getCanPlaceTerritories()) {
           territoriesToCheck.add(ppt.getTerritory());
@@ -300,10 +302,11 @@ class ProPurchaseAi {
         purchaseOptions.getLandZeroMoveOptions(),
         purchaseOptions.getAirOptions(),
         true);
+    games.strategy.triplea.ai.pro.util.ProPurTrace.emit("P01_after_purchaseDefenders_land", purchaseTerritories);
 
     // Find strategic value for each territory
     ProLogger.info("Find strategic value for place territories");
-    final Set<Territory> territoriesToCheck = new HashSet<>();
+    final Set<Territory> territoriesToCheck = new LinkedHashSet<>();
     for (final Territory t : purchaseTerritories.keySet()) {
       for (final ProPlaceTerritory ppt : purchaseTerritories.get(t).getCanPlaceTerritories()) {
         territoriesToCheck.add(ppt.getTerritory());
@@ -325,7 +328,9 @@ class ProPurchaseAi {
         prioritizeLandTerritories(purchaseTerritories);
     purchaseAaUnits(
         purchaseTerritories, prioritizedLandTerritories, purchaseOptions.getAaOptions());
+    games.strategy.triplea.ai.pro.util.ProPurTrace.emit("P02_after_purchaseAa", purchaseTerritories);
     purchaseLandUnits(purchaseTerritories, prioritizedLandTerritories, purchaseOptions);
+    games.strategy.triplea.ai.pro.util.ProPurTrace.emit("P03_after_purchaseLand", purchaseTerritories);
 
     // Prioritize sea territories that need defended and purchase additional defenders
     final List<ProPlaceTerritory> needToDefendSeaTerritories =
@@ -337,28 +342,33 @@ class ProPurchaseAi {
         List.of(),
         purchaseOptions.getAirOptions(),
         false);
+    games.strategy.triplea.ai.pro.util.ProPurTrace.emit("P04_after_purchaseDefenders_sea", purchaseTerritories);
 
     // Determine whether to purchase new land factory
-    final Map<Territory, ProPurchaseTerritory> factoryPurchaseTerritories = new HashMap<>();
+    final Map<Territory, ProPurchaseTerritory> factoryPurchaseTerritories = new LinkedHashMap<>();
     purchaseFactory(
         factoryPurchaseTerritories,
         purchaseTerritories,
         prioritizedLandTerritories,
         purchaseOptions,
         false);
+    games.strategy.triplea.ai.pro.util.ProPurTrace.emit("P05_after_purchaseFactory_first", purchaseTerritories);
 
     // Prioritize sea place options and purchase units
     final List<ProPlaceTerritory> prioritizedSeaTerritories =
         prioritizeSeaTerritories(purchaseTerritories);
     final boolean shouldSaveUpForAFleet =
         purchaseSeaAndAmphibUnits(purchaseTerritories, prioritizedSeaTerritories, purchaseOptions);
+    games.strategy.triplea.ai.pro.util.ProPurTrace.emit("P06_after_purchaseSeaAndAmphib", purchaseTerritories);
 
     // Try to use any remaining PUs on high value units, except if we need to save up for a fleet.
     if (!shouldSaveUpForAFleet) {
       purchaseUnitsWithRemainingProduction(
           purchaseTerritories, purchaseOptions.getLandOptions(), purchaseOptions.getAirOptions());
+      games.strategy.triplea.ai.pro.util.ProPurTrace.emit("P07_after_purchaseUnitsWithRemaining", purchaseTerritories);
 
       upgradeUnitsWithRemainingPUs(purchaseTerritories, purchaseOptions);
+      games.strategy.triplea.ai.pro.util.ProPurTrace.emit("P08_after_upgradeUnitsWithRemaining", purchaseTerritories);
 
       // Try to purchase land/sea factory with extra PUs
       purchaseFactory(
@@ -367,10 +377,12 @@ class ProPurchaseAi {
           prioritizedLandTerritories,
           purchaseOptions,
           true);
+      games.strategy.triplea.ai.pro.util.ProPurTrace.emit("P09_after_purchaseFactory_second", purchaseTerritories);
     }
 
     // Add factory purchase territory to list
     purchaseTerritories.putAll(factoryPurchaseTerritories);
+    games.strategy.triplea.ai.pro.util.ProPurTrace.emit("P10_final", purchaseTerritories);
 
     // Determine final count of each production rule
     final IntegerMap<ProductionRule> purchaseMap =
@@ -514,7 +526,7 @@ class ProPurchaseAi {
     // Find all place territories
     final Map<Territory, ProPurchaseTerritory> placeNonConstructionTerritories =
         ProPurchaseUtils.findPurchaseTerritories(proData, player);
-    final Set<Territory> placeTerritories = new HashSet<>();
+    final Set<Territory> placeTerritories = new LinkedHashSet<>();
     for (final Territory t : placeNonConstructionTerritories.keySet()) {
       for (final ProPlaceTerritory ppt :
           placeNonConstructionTerritories.get(t).getCanPlaceTerritories()) {
@@ -539,7 +551,7 @@ class ProPurchaseAi {
 
     // Find strategic value for each territory
     ProLogger.info("Find strategic value for place territories");
-    final Set<Territory> territoriesToCheck = new HashSet<>();
+    final Set<Territory> territoriesToCheck = new LinkedHashSet<>();
     for (final ProPurchaseTerritory t : placeNonConstructionTerritories.values()) {
       for (final ProPlaceTerritory ppt : t.getCanPlaceTerritories()) {
         territoriesToCheck.add(ppt.getTerritory());
@@ -582,6 +594,18 @@ class ProPurchaseAi {
       for (final ProPlaceTerritory placeTerritory : ppt.getCanPlaceTerritories()) {
         final Territory t = placeTerritory.getTerritory();
         final List<Unit> units = t.getMatches(Matches.isUnitAllied(player));
+        if (games.strategy.triplea.ai.pro.util.ProPurTrace.ENABLED
+            && "Russians".equals(player.getName())
+            && "Caucasus".equals(t.getName())) {
+          int allN = 0;
+          java.util.TreeMap<String, Integer> bd = new java.util.TreeMap<>();
+          for (Unit u : t.getUnitCollection().getUnits()) {
+            allN++;
+            String key = u.getOwner().getName() + ":" + u.getType().getName();
+            bd.merge(key, 1, Integer::sum);
+          }
+          System.out.printf("FD_TRACE t=Caucasus all_units=%d allied=%d breakdown=%s%n", allN, units.size(), bd);
+        }
         placeTerritory.setDefendingUnits(units);
         ProLogger.debug(t + " has numDefenders=" + units.size());
       }
@@ -596,7 +620,7 @@ class ProPurchaseAi {
     final ProOtherMoveOptions enemyAttackOptions = territoryManager.getEnemyAttackOptions();
 
     // Determine which territories need defended
-    final Set<ProPlaceTerritory> needToDefendTerritories = new HashSet<>();
+    final Set<ProPlaceTerritory> needToDefendTerritories = new LinkedHashSet<>();
     for (final ProPurchaseTerritory ppt : purchaseTerritories.values()) {
 
       // Check if any of the place territories can't be held with current defenders
@@ -726,6 +750,32 @@ class ProPurchaseAi {
     // Loop through prioritized territories and purchase defenders
     for (final ProPlaceTerritory placeTerritory : needToDefendTerritories) {
       final Territory t = placeTerritory.getTerritory();
+      if (games.strategy.triplea.ai.pro.util.ProPurTrace.ENABLED
+          && "Russians".equals(player.getName())) {
+        final java.util.TreeMap<String,Integer> defOwners = new java.util.TreeMap<>();
+        int defOwned = 0;
+        for (Unit u : placeTerritory.getDefendingUnits()) {
+          String on = u.getOwner() == null ? "-" : u.getOwner().getName();
+          String utn = u.getType() == null ? "?" : u.getType().getName();
+          defOwners.merge(on + "_" + utn, 1, Integer::sum);
+          if (u.getOwner() == player) defOwned++;
+        }
+        final java.util.TreeMap<String,Integer> enemyOwners = new java.util.TreeMap<>();
+        for (Unit u : enemyAttackOptions.getMax(t).getMaxUnits()) {
+          String on = u.getOwner() == null ? "-" : u.getOwner().getName();
+          String utn = u.getType() == null ? "?" : u.getType().getName();
+          enemyOwners.merge(on + "_" + utn, 1, Integer::sum);
+        }
+        System.out.printf("PDE_TRACE phase=%s t=%s def_n=%d def_owned=%d def_breakdown=%s enemy_n=%d amphib_n=%d enemy_breakdown=%s%n",
+            isLand ? "land" : "sea",
+            t.getName(),
+            placeTerritory.getDefendingUnits().size(),
+            defOwned,
+            defOwners,
+            enemyAttackOptions.getMax(t).getMaxUnits().size(),
+            enemyAttackOptions.getMax(t).getMaxAmphibUnits().size(),
+            enemyOwners);
+      }
       ProLogger.debug(
           "Purchasing defenders for "
               + t.getName()
@@ -803,7 +853,7 @@ class ProPurchaseAi {
               purchaseTerritories,
               remainingConstructions,
               t);
-          final Map<ProPurchaseOption, Double> defenseEfficiencies = new HashMap<>();
+          final Map<ProPurchaseOption, Double> defenseEfficiencies = new LinkedHashMap<>();
           for (final ProPurchaseOption ppo : purchaseOptionsForTerritory) {
             if (isLand) {
               defenseEfficiencies.put(
@@ -866,6 +916,21 @@ class ProPurchaseAi {
                   defenders,
                   enemyAttackOptions.getMax(t).getMaxBombardUnits());
 
+          if (games.strategy.triplea.ai.pro.util.ProPurTrace.ENABLED
+              && "Russians".equals(player.getName())) {
+            System.out.printf(
+                "PD_TRACE t=%s sel=%s utp_n=%d def_n=%d enemy_n=%d bomb_n=%d tuv_swing=%s has_land=%s win%%=%s capital=%s%n",
+                t.getName(),
+                selectedOption.getUnitType().getName(),
+                unitsToPlace.size(),
+                defenders.size(),
+                enemyAttackingUnits.size(),
+                enemyAttackOptions.getMax(t).getMaxBombardUnits().size(),
+                finalResult.getTuvSwing(),
+                finalResult.isHasLandUnitRemaining(),
+                finalResult.getWinPercentage(),
+                t.equals(proData.getMyCapital()));
+          }
           // Break if it can be held
           if ((!t.equals(proData.getMyCapital())
                   && !finalResult.isHasLandUnitRemaining()
@@ -1062,7 +1127,7 @@ class ProPurchaseAi {
     }
 
     // Loop through prioritized territories and purchase land units
-    final Set<Territory> territoriesToCheck = new HashSet<>();
+    final Set<Territory> territoriesToCheck = new LinkedHashSet<>();
     final Predicate<Territory> canMoveLandUnits =
         ProMatches.territoryCanPotentiallyMoveLandUnits(player);
     final Predicate<Territory> isEnemyTerritory =
@@ -1173,7 +1238,7 @@ class ProPurchaseAi {
         // Select purchase option
         Optional<ProPurchaseOption> optionalSelectedOption = Optional.empty();
         if (!selectFodderUnit && attackAndDefenseDifference > 0 && !landDefenseOptions.isEmpty()) {
-          final Map<ProPurchaseOption, Double> defenseEfficiencies = new HashMap<>();
+          final Map<ProPurchaseOption, Double> defenseEfficiencies = new LinkedHashMap<>();
           for (final ProPurchaseOption ppo : landDefenseOptions) {
             defenseEfficiencies.put(
                 ppo, ppo.getDefenseEfficiency(enemyDistance, data, ownedLocalUnits, unitsToPlace));
@@ -1181,7 +1246,7 @@ class ProPurchaseAi {
           optionalSelectedOption =
               ProPurchaseUtils.randomizePurchaseOption(defenseEfficiencies, "Land Defense");
         } else if (!selectFodderUnit && !landAttackOptions.isEmpty()) {
-          final Map<ProPurchaseOption, Double> attackEfficiencies = new HashMap<>();
+          final Map<ProPurchaseOption, Double> attackEfficiencies = new LinkedHashMap<>();
           for (final ProPurchaseOption ppo : landAttackOptions) {
             attackEfficiencies.put(
                 ppo, ppo.getAttackEfficiency(enemyDistance, data, ownedLocalUnits, unitsToPlace));
@@ -1189,7 +1254,7 @@ class ProPurchaseAi {
           optionalSelectedOption =
               ProPurchaseUtils.randomizePurchaseOption(attackEfficiencies, "Land Attack");
         } else if (!landFodderOptions.isEmpty()) {
-          final Map<ProPurchaseOption, Double> fodderEfficiencies = new HashMap<>();
+          final Map<ProPurchaseOption, Double> fodderEfficiencies = new LinkedHashMap<>();
           for (final ProPurchaseOption ppo : landFodderOptions) {
             fodderEfficiencies.put(
                 ppo, ppo.getFodderEfficiency(enemyDistance, data, ownedLocalUnits, unitsToPlace));
@@ -1251,7 +1316,7 @@ class ProPurchaseAi {
             data.getMap().getTerritories(),
             ProMatches.territoryHasNoInfraFactoryAndIsNotConqueredOwnedLand(player));
     possibleFactoryTerritories.removeAll(factoryPurchaseTerritories.keySet());
-    final Set<Territory> purchaseFactoryTerritories = new HashSet<>();
+    final Set<Territory> purchaseFactoryTerritories = new LinkedHashSet<>();
     final List<Territory> territoriesThatCantBeHeld = new ArrayList<>();
     for (final Territory t : possibleFactoryTerritories) {
 
@@ -1442,7 +1507,7 @@ class ProPurchaseAi {
     final ProOtherMoveOptions enemyAttackOptions = territoryManager.getEnemyAttackOptions();
 
     // Determine which sea territories can be placed in
-    final Set<ProPlaceTerritory> seaPlaceTerritories = new HashSet<>();
+    final Set<ProPlaceTerritory> seaPlaceTerritories = new LinkedHashSet<>();
     for (final ProPurchaseTerritory ppt : purchaseTerritories.values()) {
       for (final ProPlaceTerritory placeTerritory : ppt.getCanPlaceTerritories()) {
         final Territory t = placeTerritory.getTerritory();
@@ -1627,7 +1692,7 @@ class ProPurchaseAi {
                 purchaseTerritories,
                 0,
                 t);
-            final Map<ProPurchaseOption, Double> defenseEfficiencies = new HashMap<>();
+            final Map<ProPurchaseOption, Double> defenseEfficiencies = new LinkedHashMap<>();
             for (final ProPurchaseOption ppo : seaPurchaseOptionsForTerritory) {
               defenseEfficiencies.put(
                   ppo,
@@ -1839,7 +1904,7 @@ class ProPurchaseAi {
               purchaseTerritories,
               0,
               t);
-          final Map<ProPurchaseOption, Double> defenseEfficiencies = new HashMap<>();
+          final Map<ProPurchaseOption, Double> defenseEfficiencies = new LinkedHashMap<>();
           for (final ProPurchaseOption ppo : seaPurchaseOptionsForTerritory) {
             defenseEfficiencies.put(
                 ppo,
@@ -1891,7 +1956,7 @@ class ProPurchaseAi {
       // Loop through adjacent purchase territories and purchase transport/amphib units
       final int distance =
           ProTransportUtils.findMaxMovementForTransports(purchaseOptions.getSeaTransportOptions());
-      final Set<Territory> territoriesToCheck = new HashSet<>();
+      final Set<Territory> territoriesToCheck = new LinkedHashSet<>();
       for (final ProPurchaseTerritory purchaseTerritory : selectedPurchaseTerritories) {
         final Territory landTerritory = purchaseTerritory.getTerritory();
         final Set<Territory> seaTerritories =
@@ -1934,7 +1999,7 @@ class ProPurchaseAi {
 
         // Find transports that need loaded and units to ignore that are already paired up
         final List<Unit> transportsThatNeedUnits = new ArrayList<>();
-        final Set<Unit> potentialUnitsToLoad = new HashSet<>();
+        final Set<Unit> potentialUnitsToLoad = new LinkedHashSet<>();
         final Set<Territory> seaTerritories =
             data.getMap().getNeighbors(landTerritory, distance, canMoveSea);
         for (final Territory seaTerritory : seaTerritories) {
@@ -2009,7 +2074,7 @@ class ProPurchaseAi {
                   purchaseTerritories,
                   0,
                   t);
-              final Map<ProPurchaseOption, Double> amphibEfficiencies = new HashMap<>();
+              final Map<ProPurchaseOption, Double> amphibEfficiencies = new LinkedHashMap<>();
               for (final ProPurchaseOption ppo : amphibPurchaseOptionsForTerritory) {
                 if (ppo.getTransportCost() <= transportCapacity) {
                   amphibEfficiencies.put(
@@ -2045,7 +2110,7 @@ class ProPurchaseAi {
                 purchaseTerritories,
                 0,
                 t);
-            final Map<ProPurchaseOption, Double> transportEfficiencies = new HashMap<>();
+            final Map<ProPurchaseOption, Double> transportEfficiencies = new LinkedHashMap<>();
             for (final ProPurchaseOption ppo : seaTransportPurchaseOptionsForTerritory) {
               transportEfficiencies.put(ppo, ppo.getTransportEfficiencyRatio());
             }
@@ -2224,7 +2289,7 @@ class ProPurchaseAi {
             purchaseTerritories,
             0,
             t);
-        final Map<ProPurchaseOption, Double> defenseEfficiencies = new HashMap<>();
+        final Map<ProPurchaseOption, Double> defenseEfficiencies = new LinkedHashMap<>();
         for (final ProPurchaseOption ppo : purchaseOptionsForTerritory) {
           defenseEfficiencies.put(
               ppo,
@@ -2642,3 +2707,4 @@ class ProPurchaseAi {
     AbstractAi.movePause();
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProRetreatAi.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProRetreatAi.java
index 622a7fee4..ce3e46f82 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProRetreatAi.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProRetreatAi.java
@@ -19,6 +19,7 @@ import java.util.HashSet;
 import java.util.Optional;
 import java.util.UUID;
 import javax.annotation.Nullable;
+import java.util.LinkedHashSet;
 
 /**
  * Pro retreat AI.
@@ -66,7 +67,7 @@ class ProRetreatAi {
     // Calculate battle results
     final ProBattleResult result =
         calc.calculateBattleResultsNoSubmerge(
-            proData, battleTerritory, attackers, defenders, new HashSet<>());
+            proData, battleTerritory, attackers, defenders, new LinkedHashSet<>());
 
     // Determine if it has a factory
     int isFactory = (ProMatches.territoryHasInfraFactoryAndIsLand().test(battleTerritory) ? 1 : 0);
@@ -139,3 +140,4 @@ class ProRetreatAi {
     return Optional.empty();
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProScrambleAi.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProScrambleAi.java
index 3f2652faf..a5fd41e5f 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProScrambleAi.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProScrambleAi.java
@@ -23,6 +23,8 @@ import java.util.List;
 import java.util.Map;
 import java.util.Set;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Pro scramble AI. */
 class ProScrambleAi {
@@ -64,8 +66,8 @@ class ProScrambleAi {
     }
 
     // Check if max defense is worse
-    final Set<Unit> allScramblers = new HashSet<>();
-    final Map<Territory, List<Unit>> possibleMaxScramblerMap = new HashMap<>();
+    final Set<Unit> allScramblers = new LinkedHashSet<>();
+    final Map<Territory, List<Unit>> possibleMaxScramblerMap = new LinkedHashMap<>();
     for (final Territory t : possibleScramblers.keySet()) {
       final int maxCanScramble =
           ScrambleLogic.getMaxScrambleCount(possibleScramblers.get(t).getFirst());
@@ -94,12 +96,12 @@ class ProScrambleAi {
     }
 
     // Loop through all units and determine attack options
-    final Map<Unit, Set<Territory>> unitDefendOptions = new HashMap<>();
+    final Map<Unit, Set<Territory>> unitDefendOptions = new LinkedHashMap<>();
     for (final Territory t : possibleMaxScramblerMap.keySet()) {
       final Set<Territory> possibleTerritories =
           data.getMap().getNeighbors(t, ProMatches.territoryCanMoveSeaUnits(player, true));
       possibleTerritories.add(t);
-      final Set<Territory> battleTerritories = new HashSet<>();
+      final Set<Territory> battleTerritories = new LinkedHashSet<>();
       for (final Territory possibleTerritory : possibleTerritories) {
         final IBattle possibleBattle =
             delegate.getBattleTracker().getPendingBattle(possibleTerritory, BattleType.NORMAL);
@@ -144,7 +146,7 @@ class ProScrambleAi {
     }
 
     // Return units to scramble
-    final Map<Territory, Collection<Unit>> scrambleMap = new HashMap<>();
+    final Map<Territory, Collection<Unit>> scrambleMap = new LinkedHashMap<>();
     for (final Territory t : possibleScramblers.keySet()) {
       for (final Unit u : possibleScramblers.get(t).getSecond()) {
         if (unitsToScramble.contains(u)) {
@@ -161,3 +163,4 @@ class ProScrambleAi {
     return scrambleMap;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProTechAi.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProTechAi.java
index 48ca93254..e66e98e67 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProTechAi.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/ProTechAi.java
@@ -35,6 +35,8 @@ import javax.annotation.Nullable;
 import org.triplea.java.PredicateBuilder;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Pro tech AI. */
 final class ProTechAi {
@@ -117,7 +119,7 @@ final class ProTechAi {
 
     @Nullable GamePlayer enemyPlayer = null;
     final List<GamePlayer> enemyPlayers = getEnemyPlayers(data, player);
-    final Map<GamePlayer, Float> enemyPlayerAttackMap = new HashMap<>();
+    final Map<GamePlayer, Float> enemyPlayerAttackMap = new LinkedHashMap<>();
     final Iterator<GamePlayer> playerIter = enemyPlayers.iterator();
     if (location == null) {
       return -1000.0F;
@@ -198,7 +200,7 @@ final class ProTechAi {
         // note, both old and new implementations allow units to be calculated that are in
         // territories we have already assaulted
         // this can be easily changed
-        final Set<Integer> ignore = new HashSet<>();
+        final Set<Integer> ignore = new LinkedHashSet<>();
         ignore.add(1);
         final List<Route> r = new ArrayList<>();
         final List<Unit> ships =
@@ -403,7 +405,7 @@ final class ProTechAi {
       final List<Route> blitzTerrRoutes,
       final GameData data,
       final GamePlayer enemyPlayer) {
-    final Set<Integer> ignore = new HashSet<>();
+    final Set<Integer> ignore = new LinkedHashSet<>();
     ignore.add(1);
     final Predicate<Unit> blitzUnit =
         Matches.unitIsOwnedBy(enemyPlayer).and(Matches.unitCanBlitz()).and(Matches.unitCanMove());
@@ -434,7 +436,7 @@ final class ProTechAi {
       final boolean sea) {
 
     final IntegerMap<Territory> distance = new IntegerMap<>();
-    final Map<Territory, Territory> visited = new HashMap<>();
+    final Map<Territory, Territory> visited = new LinkedHashMap<>();
     final List<Unit> units = new ArrayList<>();
     final Queue<Territory> q = new ArrayDeque<>();
     q.add(start);
@@ -754,3 +756,4 @@ final class ProTechAi {
     return Matches.territoryIsLand().and(Matches.territoryIsImpassable());
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProMyMoveOptions.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProMyMoveOptions.java
index 56f274a30..d559004ae 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProMyMoveOptions.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProMyMoveOptions.java
@@ -13,12 +13,12 @@ import lombok.Getter;
 /** The result of an AI movement analysis for its own possible moves. */
 @Getter
 public class ProMyMoveOptions {
-  private final Map<Territory, ProTerritory> territoryMap = new HashMap<>();
-  private final Map<Unit, Set<Territory>> unitMoveMap = new HashMap<>();
-  private final Map<Unit, Set<Territory>> transportMoveMap = new HashMap<>();
-  private final Map<Unit, Set<Territory>> bombardMap = new HashMap<>();
+  private final Map<Territory, ProTerritory> territoryMap = new java.util.LinkedHashMap<>();  // triplea-port-bootstrap: LinkedHashMap for stable iter
+  private final Map<Unit, Set<Territory>> unitMoveMap = new java.util.LinkedHashMap<>();  // triplea-port-bootstrap: LinkedHashMap for stable iter
+  private final Map<Unit, Set<Territory>> transportMoveMap = new java.util.LinkedHashMap<>();  // triplea-port-bootstrap: LinkedHashMap for stable iter
+  private final Map<Unit, Set<Territory>> bombardMap = new java.util.LinkedHashMap<>();  // triplea-port-bootstrap: LinkedHashMap for stable iter
   private final List<ProTransport> transportList = new ArrayList<>();
-  private final Map<Unit, Set<Territory>> bomberMoveMap = new HashMap<>();
+  private final Map<Unit, Set<Territory>> bomberMoveMap = new java.util.LinkedHashMap<>();  // triplea-port-bootstrap: LinkedHashMap for stable iter
 
   ProMyMoveOptions() {}
 
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProOtherMoveOptions.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProOtherMoveOptions.java
index 2473229cc..db23b3198 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProOtherMoveOptions.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProOtherMoveOptions.java
@@ -13,6 +13,7 @@ import java.util.List;
 import java.util.Map;
 import java.util.Set;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
 
 /** The result of an AI movement analysis for another player's possible moves. */
 public class ProOtherMoveOptions {
@@ -21,8 +22,8 @@ public class ProOtherMoveOptions {
   private final Map<Territory, List<ProTerritory>> moveMaps;
 
   public ProOtherMoveOptions() {
-    maxMoveMap = new HashMap<>();
-    moveMaps = new HashMap<>();
+    maxMoveMap = new LinkedHashMap<>();
+    moveMaps = new LinkedHashMap<>();
   }
 
   public ProOtherMoveOptions(
@@ -55,7 +56,7 @@ public class ProOtherMoveOptions {
       final GamePlayer player,
       final boolean isAttacker) {
 
-    final Map<Territory, ProTerritory> result = new HashMap<>();
+    final Map<Territory, ProTerritory> result = new LinkedHashMap<>();
     final List<GamePlayer> players = ProUtils.getOtherPlayersInTurnOrder(player);
     for (final Map<Territory, ProTerritory> moveMap : moveMaps) {
       for (final Territory t : moveMap.keySet()) {
@@ -101,7 +102,7 @@ public class ProOtherMoveOptions {
 
   private static Map<Territory, List<ProTerritory>> newMoveMaps(
       final List<Map<Territory, ProTerritory>> moveMapList) {
-    final Map<Territory, List<ProTerritory>> result = new HashMap<>();
+    final Map<Territory, List<ProTerritory>> result = new LinkedHashMap<>();
     for (final Map<Territory, ProTerritory> moveMap : moveMapList) {
       for (final Territory t : moveMap.keySet()) {
         result.computeIfAbsent(t, key -> new ArrayList<>()).add(moveMap.get(t));
@@ -110,3 +111,4 @@ public class ProOtherMoveOptions {
     return result;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProPurchaseOption.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProPurchaseOption.java
index 5399a118c..7ee16cb2c 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProPurchaseOption.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProPurchaseOption.java
@@ -24,6 +24,7 @@ import java.util.Set;
 import lombok.Getter;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashSet;
 
 /** The result of an AI purchase analysis for a single production rule and unit type. */
 public class ProPurchaseOption {
@@ -310,7 +311,7 @@ public class ProPurchaseOption {
           numAddedSupport *= 2;
         }
         int numSupportProvided = -numAddedSupport;
-        final Set<Unit> supportableUnits = new HashSet<>();
+        final Set<Unit> supportableUnits = new LinkedHashSet<>();
         for (final UnitSupportAttachment usa2 : bonusType) {
           numSupportProvided += availableSupports.getSupport(usa2);
           supportableUnits.addAll(
@@ -404,3 +405,4 @@ public class ProPurchaseOption {
     return getUnitType().createTemp(getQuantity(), player);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProPurchaseOptionMap.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProPurchaseOptionMap.java
index 9bdb3b1b5..757da67e3 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProPurchaseOptionMap.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProPurchaseOptionMap.java
@@ -13,6 +13,7 @@ import java.util.HashSet;
 import java.util.List;
 import java.util.Set;
 import lombok.Getter;
+import java.util.LinkedHashSet;
 
 /**
  * Takes all available purchase options, filters out those which the AI can't handle, and sorts them
@@ -147,7 +148,7 @@ public class ProPurchaseOptionMap {
   }
 
   public List<ProPurchaseOption> getAllOptions() {
-    final Set<ProPurchaseOption> allOptions = new HashSet<>();
+    final Set<ProPurchaseOption> allOptions = new LinkedHashSet<>();
     allOptions.addAll(getLandOptions());
     allOptions.addAll(landZeroMoveOptions);
     allOptions.addAll(airOptions);
@@ -159,7 +160,7 @@ public class ProPurchaseOptionMap {
   }
 
   public List<ProPurchaseOption> getLandOptions() {
-    final Set<ProPurchaseOption> landOptions = new HashSet<>();
+    final Set<ProPurchaseOption> landOptions = new LinkedHashSet<>();
     landOptions.addAll(landFodderOptions);
     landOptions.addAll(landAttackOptions);
     landOptions.addAll(landDefenseOptions);
@@ -167,7 +168,7 @@ public class ProPurchaseOptionMap {
   }
 
   private List<ProPurchaseOption> getSeaOptions() {
-    final Set<ProPurchaseOption> seaOptions = new HashSet<>();
+    final Set<ProPurchaseOption> seaOptions = new LinkedHashSet<>();
     seaOptions.addAll(seaDefenseOptions);
     seaOptions.addAll(seaTransportOptions);
     seaOptions.addAll(seaCarrierOptions);
@@ -185,3 +186,4 @@ public class ProPurchaseOptionMap {
     ProLogger.debug(sb.toString());
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTerritory.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTerritory.java
index dfbfedc0b..46a226969 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTerritory.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTerritory.java
@@ -21,6 +21,8 @@ import java.util.function.Predicate;
 import java.util.function.Supplier;
 import lombok.Getter;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** The result of an AI territory analysis. */
 public class ProTerritory {
@@ -67,7 +69,7 @@ public class ProTerritory {
   public ProTerritory(final Territory territory, final ProData proData) {
     this.territory = territory;
     this.proData = proData;
-    maxUnits = new HashSet<>();
+    maxUnits = new LinkedHashSet<>();
     units = new ArrayList<>();
     bombers = new ArrayList<>();
     maxBattleResult = new ProBattleResult();
@@ -78,24 +80,24 @@ public class ProTerritory {
     strengthEstimate = Double.POSITIVE_INFINITY;
 
     maxAmphibUnits = new ArrayList<>();
-    amphibAttackMap = new HashMap<>();
-    transportTerritoryMap = new HashMap<>();
+    amphibAttackMap = new LinkedHashMap<>();
+    transportTerritoryMap = new LinkedHashMap<>();
     needAmphibUnits = false;
     strafing = false;
-    isTransportingMap = new HashMap<>();
-    maxBombardUnits = new HashSet<>();
-    bombardOptionsMap = new HashMap<>();
-    bombardTerritoryMap = new HashMap<>();
+    isTransportingMap = new LinkedHashMap<>();
+    maxBombardUnits = new LinkedHashSet<>();
+    bombardOptionsMap = new LinkedHashMap<>();
+    bombardTerritoryMap = new LinkedHashMap<>();
 
     currentlyWins = false;
     battleResult = null;
 
-    cantMoveUnits = new HashSet<>();
+    cantMoveUnits = new LinkedHashSet<>();
     maxEnemyUnits = new ArrayList<>();
-    maxEnemyBombardUnits = new HashSet<>();
+    maxEnemyBombardUnits = new LinkedHashSet<>();
     minBattleResult = new ProBattleResult();
     tempUnits = new ArrayList<>();
-    tempAmphibAttackMap = new HashMap<>();
+    tempAmphibAttackMap = new LinkedHashMap<>();
     loadValue = 0;
 
     maxScrambleUnits = new ArrayList<>();
@@ -139,7 +141,7 @@ public class ProTerritory {
   }
 
   public Collection<Unit> getAllDefenders() {
-    final Set<Unit> defenders = new HashSet<>(units);
+    final Set<Unit> defenders = new LinkedHashSet<>(units);
     defenders.addAll(cantMoveUnits);
     // tempUnits can already be in the units/cantMoveUnits collection
     defenders.addAll(tempUnits);
@@ -328,7 +330,7 @@ public class ProTerritory {
     if (bombardOptionsMap.containsKey(unit)) {
       bombardOptionsMap.get(unit).add(t);
     } else {
-      final Set<Territory> territories = new HashSet<>();
+      final Set<Territory> territories = new LinkedHashSet<>();
       territories.add(t);
       bombardOptionsMap.put(unit, territories);
     }
@@ -346,3 +348,4 @@ public class ProTerritory {
     return proData.getData().getMap().getNeighbors(territory, predicate);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTerritoryManager.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTerritoryManager.java
index 6ce382143..a17d223dd 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTerritoryManager.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTerritoryManager.java
@@ -37,6 +37,8 @@ import lombok.Getter;
 import org.apache.commons.lang3.mutable.MutableObject;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Manages info about territories. */
 public class ProTerritoryManager {
@@ -209,7 +211,7 @@ public class ProTerritoryManager {
               && !gameMap.getNeighbors(optionalCapital.get()).contains(t)) {
 
             // Get max enemy defenders
-            final Set<Unit> additionalEnemyDefenders = new HashSet<>();
+            final Set<Unit> additionalEnemyDefenders = new LinkedHashSet<>();
             final List<GamePlayer> players = ProUtils.getOtherPlayersInTurnOrder(player);
             for (final ProTerritory enemyDefendOption : enemyDefendOptions.getAll(t)) {
               final Set<Unit> enemyUnits = new HashSet<>(enemyDefendOption.getMaxUnits());
@@ -223,7 +225,7 @@ public class ProTerritoryManager {
             }
 
             // Check allied result without strafe
-            final Set<Unit> enemyDefendersBeforeStrafe = new HashSet<>(defenders);
+            final Set<Unit> enemyDefendersBeforeStrafe = new LinkedHashSet<>(defenders);
             enemyDefendersBeforeStrafe.addAll(additionalEnemyDefenders);
             final ProBattleResult result =
                 calc.estimateAttackBattleResults(
@@ -339,7 +341,7 @@ public class ProTerritoryManager {
   }
 
   public boolean haveUsedAllAttackTransports() {
-    final Set<Unit> movedTransports = new HashSet<>();
+    final Set<Unit> movedTransports = new LinkedHashSet<>();
     for (final ProTerritory patd : attackOptions.getTerritoryMap().values()) {
       movedTransports.addAll(patd.getAmphibAttackMap().keySet());
       movedTransports.addAll(
@@ -397,7 +399,7 @@ public class ProTerritoryManager {
       final Collection<Territory> territoriesToCheck,
       final boolean isCheckingEnemyAttacks,
       final boolean isIgnoringRelationships) {
-    final Map<Territory, Set<Territory>> landRoutesMap = new HashMap<>();
+    final Map<Territory, Set<Territory>> landRoutesMap = new LinkedHashMap<>();
     final List<Territory> territoriesThatCantBeHeld = new ArrayList<>(enemyTerritories);
     territoriesThatCantBeHeld.addAll(territoriesToCheck);
     findNavalMoveOptions(
@@ -480,10 +482,10 @@ public class ProTerritoryManager {
       final List<Territory> alliedUnitTerritories =
           CollectionUtils.getMatches(
               data.getMap().getTerritories(), Matches.territoryHasUnitsOwnedBy(alliedPlayer));
-      final Map<Territory, ProTerritory> attackMap = new HashMap<>();
-      final Map<Unit, Set<Territory>> unitAttackMap = new HashMap<>();
-      final Map<Unit, Set<Territory>> transportAttackMap = new HashMap<>();
-      final Map<Unit, Set<Territory>> bombardMap = new HashMap<>();
+      final Map<Territory, ProTerritory> attackMap = new LinkedHashMap<>();
+      final Map<Unit, Set<Territory>> unitAttackMap = new LinkedHashMap<>();
+      final Map<Unit, Set<Territory>> transportAttackMap = new LinkedHashMap<>();
+      final Map<Unit, Set<Territory>> bombardMap = new LinkedHashMap<>();
       final List<ProTransport> transportMapList = new ArrayList<>();
       alliedAttackMaps.add(attackMap);
       findAttackOptions(
@@ -514,8 +516,14 @@ public class ProTerritoryManager {
     // Get enemy players in order of turn
     final List<GamePlayer> enemyPlayers = ProUtils.getEnemyPlayersInTurnOrder(player);
     final List<Map<Territory, ProTerritory>> enemyAttackMaps = new ArrayList<>();
-    final Set<Territory> alliedTerritories = new HashSet<>();
+    final Set<Territory> alliedTerritories = new LinkedHashSet<>();
     final List<Territory> enemyTerritories = new ArrayList<>(clearedTerritories);
+    if (Boolean.parseBoolean(System.getProperty("enemy.atk.dump", "false"))) {
+      System.out.printf(
+          "FEAO_ENTRY for=%s n_enemy_players=%d cleared=%d to_check=%d%n",
+          player.getName(), enemyPlayers.size(),
+          clearedTerritories.size(), territoriesToCheck.size());
+    }
 
     // Loop through each enemy to determine the maximum number of enemy units that can attack each
     // territory
@@ -524,12 +532,17 @@ public class ProTerritoryManager {
           CollectionUtils.getMatches(
               data.getMap().getTerritories(), Matches.territoryHasUnitsOwnedBy(enemyPlayer));
       enemyUnitTerritories.removeAll(clearedTerritories);
-      final Map<Territory, ProTerritory> attackMap = new HashMap<>();
-      final Map<Unit, Set<Territory>> unitAttackMap = new HashMap<>();
-      final Map<Unit, Set<Territory>> transportAttackMap = new HashMap<>();
-      final Map<Unit, Set<Territory>> bombardMap = new HashMap<>();
+      final Map<Territory, ProTerritory> attackMap = new LinkedHashMap<>();
+      final Map<Unit, Set<Territory>> unitAttackMap = new LinkedHashMap<>();
+      final Map<Unit, Set<Territory>> transportAttackMap = new LinkedHashMap<>();
+      final Map<Unit, Set<Territory>> bombardMap = new LinkedHashMap<>();
       final List<ProTransport> transportMapList = new ArrayList<>();
       enemyAttackMaps.add(attackMap);
+      if (Boolean.parseBoolean(System.getProperty("enemy.atk.dump", "false"))) {
+        System.out.printf(
+            "FEAO_PLAYER for=%s enemy=%s n_enemy_unit_terr=%d%n",
+            player.getName(), enemyPlayer.getName(), enemyUnitTerritories.size());
+      }
       findAttackOptions(
           proData,
           enemyPlayer,
@@ -547,6 +560,25 @@ public class ProTerritoryManager {
       alliedTerritories.addAll(
           CollectionUtils.getMatches(attackMap.keySet(), Matches.territoryIsLand()));
       enemyTerritories.removeAll(alliedTerritories);
+      if (Boolean.parseBoolean(System.getProperty("enemy.atk.dump", "false"))) {
+        for (final Map.Entry<Territory, ProTerritory> e : attackMap.entrySet()) {
+          final Territory t = e.getKey();
+          if (t == null) continue;
+          final String tn = t.getName();
+          if (!tn.equals("India") && !tn.equals("Trans-Jordan")) continue;
+          final ProTerritory pt = e.getValue();
+          System.out.printf(
+              "ENEMY_ATK_DUMP for=%s vs_player=%s enemy=%s max_units=%d amphib=%d%n",
+              player.getName(), enemyPlayer.getName(), tn,
+              pt.getMaxUnits().size(), pt.getMaxAmphibUnits().size());
+          for (final Unit u : pt.getMaxUnits()) {
+            System.out.printf("  EAU t=%s ep=%s u=%s%n", tn, enemyPlayer.getName(), u.getType().getName());
+          }
+          for (final Unit u : pt.getMaxAmphibUnits()) {
+            System.out.printf("  EAA t=%s ep=%s u=%s%n", tn, enemyPlayer.getName(), u.getType().getName());
+          }
+        }
+      }
     }
     return new ProOtherMoveOptions(enemyAttackMaps, player, true);
   }
@@ -560,7 +592,7 @@ public class ProTerritoryManager {
       final Map<Unit, Set<Territory>> transportMoveMap,
       final Map<Unit, Set<Territory>> bombardMap,
       final List<ProTransport> transportMapList) {
-    final Map<Territory, Set<Territory>> landRoutesMap = new HashMap<>();
+    final Map<Territory, Set<Territory>> landRoutesMap = new LinkedHashMap<>();
     final List<GamePlayer> otherPlayers = ProUtils.getPotentialEnemyPlayers(player);
     findNavalMoveOptions(
         proData,
@@ -623,7 +655,7 @@ public class ProTerritoryManager {
       final List<ProTransport> transportMapList,
       final List<Territory> clearedTerritories,
       final boolean isCheckingEnemyAttacks) {
-    final Map<Territory, Set<Territory>> landRoutesMap = new HashMap<>();
+    final Map<Territory, Set<Territory>> landRoutesMap = new LinkedHashMap<>();
     findNavalMoveOptions(
         proData,
         player,
@@ -635,6 +667,7 @@ public class ProTerritoryManager {
         clearedTerritories,
         false,
         isCheckingEnemyAttacks);
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emitRaw("01a_after_naval", moveMap);
     findLandMoveOptions(
         proData,
         player,
@@ -648,6 +681,7 @@ public class ProTerritoryManager {
         false,
         isCheckingEnemyAttacks,
         false);
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emitRaw("01b_after_land", moveMap);
     findAirMoveOptions(
         proData,
         player,
@@ -660,6 +694,7 @@ public class ProTerritoryManager {
         false,
         isCheckingEnemyAttacks,
         false);
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emitRaw("01c_after_air", moveMap);
     findAmphibMoveOptions(
         proData,
         player,
@@ -671,6 +706,7 @@ public class ProTerritoryManager {
         false,
         isCheckingEnemyAttacks,
         false);
+    games.strategy.triplea.ai.pro.util.ProNcmTrace.emitRaw("01d_after_amphib", moveMap);
   }
 
   private static ProOtherMoveOptions findEnemyDefendOptions(
@@ -690,9 +726,9 @@ public class ProTerritoryManager {
       final List<Territory> enemyUnitTerritories =
           CollectionUtils.getMatches(
               data.getMap().getTerritories(), Matches.territoryHasUnitsOwnedBy(enemyPlayer));
-      final Map<Territory, ProTerritory> moveMap = new HashMap<>();
-      final Map<Unit, Set<Territory>> unitMoveMap = new HashMap<>();
-      final Map<Unit, Set<Territory>> transportMoveMap = new HashMap<>();
+      final Map<Territory, ProTerritory> moveMap = new LinkedHashMap<>();
+      final Map<Unit, Set<Territory>> unitMoveMap = new LinkedHashMap<>();
+      final Map<Unit, Set<Territory>> transportMoveMap = new LinkedHashMap<>();
       final List<ProTransport> transportMapList = new ArrayList<>();
       enemyMoveMaps.add(moveMap);
       findDefendOptions(
@@ -778,10 +814,10 @@ public class ProTerritoryManager {
           // Populate appropriate unit move options map
           if (Matches.unitIsSeaTransport().test(mySeaUnit)) {
             transportMoveMap
-                .computeIfAbsent(mySeaUnit, k -> new HashSet<>())
+                .computeIfAbsent(mySeaUnit, k -> new LinkedHashSet<>())
                 .add(potentialTerritory);
           } else {
-            unitMoveMap.computeIfAbsent(mySeaUnit, k -> new HashSet<>()).add(potentialTerritory);
+            unitMoveMap.computeIfAbsent(mySeaUnit, k -> new LinkedHashSet<>()).add(potentialTerritory);
           }
         }
       }
@@ -841,7 +877,7 @@ public class ProTerritoryManager {
           }
 
           // Add to route map
-          landRoutesMap.computeIfAbsent(t, k -> new HashSet<>()).add(myUnitTerritory);
+          landRoutesMap.computeIfAbsent(t, k -> new LinkedHashSet<>()).add(myUnitTerritory);
 
           // Populate territories with land units
           final ProTerritory potentialTerritoryMove = proData.getProTerritory(moveMap, t);
@@ -851,7 +887,7 @@ public class ProTerritoryManager {
           potentialTerritoryMove.addMaxUnits(unitsToAdd);
 
           // Populate unit move options map
-          unitMoveMap.computeIfAbsent(u, k -> new HashSet<>()).add(t);
+          unitMoveMap.computeIfAbsent(u, k -> new LinkedHashSet<>()).add(t);
         }
       }
     }
@@ -909,16 +945,16 @@ public class ProTerritoryManager {
 
     // TODO: add carriers to landing possibilities for non-enemy attacks
     // Find possible carrier landing territories
-    final Set<Territory> possibleCarrierTerritories = new HashSet<>();
+    final Set<Territory> possibleCarrierTerritories = new LinkedHashSet<>();
     if (isCheckingEnemyAttacks || !isCombatMove) {
-      final Map<Unit, Set<Territory>> unitMoveMap2 = new HashMap<>();
+      final Map<Unit, Set<Territory>> unitMoveMap2 = new LinkedHashMap<>();
       findNavalMoveOptions(
           proData,
           player,
           myUnitTerritories,
-          new HashMap<>(),
+          new LinkedHashMap<>(),
           unitMoveMap2,
-          new HashMap<>(),
+          new LinkedHashMap<>(),
           Matches.territoryIsWater(),
           enemyTerritories,
           false,
@@ -955,6 +991,16 @@ public class ProTerritoryManager {
         // Find range
         final BigDecimal range =
             getUnitRange(myAirUnit, myUnitTerritory, player, isCheckingEnemyAttacks);
+        if (games.strategy.triplea.ai.pro.util.ProNcmTrace.ENABLED
+            && "Russians".equals(player.getName()) && !isCombatMove) {
+          System.out.printf(
+              "AIR_UNIT terr=%s ml=%s max=%d range=%s is_check_enemy=%b%n",
+              myUnitTerritory.getName(),
+              myAirUnit.getMovementLeft().toPlainString(),
+              myAirUnit.getUnitAttachment().getMovement(player),
+              range.toPlainString(),
+              isCheckingEnemyAttacks);
+        }
 
         // Find potential territories to move to
         final Set<Territory> possibleMoveTerritories =
@@ -1008,7 +1054,7 @@ public class ProTerritoryManager {
           proData.getProTerritory(moveMap, potentialTerritory).addMaxUnit(myAirUnit);
 
           // Populate unit attack options map
-          unitMoveMap.computeIfAbsent(myAirUnit, k -> new HashSet<>()).add(potentialTerritory);
+          unitMoveMap.computeIfAbsent(myAirUnit, k -> new LinkedHashSet<>()).add(potentialTerritory);
         }
       }
     }
@@ -1044,13 +1090,19 @@ public class ProTerritoryManager {
     for (final Territory transportTerritory : myUnitTerritories) {
       // Find my transports and amphibious units that have movement left
       final List<Unit> transports = transportTerritory.getMatches(isTransport);
+      if (isCheckingEnemyAttacks
+          && !transports.isEmpty()
+          && Boolean.parseBoolean(System.getProperty("enemy.atk.dump", "false"))) {
+        System.out.printf("FAMO_TT player=%s tt=%s n_tx=%d%n",
+            player.getName(), transportTerritory.getName(), transports.size());
+      }
 
       // Check each transport unit individually since they can have different ranges
       for (final Unit transport : transports) {
         // Find units to load and territories to unload
         final ProTransport proTransportData = new ProTransport(transport);
         transportMapList.add(proTransportData);
-        final Set<Territory> currentTerritories = new HashSet<>();
+        final Set<Territory> currentTerritories = new LinkedHashSet<>();
         currentTerritories.add(transportTerritory);
         final Predicate<Unit> canBeTransported =
             isCheckingEnemyAttacks
@@ -1062,7 +1114,7 @@ public class ProTerritoryManager {
             getUnitRange(transport, transportTerritory, player, isCheckingEnemyAttacks).intValue();
         MoveValidator moveValidator = new MoveValidator(data, !isCombatMove);
         while (movesLeft >= 0) {
-          final Set<Territory> nextTerritories = new HashSet<>();
+          final Set<Territory> nextTerritories = new LinkedHashSet<>();
           for (final Territory from : currentTerritories) {
             // Find neighbors I can move to
             for (final Territory neighbor : map.getNeighbors(from, canMoveSeaThrough)) {
@@ -1075,7 +1127,7 @@ public class ProTerritoryManager {
             // Get loaded units or get units that can be loaded into current territory if no enemies
             // present
             boolean haveUnitsToTransport = false;
-            final Set<Territory> loadFromTerritories = new HashSet<>();
+            final Set<Territory> loadFromTerritories = new LinkedHashSet<>();
             if (!transport.getTransporting(transportTerritory).isEmpty()) {
               haveUnitsToTransport = true;
             } else if (Matches.territoryHasEnemySeaUnits(player).negate().test(from)) {
@@ -1095,7 +1147,7 @@ public class ProTerritoryManager {
               continue;
             }
             // Find all water territories I can move to
-            final Set<Territory> seaMoveTerritories = new HashSet<>();
+            final Set<Territory> seaMoveTerritories = new LinkedHashSet<>();
             seaMoveTerritories.add(from);
             if (movesLeft > 0) {
               final Predicate<Territory> canMove =
@@ -1108,7 +1160,7 @@ public class ProTerritoryManager {
               }
             }
             // Find possible unload territories
-            final Set<Territory> unloadTerritories = new HashSet<>();
+            final Set<Territory> unloadTerritories = new LinkedHashSet<>();
             for (final Territory to : seaMoveTerritories) {
               unloadTerritories.addAll(map.getNeighbors(to, unloadAmphibTerritoryMatch));
             }
@@ -1162,6 +1214,22 @@ public class ProTerritoryManager {
 
         // Add amphib units to attack map
         proData.getProTerritory(moveMap, moveTerritory).addMaxAmphibUnits(amphibUnits);
+        if (isCheckingEnemyAttacks
+            && Boolean.parseBoolean(System.getProperty("enemy.atk.dump", "false"))) {
+          final String mtn = moveTerritory.getName();
+          if (mtn.equals("India") || mtn.equals("Trans-Jordan")) {
+            final List<String> loadNames = new java.util.ArrayList<>();
+            for (final Territory lt : territoriesCanLoadFrom) loadNames.add(lt.getName());
+            System.out.printf(
+                "AMPHIB_ADD player=%s target=%s tx=%s loadable=%d added=%d already=%d load_terrs=%s%n",
+                player.getName(), mtn,
+                transport.toString().split("@")[0],
+                territoriesCanLoadFrom.size(),
+                amphibUnits.size(),
+                alreadyAddedToMaxAmphibUnits.size(),
+                loadNames);
+          }
+        }
       }
     }
   }
@@ -1178,8 +1246,8 @@ public class ProTerritoryManager {
     final GameMap gameMap = data.getMap();
 
     // Find all transport unload from and to territories
-    final Set<Territory> unloadFromTerritories = new HashSet<>();
-    final Set<Territory> unloadToTerritories = new HashSet<>();
+    final Set<Territory> unloadFromTerritories = new LinkedHashSet<>();
+    final Set<Territory> unloadToTerritories = new LinkedHashSet<>();
     for (final ProTransport amphibData : transportMapList) {
       unloadFromTerritories.addAll(amphibData.getSeaTransportMap().keySet());
       unloadToTerritories.addAll(amphibData.getTransportMap().keySet());
@@ -1235,7 +1303,7 @@ public class ProTerritoryManager {
           }
 
           // Populate bombard options map
-          bombardMap.computeIfAbsent(mySeaUnit, k -> new HashSet<>()).addAll(bombardToTerritories);
+          bombardMap.computeIfAbsent(mySeaUnit, k -> new LinkedHashSet<>()).addAll(bombardToTerritories);
         }
       }
     }
@@ -1274,3 +1342,4 @@ public class ProTerritoryManager {
     return Optional.ofNullable(destination.getValue());
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTransport.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTransport.java
index d2d61fe47..d322ade98 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTransport.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/data/ProTransport.java
@@ -7,13 +7,15 @@ import java.util.HashSet;
 import java.util.Map;
 import java.util.Set;
 import lombok.Getter;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** The result of an AI amphibious movement analysis. */
 @Getter
 public class ProTransport {
   private final Unit transport;
-  private final Map<Territory, Set<Territory>> transportMap = new HashMap<>();
-  private final Map<Territory, Set<Territory>> seaTransportMap = new HashMap<>();
+  private final Map<Territory, Set<Territory>> transportMap = new java.util.LinkedHashMap<>();  // triplea-port-bootstrap: LinkedHashMap for stable iter
+  private final Map<Territory, Set<Territory>> seaTransportMap = new java.util.LinkedHashMap<>();  // triplea-port-bootstrap: LinkedHashMap for stable iter
 
   ProTransport(final Unit transport) {
     this.transport = transport;
@@ -21,13 +23,14 @@ public class ProTransport {
 
   void addTerritories(Set<Territory> attackTerritories, Set<Territory> loadFromTerritories) {
     for (Territory t : attackTerritories) {
-      transportMap.computeIfAbsent(t, key -> new HashSet<>()).addAll(loadFromTerritories);
+      transportMap.computeIfAbsent(t, key -> new LinkedHashSet<>()).addAll(loadFromTerritories);
     }
   }
 
   void addSeaTerritories(Set<Territory> attackTerritories, Set<Territory> loadFromTerritories) {
     for (Territory t : attackTerritories) {
-      seaTransportMap.computeIfAbsent(t, key -> new HashSet<>()).addAll(loadFromTerritories);
+      seaTransportMap.computeIfAbsent(t, key -> new LinkedHashSet<>()).addAll(loadFromTerritories);
     }
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/simulate/ProSimulateTurnUtils.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/simulate/ProSimulateTurnUtils.java
index 00e33252e..b1c94dba9 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/simulate/ProSimulateTurnUtils.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/simulate/ProSimulateTurnUtils.java
@@ -31,6 +31,7 @@ import java.util.Map.Entry;
 import java.util.Optional;
 import javax.annotation.Nullable;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
 
 /** Pro AI simulate turn utilities. */
 public final class ProSimulateTurnUtils {
@@ -70,6 +71,38 @@ public final class ProSimulateTurnUtils {
             calc.callBattleCalc(proData, t, attackers, defenders, bombardingUnits);
         final Collection<Unit> remainingAttackers = result.getAverageAttackersRemaining();
         final Collection<Unit> remainingDefenders = result.getAverageDefendersRemaining();
+        if (Boolean.getBoolean("simbattle.trace") && "Russians".equals(player.getName())) {
+          final java.util.TreeMap<String,Integer> attTally = new java.util.TreeMap<>();
+          for (Unit u : attackers) {
+            String on = u.getOwner() == null ? "-" : u.getOwner().getName();
+            String utn = u.getType() == null ? "?" : u.getType().getName();
+            attTally.merge(on + "_" + utn, 1, Integer::sum);
+          }
+          final java.util.TreeMap<String,Integer> defTally = new java.util.TreeMap<>();
+          for (Unit u : defenders) {
+            String on = u.getOwner() == null ? "-" : u.getOwner().getName();
+            String utn = u.getType() == null ? "?" : u.getType().getName();
+            defTally.merge(on + "_" + utn, 1, Integer::sum);
+          }
+          final java.util.TreeMap<String,Integer> remAttTally = new java.util.TreeMap<>();
+          for (Unit u : remainingAttackers) {
+            String on = u.getOwner() == null ? "-" : u.getOwner().getName();
+            String utn = u.getType() == null ? "?" : u.getType().getName();
+            remAttTally.merge(on + "_" + utn, 1, Integer::sum);
+          }
+          final java.util.TreeMap<String,Integer> remDefTally = new java.util.TreeMap<>();
+          for (Unit u : remainingDefenders) {
+            String on = u.getOwner() == null ? "-" : u.getOwner().getName();
+            String utn = u.getType() == null ? "?" : u.getType().getName();
+            remDefTally.merge(on + "_" + utn, 1, Integer::sum);
+          }
+          System.out.printf("SB_TRACE t=%s att_n=%d def_n=%d att=%s def=%s win%%=%s tuv_swing=%s rem_att_n=%d rem_att=%s rem_def_n=%d rem_def=%s%n",
+              t.getName(),
+              attackers.size(), defenders.size(), attTally, defTally,
+              result.getWinPercentage(), result.getTuvSwing(),
+              remainingAttackers.size(), remAttTally,
+              remainingDefenders.size(), remDefTally);
+        }
         ProLogger.debug("remainingAttackers=" + remainingAttackers);
         ProLogger.debug("remainingDefenders=" + remainingDefenders);
 
@@ -119,7 +152,7 @@ public final class ProSimulateTurnUtils {
 
     final Map<Unit, Territory> unitTerritoryMap = proData.getUnitTerritoryMap();
 
-    final Map<Territory, ProTerritory> result = new HashMap<>();
+    final Map<Territory, ProTerritory> result = new LinkedHashMap<>();
     final List<Unit> usedUnits = new ArrayList<>();
     for (final Territory fromTerritory : moveMap.keySet()) {
       final Territory toTerritory = toData.getMap().getTerritoryOrNull(fromTerritory.getName());
@@ -320,3 +353,4 @@ public final class ProSimulateTurnUtils {
     return null;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProBattleUtils.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProBattleUtils.java
index ad524f2f5..e7314a979 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProBattleUtils.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProBattleUtils.java
@@ -32,6 +32,8 @@ import java.util.Optional;
 import java.util.Set;
 import java.util.function.Predicate;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Pro AI battle utilities. */
 public final class ProBattleUtils {
@@ -104,6 +106,9 @@ public final class ProBattleUtils {
     }
     final double attackerStrength = estimateStrength(t, attackingUnits, defendingUnits, true);
     final double defenderStrength = estimateStrength(t, defendingUnits, attackingUnits, false);
+    if (Boolean.getBoolean("plan") && (t.getName().equals("West Russia") || t.getName().equals("Baltic States"))) {
+      System.out.printf("ESTSTR t=%s a=%d d=%d atkStr=%.6f defStr=%.6f%n", t.getName(), attackingUnits.size(), defendingUnits.size(), attackerStrength, defenderStrength);
+    }
     return ((attackerStrength - defenderStrength) / Math.pow(defenderStrength, 0.85) * 50 + 50);
   }
 
@@ -156,12 +161,27 @@ public final class ProBattleUtils {
                     .territoryEffects(TerritoryEffectHelper.getEffects(t))
                     .build())
             .calculateTotalPower();
+    if (Boolean.getBoolean("plan") && (t.getName().equals("West Russia") || t.getName().equals("Baltic States"))) {
+      System.out.printf("ESTPOW t=%s side=%s u=%d sup=%d power=%d%n", t.getName(), attacking ? "OFFENSE" : "DEFENSE", unitsThatCanFight.size(), data.getUnitTypeList().getSupportRules().size(), myPower);
+      var psar = PowerStrengthAndRolls.build(unitsThatCanFight, CombatValueBuilder.mainCombatValue().enemyUnits(enemyUnits).friendlyUnits(unitsThatCanFight).side(attacking ? BattleState.Side.OFFENSE : BattleState.Side.DEFENSE).gameSequence(data.getSequence()).supportAttachments(data.getUnitTypeList().getSupportRules()).lhtrHeavyBombers(Properties.getLhtrHeavyBombers(data.getProperties())).gameDiceSides(data.getDiceSides()).territoryEffects(TerritoryEffectHelper.getEffects(t)).build());
+      java.util.TreeMap<String, int[]> agg = new java.util.TreeMap<>();
+      for (Unit u : unitsThatCanFight) {
+        String utn = u.getType() == null ? "?" : u.getType().getName();
+        int[] a = agg.computeIfAbsent(utn, k -> new int[3]);
+        a[0]++;
+        a[1] += psar.getStrength(u);
+        a[2] += psar.getPower(u);
+      }
+      for (Map.Entry<String, int[]> e : agg.entrySet()) {
+        System.out.printf("ESTPOW_T t=%s side=%s type=%s n=%d str=%d power=%d%n", t.getName(), attacking ? "OFFENSE" : "DEFENSE", e.getKey(), e.getValue()[0], e.getValue()[1], e.getValue()[2]);
+      }
+    }
     return (myPower * 6.0 / data.getDiceSides());
   }
 
   public static boolean territoryHasLocalLandSuperiority(
       final ProData proData, final Territory t, final int distance, final GamePlayer player) {
-    return territoryHasLocalLandSuperiority(proData, t, distance, player, new HashMap<>());
+    return territoryHasLocalLandSuperiority(proData, t, distance, player, new LinkedHashMap<>());
   }
 
   /**
@@ -253,7 +273,7 @@ public final class ProBattleUtils {
         data.getMap()
             .getNeighbors(t, distance - 1, ProMatches.territoryCanMoveLandUnits(player, false));
     nearbyTerritoriesForAllied.add(t);
-    final Set<Unit> alliedUnits = new HashSet<>();
+    final Set<Unit> alliedUnits = new LinkedHashSet<>();
     for (final Territory nearbyTerritory : nearbyTerritoriesForAllied) {
       if (moveMap.get(nearbyTerritory) != null) {
         alliedUnits.addAll(moveMap.get(nearbyTerritory).getMaxDefenders());
@@ -430,3 +450,4 @@ public final class ProBattleUtils {
     return hasSuperiority;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProOddsCalculator.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProOddsCalculator.java
index a51fa2582..29ab4f1c3 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProOddsCalculator.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProOddsCalculator.java
@@ -54,6 +54,11 @@ public class ProOddsCalculator {
     // Determine if attackers have no chance
     final double strengthDifference =
         ProBattleUtils.estimateStrengthDifference(t, attackingUnits, defendingUnits);
+    if (Boolean.getBoolean("plan") && (t.getName().equals("West Russia") || t.getName().equals("Baltic States"))) {
+      System.out.printf("ESTBR t=%s attackers=%d defenders=%d strDiff=%.4f%n", t.getName(), attackingUnits.size(), defendingUnits.size(), strengthDifference);
+      for (Unit u : attackingUnits) System.out.printf("ESTBR_A t=%s owner=%s type=%s%n", t.getName(), u.getOwner() == null ? "?" : u.getOwner().getName(), u.getType() == null ? "?" : u.getType().getName());
+      for (Unit u : defendingUnits) System.out.printf("ESTBR_D t=%s owner=%s type=%s%n", t.getName(), u.getOwner() == null ? "?" : u.getOwner().getName(), u.getType() == null ? "?" : u.getType().getName());
+    }
     if (strengthDifference < 45) {
       return new ProBattleResult(0, -999, false, List.of(), defendingUnits, 1);
     }
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProPurchaseUtils.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProPurchaseUtils.java
index 457222731..86eb2644e 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProPurchaseUtils.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProPurchaseUtils.java
@@ -35,6 +35,7 @@ import java.util.function.Predicate;
 import lombok.experimental.UtilityClass;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashSet;
 
 /** Pro AI purchase utilities. */
 @UtilityClass
@@ -59,7 +60,15 @@ public final class ProPurchaseUtils {
     }
     final Map<ProPurchaseOption, Double> purchasePercentages = new LinkedHashMap<>();
     double upperBound = 0.0;
-    for (final ProPurchaseOption ppo : purchaseEfficiencies.keySet()) {
+    // triplea-port-bootstrap: stable iteration: sort options by production-rule name so the
+    // snapshot run is reproducible (HashMap iteration order otherwise
+    // depends on identity hashCode). Matches Odin port's
+    // pro_purchase_utils_randomize_purchase_option sort key.
+    final java.util.List<ProPurchaseOption> sortedOptions =
+        new java.util.ArrayList<>(purchaseEfficiencies.keySet());
+    sortedOptions.sort(java.util.Comparator.comparing(
+        ppo -> ppo.getProductionRule().getName()));
+    for (final ProPurchaseOption ppo : sortedOptions) {
       final double chance = purchaseEfficiencies.get(ppo) / totalEfficiency * 100;
       upperBound += chance;
       purchasePercentages.put(ppo, upperBound);
@@ -150,7 +159,7 @@ public final class ProPurchaseUtils {
                 player, false, false, false, false, false));
 
     // Create purchase territory holder for each factory territory
-    final Map<Territory, ProPurchaseTerritory> purchaseTerritories = new HashMap<>();
+    final Map<Territory, ProPurchaseTerritory> purchaseTerritories = new LinkedHashMap<>();
     for (final Territory t : potentialTerritories) {
       final ProPurchaseTerritory ppt = new ProPurchaseTerritory(t, data, player, 1, true);
       purchaseTerritories.put(t, ppt);
@@ -188,7 +197,7 @@ public final class ProPurchaseUtils {
             ProMatches.territoryCanMoveLandUnits(player, false));
 
     // Create purchase territory holder for each factory territory
-    final Map<Territory, ProPurchaseTerritory> purchaseTerritories = new HashMap<>();
+    final Map<Territory, ProPurchaseTerritory> purchaseTerritories = new LinkedHashMap<>();
     for (final Territory t : ownedAndNotConqueredFactoryTerritories) {
       final int unitProduction = getUnitProduction(t, player);
       final ProPurchaseTerritory ppt = new ProPurchaseTerritory(t, data, player, unitProduction);
@@ -318,7 +327,7 @@ public final class ProPurchaseUtils {
       GamePlayer player, Collection<Unit> existingUnits, Collection<Unit> unitsToPlace) {
     Collection<Unit> unitsThatConsume =
         CollectionUtils.getMatches(unitsToPlace, Matches.unitConsumesUnitsOnCreation());
-    Set<Unit> unitsToConsume = new HashSet<>();
+    Set<Unit> unitsToConsume = new LinkedHashSet<>();
     for (Unit unitToBuild : unitsThatConsume) {
       IntegerMap<UnitType> needed = unitToBuild.getUnitAttachment().getConsumesUnits();
       for (UnitType neededType : needed.keySet()) {
@@ -336,3 +345,4 @@ public final class ProPurchaseUtils {
     return unitsToConsume;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProSortMoveOptionsUtils.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProSortMoveOptionsUtils.java
index e958b10ec..6fbd028c8 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProSortMoveOptionsUtils.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProSortMoveOptionsUtils.java
@@ -36,6 +36,7 @@ public final class ProSortMoveOptionsUtils {
   public static Map<Unit, Set<Territory>> sortUnitMoveOptions(
       final ProData proData, final Map<Unit, Set<Territory>> unitAttackOptions) {
 
+    final Map<Unit, Territory> unitTerritoryMap = proData.getUnitTerritoryMap();
     final List<Map.Entry<Unit, Set<Territory>>> list =
         new ArrayList<>(unitAttackOptions.entrySet());
     list.sort(
@@ -49,7 +50,23 @@ public final class ProSortMoveOptionsUtils {
             return (proData.getUnitValue(o1.getKey().getType())
                 - proData.getUnitValue(o2.getKey().getType()));
           }
-          return o1.getKey().getType().getName().compareTo(o2.getKey().getType().getName());
+          final int byType =
+              o1.getKey().getType().getName().compareTo(o2.getKey().getType().getName());
+          if (byType != 0) {
+            return byType;
+          }
+          // Deterministic tiebreakers (matched on Odin port — see
+          // deterministic-maps.md). Home-territory name then alreadyMoved.
+          // Stable sort preserves input-collection order for true ties.
+          final Territory home1 = unitTerritoryMap.get(o1.getKey());
+          final Territory home2 = unitTerritoryMap.get(o2.getKey());
+          final String homeName1 = home1 == null ? "" : home1.getName();
+          final String homeName2 = home2 == null ? "" : home2.getName();
+          final int byHome = homeName1.compareTo(homeName2);
+          if (byHome != 0) {
+            return byHome;
+          }
+          return o1.getKey().getAlreadyMoved().compareTo(o2.getKey().getAlreadyMoved());
         });
     final Map<Unit, Set<Territory>> sortedUnitAttackOptions = new LinkedHashMap<>();
     for (final Map.Entry<Unit, Set<Territory>> entry : list) {
@@ -69,6 +86,7 @@ public final class ProSortMoveOptionsUtils {
       final Map<Unit, Set<Territory>> unitAttackOptions,
       final Map<Territory, ProTerritory> attackMap,
       final ProOddsCalculator calc) {
+    final Map<Unit, Territory> unitTerritoryMap = proData.getUnitTerritoryMap();
     final List<Map.Entry<Unit, Set<Territory>>> list =
         new ArrayList<>(unitAttackOptions.entrySet());
     list.sort(
@@ -89,7 +107,20 @@ public final class ProSortMoveOptionsUtils {
           if (value1 != value2) {
             return value1 - value2;
           }
-          return unitType1.getName().compareTo(unitType2.getName());
+          final int byType = unitType1.getName().compareTo(unitType2.getName());
+          if (byType != 0) {
+            return byType;
+          }
+          // Deterministic tiebreakers — see deterministic-maps.md.
+          final Territory home1 = unitTerritoryMap.get(o1.getKey());
+          final Territory home2 = unitTerritoryMap.get(o2.getKey());
+          final String homeName1 = home1 == null ? "" : home1.getName();
+          final String homeName2 = home2 == null ? "" : home2.getName();
+          final int byHome = homeName1.compareTo(homeName2);
+          if (byHome != 0) {
+            return byHome;
+          }
+          return o1.getKey().getAlreadyMoved().compareTo(o2.getKey().getAlreadyMoved());
         });
     final Map<Unit, Set<Territory>> sortedUnitAttackOptions = new LinkedHashMap<>();
     for (final Map.Entry<Unit, Set<Territory>> entry : list) {
@@ -115,7 +146,7 @@ public final class ProSortMoveOptionsUtils {
 
     final List<Map.Entry<Unit, Set<Territory>>> list =
         new ArrayList<>(unitAttackOptions.entrySet());
-    final Map<Object, Double> attackEfficiencyCache = new HashMap<>();
+    final Map<Object, Double> attackEfficiencyCache = new LinkedHashMap<>();
     list.sort(
         (o1, o2) -> {
           final Collection<Territory> territories1 =
@@ -167,7 +198,20 @@ public final class ProSortMoveOptionsUtils {
             }
           }
 
-          return unitType1.getName().compareTo(unitType2.getName());
+          final int byType = unitType1.getName().compareTo(unitType2.getName());
+          if (byType != 0) {
+            return byType;
+          }
+          // Deterministic tiebreakers — see deterministic-maps.md.
+          final Territory home1 = unitTerritoryMap.get(u1);
+          final Territory home2 = unitTerritoryMap.get(u2);
+          final String homeName1 = home1 == null ? "" : home1.getName();
+          final String homeName2 = home2 == null ? "" : home2.getName();
+          final int byHome = homeName1.compareTo(homeName2);
+          if (byHome != 0) {
+            return byHome;
+          }
+          return u1.getAlreadyMoved().compareTo(u2.getAlreadyMoved());
         });
     final Map<Unit, Set<Territory>> sortedUnitAttackOptions = new LinkedHashMap<>();
     for (final Map.Entry<Unit, Set<Territory>> entry : list) {
@@ -240,3 +284,4 @@ public final class ProSortMoveOptionsUtils {
     return result;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProTerritoryValueUtils.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProTerritoryValueUtils.java
index e6da5f0e3..7e5901783 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProTerritoryValueUtils.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProTerritoryValueUtils.java
@@ -23,6 +23,7 @@ import java.util.function.Predicate;
 import java.util.function.ToIntFunction;
 import lombok.experimental.UtilityClass;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
 
 /** Pro AI battle utilities. */
 @UtilityClass
@@ -62,7 +63,7 @@ public final class ProTerritoryValueUtils {
         findEnemyCapitalsAndFactoriesValue(
             player, maxLandMassSize, territoriesThatCantBeHeld, territoriesToAttack);
 
-    final Map<Territory, Double> territoryValueMap = new HashMap<>();
+    final Map<Territory, Double> territoryValueMap = new LinkedHashMap<>();
     for (final Territory t : territoriesToCheck) {
       if (!t.isWater()) {
         final double value =
@@ -104,7 +105,7 @@ public final class ProTerritoryValueUtils {
       final List<Territory> territoriesToCheck) {
 
     // Determine value for water territories
-    final Map<Territory, Double> territoryValueMap = new HashMap<>();
+    final Map<Territory, Double> territoryValueMap = new LinkedHashMap<>();
     final GameData data = player.getData();
     for (final Territory t : territoriesToCheck) {
       if (!territoriesThatCantBeHeld.contains(t)
@@ -229,7 +230,7 @@ public final class ProTerritoryValueUtils {
     enemyCapitalsAndFactories.removeAll(territoriesToAttack);
 
     // Find value for each enemy capital and factory
-    final Map<Territory, Double> enemyCapitalsAndFactoriesMap = new HashMap<>();
+    final Map<Territory, Double> enemyCapitalsAndFactoriesMap = new LinkedHashMap<>();
     for (final Territory t : enemyCapitalsAndFactories) {
       // Get factory production if factory
       int factoryProduction = 0;
@@ -466,3 +467,4 @@ public final class ProTerritoryValueUtils {
     return found;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProTransportUtils.java b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProTransportUtils.java
index 4d92eba53..66c37436a 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProTransportUtils.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/ai/pro/util/ProTransportUtils.java
@@ -28,6 +28,7 @@ import java.util.Set;
 import java.util.function.Predicate;
 import java.util.stream.Collectors;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashSet;
 
 /** Pro AI transport utilities. */
 public final class ProTransportUtils {
@@ -198,7 +199,7 @@ public final class ProTransportUtils {
 
   public static Set<Unit> getMovedUnits(
       final List<Unit> alreadyMovedUnits, final Map<Territory, ProTerritory> attackMap) {
-    final Set<Unit> movedUnits = new HashSet<>(alreadyMovedUnits);
+    final Set<Unit> movedUnits = new LinkedHashSet<>(alreadyMovedUnits);
     movedUnits.addAll(
         attackMap.values().stream()
             .map(ProTerritory::getAllDefenders)
@@ -549,3 +550,4 @@ public final class ProTransportUtils {
         || result.getTuvSwing() > 0;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/AbstractRulesAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/AbstractRulesAttachment.java
index 43af90592..a6062bf29 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/AbstractRulesAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/AbstractRulesAttachment.java
@@ -23,6 +23,8 @@ import javax.annotation.Nullable;
 import lombok.Getter;
 import org.triplea.java.RenameOnNextMajorRelease;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /**
  * The Purpose of this class is to hold shared and simple methods used by RulesAttachment. Note:
@@ -172,7 +174,7 @@ public abstract class AbstractRulesAttachment extends AbstractConditionsAttachme
   }
 
   private void setRounds(final String rounds) throws GameParseException {
-    turns = new HashMap<>();
+    turns = new LinkedHashMap<>();
     final String[] s = splitOnColon(rounds);
     if (s.length < 1) {
       throw new GameParseException("Empty turn list" + thisErrorMsg());
@@ -234,7 +236,7 @@ public abstract class AbstractRulesAttachment extends AbstractConditionsAttachme
       case "original":
       case "enemy":
         // get all originally owned territories
-        final Set<Territory> originalTerrs = new HashSet<>();
+        final Set<Territory> originalTerrs = new LinkedHashSet<>();
         for (final GamePlayer player : players) {
           originalTerrs.addAll(OriginalOwnerTracker.getOriginallyOwned(data, player));
         }
@@ -242,7 +244,7 @@ public abstract class AbstractRulesAttachment extends AbstractConditionsAttachme
         return originalTerrs;
       case "originalNoWater":
         // get all originally owned territories, but no water or impassables
-        final Set<Territory> originalTerritories = new HashSet<>();
+        final Set<Territory> originalTerritories = new LinkedHashSet<>();
         for (final GamePlayer player : players) {
           originalTerritories.addAll(
               CollectionUtils.getMatches(
@@ -254,14 +256,14 @@ public abstract class AbstractRulesAttachment extends AbstractConditionsAttachme
         return originalTerritories;
 
       case "controlled":
-        final Set<Territory> ownedTerrs = new HashSet<>();
+        final Set<Territory> ownedTerrs = new LinkedHashSet<>();
         for (final GamePlayer player : players) {
           ownedTerrs.addAll(gameMap.getTerritoriesOwnedBy(player));
         }
         setTerritoryCount(ownedTerrs.size());
         return ownedTerrs;
       case "controlledNoWater":
-        final Set<Territory> ownedTerrsNoWater = new HashSet<>();
+        final Set<Territory> ownedTerrsNoWater = new LinkedHashSet<>();
         for (final GamePlayer player : players) {
           ownedTerrsNoWater.addAll(
               CollectionUtils.getMatches(
@@ -271,7 +273,7 @@ public abstract class AbstractRulesAttachment extends AbstractConditionsAttachme
         setTerritoryCount(ownedTerrsNoWater.size());
         return ownedTerrsNoWater;
       case "all":
-        final Set<Territory> allTerrs = new HashSet<>();
+        final Set<Territory> allTerrs = new LinkedHashSet<>();
         for (final GamePlayer player : players) {
           allTerrs.addAll(gameMap.getTerritoriesOwnedBy(player));
           allTerrs.addAll(OriginalOwnerTracker.getOriginallyOwned(data, player));
@@ -287,7 +289,7 @@ public abstract class AbstractRulesAttachment extends AbstractConditionsAttachme
         if (t == null) {
           throw new IllegalStateException("No territory called: " + name + thisErrorMsg());
         }
-        final Set<Territory> terr = new HashSet<>();
+        final Set<Territory> terr = new LinkedHashSet<>();
         terr.add(t);
         setTerritoryCount(1);
         return terr;
@@ -350,7 +352,7 @@ public abstract class AbstractRulesAttachment extends AbstractConditionsAttachme
       final String[] list,
       final boolean testFirstItemForCount,
       final boolean mustSetTerritoryCount) {
-    final Set<Territory> territories = new HashSet<>();
+    final Set<Territory> territories = new LinkedHashSet<>();
     // this list is null, empty, or contains "", so return a blank list of territories
     if (list == null
         || list.length == 0
@@ -440,3 +442,4 @@ public abstract class AbstractRulesAttachment extends AbstractConditionsAttachme
     };
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/CanalAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/CanalAttachment.java
index a63480ec4..789b00aeb 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/CanalAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/CanalAttachment.java
@@ -22,6 +22,7 @@ import javax.annotation.Nullable;
 import lombok.Getter;
 import org.jetbrains.annotations.NonNls;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashSet;
 
 /**
  * An attachment for instances of {@link Territory} that defines a canal through which certain land
@@ -85,7 +86,7 @@ public class CanalAttachment extends DefaultAttachment {
   }
 
   private void setLandTerritories(final String landTerritories) throws GameParseException {
-    final Set<Territory> terrs = new HashSet<>();
+    final Set<Territory> terrs = new LinkedHashSet<>();
     for (final String territoryName : splitOnColon(landTerritories)) {
       final Territory territory =
           getTerritory(territoryName)
@@ -114,7 +115,7 @@ public class CanalAttachment extends DefaultAttachment {
 
   private void setExcludedUnits(final String value) {
     if (excludedUnits == null) {
-      excludedUnits = new HashSet<>();
+      excludedUnits = new LinkedHashSet<>();
     }
     if (value.equalsIgnoreCase("NONE")) {
       return;
@@ -175,7 +176,7 @@ public class CanalAttachment extends DefaultAttachment {
           MessageFormat.format(
               "Canal named ''{0}'' must have landTerritories set!{1}", canalName, thisErrorMsg()));
     }
-    final Set<Territory> territories = new HashSet<>();
+    final Set<Territory> territories = new LinkedHashSet<>();
     for (final Territory t : data.getMap()) {
       if (hasCanal(t, canalName)) {
         territories.add(t);
@@ -221,3 +222,4 @@ public class CanalAttachment extends DefaultAttachment {
     };
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/PlayerAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/PlayerAttachment.java
index 94eaaf02a..a95f9276c 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/PlayerAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/PlayerAttachment.java
@@ -19,6 +19,7 @@ import lombok.Getter;
 import org.jetbrains.annotations.NonNls;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.util.Triple;
+import java.util.LinkedHashSet;
 
 /**
  * An attachment for instances of {@link GamePlayer} that defines properties unrelated to rules (see
@@ -81,7 +82,7 @@ public class PlayerAttachment extends DefaultAttachment {
 
   private void setPlacementLimit(final String value) throws GameParseException {
     if (placementLimit == null) {
-      placementLimit = new HashSet<>();
+      placementLimit = new LinkedHashSet<>();
     }
     placementLimit.add(parseUnitLimit("placementLimit", value));
   }
@@ -100,7 +101,7 @@ public class PlayerAttachment extends DefaultAttachment {
 
   private void setMovementLimit(final String value) throws GameParseException {
     if (movementLimit == null) {
-      movementLimit = new HashSet<>();
+      movementLimit = new LinkedHashSet<>();
     }
     movementLimit.add(parseUnitLimit("movementLimit", value));
   }
@@ -119,7 +120,7 @@ public class PlayerAttachment extends DefaultAttachment {
 
   private void setAttackingLimit(final String value) throws GameParseException {
     if (attackingLimit == null) {
-      attackingLimit = new HashSet<>();
+      attackingLimit = new LinkedHashSet<>();
     }
     attackingLimit.add(parseUnitLimit("attackingLimit", value));
   }
@@ -151,7 +152,7 @@ public class PlayerAttachment extends DefaultAttachment {
       throw new GameParseException(
           type + " type must be: owned, allied, or total" + thisErrorMsg());
     }
-    final Set<UnitType> types = new HashSet<>();
+    final Set<UnitType> types = new LinkedHashSet<>();
     if (s[2].equalsIgnoreCase("all")) {
       types.addAll(getData().getUnitTypeList().getAllUnitTypes());
     } else {
@@ -475,3 +476,4 @@ public class PlayerAttachment extends DefaultAttachment {
     };
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/RulesAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/RulesAttachment.java
index fb4ac1b4b..c67bf3e1e 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/RulesAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/RulesAttachment.java
@@ -43,6 +43,7 @@ import org.triplea.java.Interruptibles;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashSet;
 
 /**
  * An attachment for instances of {@link GamePlayer} that defines various conditions for enabling
@@ -137,7 +138,7 @@ public class RulesAttachment extends AbstractPlayerRulesAttachment {
    * inside that trigger.
    */
   public static Set<RulesAttachment> getNationalObjectives(final GamePlayer player) {
-    final Set<RulesAttachment> natObjs = new HashSet<>();
+    final Set<RulesAttachment> natObjs = new LinkedHashSet<>();
     final Map<String, IAttachment> map = player.getAttachments();
     for (final Map.Entry<String, IAttachment> entry : map.entrySet()) {
       final IAttachment attachment = entry.getValue();
@@ -541,7 +542,7 @@ public class RulesAttachment extends AbstractPlayerRulesAttachment {
     if (s.length == 1 && count != -1) {
       throw new GameParseException("Empty enemy list" + thisErrorMsg());
     }
-    atWarPlayers = new HashSet<>();
+    atWarPlayers = new LinkedHashSet<>();
     for (int i = count == -1 ? 0 : 1; i < s.length; i++) {
       final int currentIndex = i;
       atWarPlayers.add(
@@ -632,7 +633,7 @@ public class RulesAttachment extends AbstractPlayerRulesAttachment {
               .orElseGet(
                   () ->
                       testAllConditionsRecursive(
-                          getAllConditionsRecursive(new HashSet<>(conditions), null),
+                          getAllConditionsRecursive(new LinkedHashSet<>(conditions), null),
                           null,
                           delegateBridge));
       objectiveMet =
@@ -1237,3 +1238,4 @@ public class RulesAttachment extends AbstractPlayerRulesAttachment {
     };
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TechAbilityAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TechAbilityAttachment.java
index 01c60ddce..fcefafa00 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TechAbilityAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TechAbilityAttachment.java
@@ -30,6 +30,8 @@ import lombok.Getter;
 import org.jetbrains.annotations.NonNls;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /**
  * Attaches to technologies. Also contains static methods of interpreting data from all technology
@@ -411,9 +413,9 @@ public class TechAbilityAttachment extends DefaultAttachment {
     // validate that this unit exists in the xml
     final UnitType ut = getUnitTypeOrThrow(unitType);
     if (unitAbilitiesGained == null) {
-      unitAbilitiesGained = new HashMap<>();
+      unitAbilitiesGained = new LinkedHashMap<>();
     }
-    final Set<String> abilities = unitAbilitiesGained.computeIfAbsent(ut, key -> new HashSet<>());
+    final Set<String> abilities = unitAbilitiesGained.computeIfAbsent(ut, key -> new LinkedHashSet<>());
     // start at 1
     for (int i = 1; i < s.length; i++) {
       final String ability = s[i];
@@ -580,12 +582,12 @@ public class TechAbilityAttachment extends DefaultAttachment {
       throw new GameParseException(
           "airborneTargettedByAA must have at least two fields" + thisErrorMsg());
     }
-    final Set<UnitType> unitTypes = new HashSet<>();
+    final Set<UnitType> unitTypes = new LinkedHashSet<>();
     for (int i = 1; i < s.length; i++) {
       unitTypes.add(getUnitTypeOrThrow(s[i]));
     }
     if (airborneTargetedByAa == null) {
-      airborneTargetedByAa = new HashMap<>();
+      airborneTargetedByAa = new LinkedHashMap<>();
     }
     airborneTargetedByAa.put(s[0].intern(), unitTypes);
   }
@@ -605,7 +607,7 @@ public class TechAbilityAttachment extends DefaultAttachment {
    */
   public static Map<String, Set<UnitType>> getAirborneTargettedByAa(
       final Collection<TechAdvance> techAdvances) {
-    final Map<String, Set<UnitType>> airborneTargettedByAa = new HashMap<>();
+    final Map<String, Set<UnitType>> airborneTargettedByAa = new LinkedHashMap<>();
     for (final TechAdvance ta : techAdvances) {
       final TechAbilityAttachment taa = TechAbilityAttachment.get(ta);
       if (taa != null) {
@@ -613,7 +615,7 @@ public class TechAbilityAttachment extends DefaultAttachment {
         if (mapAa != null && !mapAa.isEmpty()) {
           for (final Entry<String, Set<UnitType>> entry : mapAa.entrySet()) {
             airborneTargettedByAa
-                .computeIfAbsent(entry.getKey(), key -> new HashSet<>())
+                .computeIfAbsent(entry.getKey(), key -> new LinkedHashSet<>())
                 .addAll(entry.getValue());
           }
         }
@@ -1051,3 +1053,4 @@ public class TechAbilityAttachment extends DefaultAttachment {
     };
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TechAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TechAttachment.java
index 2999f92ff..0cf9d49e4 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TechAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TechAttachment.java
@@ -15,6 +15,7 @@ import java.util.Map;
 import java.util.Optional;
 import lombok.Getter;
 import org.jetbrains.annotations.NonNls;
+import java.util.LinkedHashMap;
 
 /**
  * An attachment for instances of {@link GamePlayer} that defines properties related to technology
@@ -40,7 +41,7 @@ public class TechAttachment extends DefaultAttachment {
   private boolean mechanizedInfantry = false;
   private boolean aaRadar = false;
   private boolean shipyards = false;
-  @Getter private final Map<String, Boolean> genericTech = new HashMap<>();
+  @Getter private final Map<String, Boolean> genericTech = new LinkedHashMap<>();
 
   public TechAttachment(final String name, final Attachable attachable, final GameData gameData) {
     super(name, attachable, gameData);
@@ -409,3 +410,4 @@ public class TechAttachment extends DefaultAttachment {
     };
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TerritoryAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TerritoryAttachment.java
index ff7cdf5c6..5a8d16627 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TerritoryAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TerritoryAttachment.java
@@ -36,6 +36,7 @@ import lombok.Getter;
 import lombok.ToString;
 import org.jetbrains.annotations.NonNls;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashSet;
 
 /**
  * An attachment for instances of {@link Territory}.
@@ -259,10 +260,10 @@ public class TerritoryAttachment extends DefaultAttachment {
         TerritoryAttachment.get(territory);
     if (optionalTerritoryAttachment.isEmpty()
         || !optionalTerritoryAttachment.get().getConvoyRoute()) {
-      return new HashSet<>();
+      return new LinkedHashSet<>();
     }
 
-    final Collection<Territory> territories = new HashSet<>();
+    final Collection<Territory> territories = new LinkedHashSet<>();
     // already checked above
     data.getMap().getTerritories().stream()
         .filter(current -> !current.equals(territory))
@@ -641,7 +642,7 @@ public class TerritoryAttachment extends DefaultAttachment {
                               "TerritoryAttachment: No territory found for {0}; Setting convoyAttached not possible with value {1}",
                               subString, value)));
       if (convoyAttached == null) {
-        convoyAttached = new HashSet<>();
+        convoyAttached = new LinkedHashSet<>();
       }
       convoyAttached.add(territory);
     }
@@ -988,3 +989,4 @@ public class TerritoryAttachment extends DefaultAttachment {
     public final GamePlayer receivingPlayer;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TerritoryEffectAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TerritoryEffectAttachment.java
index 057098b4f..d4ee679a9 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TerritoryEffectAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TerritoryEffectAttachment.java
@@ -20,6 +20,7 @@ import java.util.Optional;
 import javax.annotation.Nullable;
 import org.jetbrains.annotations.NonNls;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
 
 /**
  * An attachment for instances of {@link TerritoryEffect}. Note: Empty collection fields default to
@@ -135,7 +136,7 @@ public class TerritoryEffectAttachment extends DefaultAttachment {
     }
     while (iter.hasNext()) {
       if (movementCostModifier == null) {
-        movementCostModifier = new HashMap<>();
+        movementCostModifier = new LinkedHashMap<>();
       }
       movementCostModifier.put(getUnitTypeOrThrow(iter.next()), effect);
     }
@@ -246,3 +247,4 @@ public class TerritoryEffectAttachment extends DefaultAttachment {
     };
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TriggerAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TriggerAttachment.java
index 3eeb56b4d..8e80a7c37 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TriggerAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/TriggerAttachment.java
@@ -64,6 +64,7 @@ import org.triplea.java.collections.IntegerMap;
 import org.triplea.sound.ISound;
 import org.triplea.sound.SoundPath;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashSet;
 
 /**
  * An attachment for instances of {@link GamePlayer} that defines actions to be triggered upon
@@ -188,7 +189,7 @@ public class TriggerAttachment extends AbstractTriggerAttachment {
   static Set<TriggerAttachment> getTriggers(
       final GamePlayer player, final Predicate<TriggerAttachment> cond) {
     Preconditions.checkNotNull(cond);
-    final Set<TriggerAttachment> trigs = new HashSet<>();
+    final Set<TriggerAttachment> trigs = new LinkedHashSet<>();
     for (final IAttachment a : player.getAttachments().values()) {
       if (a instanceof TriggerAttachment && cond.test((TriggerAttachment) a)) {
         trigs.add((TriggerAttachment) a);
@@ -222,7 +223,7 @@ public class TriggerAttachment extends AbstractTriggerAttachment {
       return;
     }
     TriggerAttachment.fireTriggers(
-        new HashSet<>(toFireTestedAndSatisfied),
+        new LinkedHashSet<>(toFireTestedAndSatisfied),
         testedConditions,
         bridge,
         new FireTriggerParams(beforeOrAfter, stepName, true, true, true, true));
@@ -269,7 +270,7 @@ public class TriggerAttachment extends AbstractTriggerAttachment {
     // all triggers at this point have their conditions satisfied so we now test chance (because we
     // test chance last),
     // and remove any conditions that do not succeed in their dice rolls
-    final Set<TriggerAttachment> triggersToFire = new HashSet<>();
+    final Set<TriggerAttachment> triggersToFire = new LinkedHashSet<>();
     for (final TriggerAttachment t : triggersToBeFired) {
       if (initialFireTriggerParams.testChance && !t.testChance(bridge)) {
         continue;
@@ -539,7 +540,7 @@ public class TriggerAttachment extends AbstractTriggerAttachment {
     final GameState data = bridge.getData();
     final Collection<TriggerAttachment> trigs =
         filterSatisfiedTriggers(satisfiedTriggers, notificationMatch(), fireTriggerParams);
-    final Set<String> notifications = new HashSet<>();
+    final Set<String> notifications = new LinkedHashSet<>();
     for (final TriggerAttachment t : trigs) {
       if (fireTriggerParams.testChance && !t.testChance(bridge)) {
         continue;
@@ -704,7 +705,7 @@ public class TriggerAttachment extends AbstractTriggerAttachment {
     final Collection<TriggerAttachment> trigs =
         filterSatisfiedTriggers(satisfiedTriggers, territoryPropertyMatch(), fireTriggerParams);
     final CompositeChange change = new CompositeChange();
-    final Set<Territory> territoriesNeedingReDraw = new HashSet<>();
+    final Set<Territory> territoriesNeedingReDraw = new LinkedHashSet<>();
     for (final TriggerAttachment t : trigs) {
       if (fireTriggerParams.testChance && !t.testChance(bridge)) {
         continue;
@@ -1405,7 +1406,7 @@ public class TriggerAttachment extends AbstractTriggerAttachment {
             break;
           }
         }
-        final Set<TriggerAttachment> toFireSet = new HashSet<>();
+        final Set<TriggerAttachment> toFireSet = new LinkedHashSet<>();
         toFireSet.add(toFire);
         final String[] options = splitOnColon(tuple.getSecond());
         final int numberOfTimesToFire = getInt(options[0]);
@@ -1800,7 +1801,7 @@ public class TriggerAttachment extends AbstractTriggerAttachment {
       tlist.put(ta, add);
     }
     if (availableTech == null) {
-      availableTech = new HashMap<>();
+      availableTech = new LinkedHashMap<>();
     }
     if (availableTech.containsKey(cat)) {
       tlist.putAll(availableTech.get(cat));
@@ -2441,7 +2442,7 @@ public class TriggerAttachment extends AbstractTriggerAttachment {
       map.add(getUnitTypeOrThrow(s[i]), count);
     }
     if (placement == null) {
-      placement = new HashMap<>();
+      placement = new LinkedHashMap<>();
     }
     if (placement.containsKey(territory)) {
       map.add(placement.get(territory));
@@ -2463,7 +2464,7 @@ public class TriggerAttachment extends AbstractTriggerAttachment {
 
   private void setRemoveUnits(final String value) throws GameParseException {
     if (removeUnits == null) {
-      removeUnits = new HashMap<>();
+      removeUnits = new LinkedHashMap<>();
     }
     final String[] s = splitOnColon(value);
     if (s.length < 1) {
@@ -2808,3 +2809,4 @@ public class TriggerAttachment extends AbstractTriggerAttachment {
     };
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UnitAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UnitAttachment.java
index d065010b1..f865753ea 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UnitAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UnitAttachment.java
@@ -48,6 +48,7 @@ import org.triplea.java.ChangeOnNextMajorRelease;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashSet;
 
 /**
  * Despite the misleading name, this attaches not to individual Units but to UnitTypes. Note: Empty
@@ -532,7 +533,7 @@ public class UnitAttachment extends DefaultAttachment {
     }
     final UnitType unitType = getUnitTypeOrThrow(s[2]);
     if (whenHitPointsDamagedChangesInto == null) {
-      whenHitPointsDamagedChangesInto = new HashMap<>();
+      whenHitPointsDamagedChangesInto = new LinkedHashMap<>();
     }
     whenHitPointsDamagedChangesInto.put(getInt(s[0]), Tuple.of(getBool(s[1]), unitType));
   }
@@ -559,7 +560,7 @@ public class UnitAttachment extends DefaultAttachment {
     }
     final UnitType unitType = getUnitTypeOrThrow(s[2]);
     if (whenHitPointsRepairedChangesInto == null) {
-      whenHitPointsRepairedChangesInto = new HashMap<>();
+      whenHitPointsRepairedChangesInto = new LinkedHashMap<>();
     }
     whenHitPointsRepairedChangesInto.put(getInt(s[0]), Tuple.of(getBool(s[1]), unitType));
   }
@@ -798,7 +799,7 @@ public class UnitAttachment extends DefaultAttachment {
               ? new HashSet<>(
                   CollectionUtils.getMatches(
                       getData().getUnitTypeList().getAllUnitTypes(), Matches.unitTypeIsAir()))
-              : new HashSet<>();
+              : new LinkedHashSet<>();
     }
     return getSetProperty(canNotBeTargetedBy);
   }
@@ -1021,7 +1022,7 @@ public class UnitAttachment extends DefaultAttachment {
         throw new GameParseException("special does not allow: " + option + thisErrorMsg());
       }
       if (special == null) {
-        special = new HashSet<>();
+        special = new LinkedHashSet<>();
       }
       special.add(option.intern());
     }
@@ -1243,7 +1244,7 @@ public class UnitAttachment extends DefaultAttachment {
       return new ArrayList<>();
     }
     final Collection<Unit> unitsCopy = new ArrayList<>(units);
-    final Set<Unit> whichReceiveNoDuplicates = new HashSet<>();
+    final Set<Unit> whichReceiveNoDuplicates = new LinkedHashSet<>();
     final IntegerMap<Tuple<String, String>> whichGive =
         getReceivesAbilityWhenWithMap(unitsCopy, filterForAbility, unitTypeList);
     for (final Tuple<String, String> abilityUnitType : whichGive.keySet()) {
@@ -2025,7 +2026,7 @@ public class UnitAttachment extends DefaultAttachment {
   public static Set<UnitType> getAllowedBombingTargetsIntersection(
       final Collection<Unit> bombersOrRockets, final UnitTypeList unitTypeList) {
     if (bombersOrRockets.isEmpty()) {
-      return new HashSet<>();
+      return new LinkedHashSet<>();
     }
     Collection<UnitType> allowedTargets = unitTypeList.getAllUnitTypes();
     for (final Unit u : bombersOrRockets) {
@@ -2033,7 +2034,7 @@ public class UnitAttachment extends DefaultAttachment {
       final Set<UnitType> bombingTargets = ua.getBombingTargets(unitTypeList);
       allowedTargets = CollectionUtils.intersection(allowedTargets, bombingTargets);
     }
-    return new HashSet<>(allowedTargets);
+    return new LinkedHashSet<>(allowedTargets);
   }
 
   private void setIsAa(final String s) throws GameParseException {
@@ -2270,7 +2271,7 @@ public class UnitAttachment extends DefaultAttachment {
   }
 
   public static List<String> getAllOfTypeAas(final Collection<Unit> aaUnitsAlreadyVerified) {
-    final Set<String> aaSet = new HashSet<>();
+    final Set<String> aaSet = new LinkedHashSet<>();
     for (final Unit u : aaUnitsAlreadyVerified) {
       aaSet.add(u.getUnitAttachment().getTypeAa());
     }
@@ -2667,7 +2668,7 @@ public class UnitAttachment extends DefaultAttachment {
 
   private Collection<Territory> getListedTerritories(final String[] list)
       throws GameParseException {
-    final Set<Territory> territories = new HashSet<>();
+    final Set<Territory> territories = new LinkedHashSet<>();
     for (final String name : list) {
       // Validate all territories exist
       final Territory territory = getData().getMap().getTerritoryOrNull(name);
@@ -4151,3 +4152,4 @@ public class UnitAttachment extends DefaultAttachment {
     };
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UnitSupportAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UnitSupportAttachment.java
index ba5fe5084..5c53cba47 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UnitSupportAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UnitSupportAttachment.java
@@ -29,6 +29,7 @@ import lombok.Getter;
 import lombok.ToString;
 import lombok.Value;
 import org.jetbrains.annotations.NonNls;
+import java.util.LinkedHashSet;
 
 /**
  * An attachment for instances of {@link UnitType} that defines properties for unit types that
@@ -158,7 +159,7 @@ public class UnitSupportAttachment extends DefaultAttachment {
   }
 
   private void setUnitType(final String names) throws GameParseException {
-    unitType = new HashSet<>();
+    unitType = new LinkedHashSet<>();
     for (final String element : splitOnColon(names)) {
       unitType.add(getUnitTypeOrThrow(element));
     }
@@ -436,7 +437,7 @@ public class UnitSupportAttachment extends DefaultAttachment {
 
   private void addUnitTypes(final Set<UnitType> types) {
     if (unitType == null) {
-      unitType = new HashSet<>();
+      unitType = new LinkedHashSet<>();
     }
     unitType.addAll(types);
   }
@@ -474,3 +475,4 @@ public class UnitSupportAttachment extends DefaultAttachment {
     return PropertyEnum.getPropertyOrEmpty(PropertyName.class, propertyName, this);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UserActionAttachment.java b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UserActionAttachment.java
index 615128ecd..0be93a2a2 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UserActionAttachment.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/attachments/UserActionAttachment.java
@@ -22,6 +22,7 @@ import java.util.stream.Collectors;
 import javax.annotation.Nullable;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashSet;
 
 /**
  * A class of attachments that can be "activated" during a user action delegate. For now they will
@@ -121,7 +122,7 @@ public class UserActionAttachment extends AbstractUserActionAttachment {
         continue;
       }
       final TriggerAttachment toFire = optionalTrigger.get();
-      final Set<TriggerAttachment> toFireSet = new HashSet<>();
+      final Set<TriggerAttachment> toFireSet = new LinkedHashSet<>();
       toFireSet.add(toFire);
       final String[] options = splitOnColon(tuple.getSecond());
       final int numberOfTimesToFire = getInt(options[0]);
@@ -157,7 +158,7 @@ public class UserActionAttachment extends AbstractUserActionAttachment {
   }
 
   public Set<GamePlayer> getOtherPlayers() {
-    final Set<GamePlayer> otherPlayers = new HashSet<>();
+    final Set<GamePlayer> otherPlayers = new LinkedHashSet<>();
     otherPlayers.add((GamePlayer) this.getAttachedTo());
     otherPlayers.addAll(getActionAccept());
     return otherPlayers;
@@ -185,3 +186,4 @@ public class UserActionAttachment extends AbstractUserActionAttachment {
     };
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/AbstractEndTurnDelegate.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/AbstractEndTurnDelegate.java
index 65f34a521..3fea664b9 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/AbstractEndTurnDelegate.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/AbstractEndTurnDelegate.java
@@ -46,6 +46,7 @@ import org.triplea.java.Interruptibles;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashMap;
 
 /** At the end of the turn collect income. */
 public abstract class AbstractEndTurnDelegate extends BaseTripleADelegate
@@ -514,7 +515,7 @@ public abstract class AbstractEndTurnDelegate extends BaseTripleADelegate
     final boolean rollDiceForBlockadeDamage =
         Properties.getConvoyBlockadesRollDiceForCost(data.getProperties());
     final Collection<String> transcripts = new ArrayList<>();
-    final Map<Territory, Tuple<Integer, List<Territory>>> damagePerBlockadeZone = new HashMap<>();
+    final Map<Territory, Tuple<Integer, List<Territory>>> damagePerBlockadeZone = new LinkedHashMap<>();
     boolean rolledDice = false;
     for (final Territory b : blockable) {
       // match will check for land, convoy zones, and also contested territories
@@ -699,3 +700,4 @@ public abstract class AbstractEndTurnDelegate extends BaseTripleADelegate
         });
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/AbstractPlaceDelegate.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/AbstractPlaceDelegate.java
index b7f7e57fe..915b562e7 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/AbstractPlaceDelegate.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/AbstractPlaceDelegate.java
@@ -47,6 +47,7 @@ import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.sound.SoundPath;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashMap;
 
 /**
  * Logic for placing units.
@@ -62,7 +63,7 @@ import org.triplea.util.Tuple;
 public abstract class AbstractPlaceDelegate extends BaseTripleADelegate
     implements IAbstractPlaceDelegate {
   // maps Territory-> Collection of units
-  protected Map<Territory, Collection<Unit>> produced = new HashMap<>();
+  protected Map<Territory, Collection<Unit>> produced = new LinkedHashMap<>();
   // a list of CompositeChanges
   protected List<UndoablePlacement> placements = new ArrayList<>();
 
@@ -90,7 +91,7 @@ public abstract class AbstractPlaceDelegate extends BaseTripleADelegate
       bridge.addChange(change);
     }
     // reset ourselves for next turn
-    produced = new HashMap<>();
+    produced = new LinkedHashMap<>();
     placements.clear();
     if (GameStepPropertiesHelper.isRemoveAirThatCanNotLand(data)) {
       removeAirThatCantLand();
@@ -383,7 +384,7 @@ public abstract class AbstractPlaceDelegate extends BaseTripleADelegate
     // placements of the producer that could be redone by other territories
     final List<UndoablePlacement> redoPlacements = new ArrayList<>();
     // territories the producer produced for (but not itself) and the amount of units it produced
-    final Map<Territory, Integer> redoPlacementsCount = new HashMap<>();
+    final Map<Territory, Integer> redoPlacementsCount = new LinkedHashMap<>();
     // find map place territory -> possible free space for producer
     for (final UndoablePlacement placement : placements) {
       // find placement move of producer that can be taken over
@@ -1108,7 +1109,7 @@ public abstract class AbstractPlaceDelegate extends BaseTripleADelegate
     }
     producers.sort(getBestProducerComparator(to, units, player));
     final Collection<Territory> notUsableAsOtherProducers = new ArrayList<>(producers);
-    final Map<Territory, Integer> currentAvailablePlacementForOtherProducers = new HashMap<>();
+    final Map<Territory, Integer> currentAvailablePlacementForOtherProducers = new LinkedHashMap<>();
     for (final Territory producerTerritory : producers) {
       final int prodT =
           getMaxUnitsToBePlacedFrom(
@@ -1697,3 +1698,4 @@ public abstract class AbstractPlaceDelegate extends BaseTripleADelegate
     return IAbstractPlaceDelegate.class;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/EditDelegate.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/EditDelegate.java
index fcf12d3f2..92a3f5b2e 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/EditDelegate.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/EditDelegate.java
@@ -31,6 +31,7 @@ import javax.annotation.Nullable;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.util.Triple;
+import java.util.LinkedHashSet;
 
 /** Edit game state. */
 public class EditDelegate extends BasePersistentDelegate implements IEditDelegate {
@@ -62,7 +63,7 @@ public class EditDelegate extends BasePersistentDelegate implements IEditDelegat
     if (units.isEmpty()) {
       return null;
     }
-    final Collection<GamePlayer> owners = new HashSet<>();
+    final Collection<GamePlayer> owners = new LinkedHashSet<>();
     for (final Unit u : units) {
       owners.add(u.getOwner());
     }
@@ -436,3 +437,4 @@ public class EditDelegate extends BasePersistentDelegate implements IEditDelegat
     }
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/EndTurnDelegate.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/EndTurnDelegate.java
index 2cda16390..a0edede5b 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/EndTurnDelegate.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/EndTurnDelegate.java
@@ -40,6 +40,8 @@ import java.util.function.Predicate;
 import org.triplea.java.Interruptibles;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** At the end of the turn collect income. */
 @AutoSave(afterStepStart = true)
@@ -237,7 +239,7 @@ public class EndTurnDelegate extends AbstractEndTurnDelegate {
     final IDelegateBridge bridge = new ObjectiveDummyDelegateBridge(data);
 
     // Find and test all the conditions for triggers and national objectives
-    final Set<TriggerAttachment> triggers = new HashSet<>();
+    final Set<TriggerAttachment> triggers = new LinkedHashSet<>();
     final List<RulesAttachment> objectives = new ArrayList<>();
     final Map<ICondition, Boolean> testedConditions =
         testNationalObjectivesAndTriggers(player, data, bridge, triggers, objectives);
@@ -278,7 +280,7 @@ public class EndTurnDelegate extends AbstractEndTurnDelegate {
     final GamePlayer player = data.getSequence().getStep().getPlayerId();
 
     // Find and test all the conditions for triggers and national objectives
-    final Set<TriggerAttachment> triggers = new HashSet<>();
+    final Set<TriggerAttachment> triggers = new LinkedHashSet<>();
     final List<RulesAttachment> objectives = new ArrayList<>();
     final Map<ICondition, Boolean> testedConditions =
         testNationalObjectivesAndTriggers(player, data, bridge, triggers, objectives);
@@ -349,7 +351,7 @@ public class EndTurnDelegate extends AbstractEndTurnDelegate {
 
     // First figure out all the conditions that will be tested, so we can test them all at the same
     // time.
-    final Set<ICondition> allConditionsNeeded = new HashSet<>();
+    final Set<ICondition> allConditionsNeeded = new LinkedHashSet<>();
     final boolean useTriggers = Properties.getTriggers(data.getProperties());
     if (useTriggers) {
 
@@ -362,16 +364,16 @@ public class EndTurnDelegate extends AbstractEndTurnDelegate {
           TriggerAttachment.collectForAllTriggersMatching(
               Set.of(player), endTurnDelegateTriggerMatch));
       allConditionsNeeded.addAll(
-          AbstractConditionsAttachment.getAllConditionsRecursive(new HashSet<>(triggers), null));
+          AbstractConditionsAttachment.getAllConditionsRecursive(new LinkedHashSet<>(triggers), null));
     }
 
     // Add conditions required for national objectives (nat objs that have uses left)
     objectives.addAll(
         CollectionUtils.getMatches(RulesAttachment.getNationalObjectives(player), availableUses));
     allConditionsNeeded.addAll(
-        AbstractConditionsAttachment.getAllConditionsRecursive(new HashSet<>(objectives), null));
+        AbstractConditionsAttachment.getAllConditionsRecursive(new LinkedHashSet<>(objectives), null));
     if (allConditionsNeeded.isEmpty()) {
-      return new HashMap<>();
+      return new LinkedHashMap<>();
     }
 
     // Now test all the conditions
@@ -436,3 +438,4 @@ public class EndTurnDelegate extends AbstractEndTurnDelegate {
     return resources;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/GameStepPropertiesHelper.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/GameStepPropertiesHelper.java
index 4fe14780d..dde7a84aa 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/GameStepPropertiesHelper.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/GameStepPropertiesHelper.java
@@ -15,6 +15,7 @@ import java.util.Set;
 import javax.annotation.Nullable;
 import lombok.experimental.UtilityClass;
 import lombok.extern.slf4j.Slf4j;
+import java.util.LinkedHashSet;
 
 /**
  * A helper class for determining Game Step Properties. These are things such as whether a move
@@ -53,7 +54,7 @@ public final class GameStepPropertiesHelper {
 
   private static Set<GamePlayer> getPlayersFromProperty(
       final GameData gameData, final String propertyKey, final @Nullable GamePlayer defaultPlayer) {
-    final Set<GamePlayer> players = new HashSet<>();
+    final Set<GamePlayer> players = new LinkedHashSet<>();
     if (defaultPlayer != null) {
       players.add(defaultPlayer);
     }
@@ -305,3 +306,4 @@ public final class GameStepPropertiesHelper {
     return GameStep.isPurchaseStepName(data.getSequence().getStep().getName());
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/MoveDelegate.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/MoveDelegate.java
index fe76b5acc..f59a29f2e 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/MoveDelegate.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/MoveDelegate.java
@@ -41,6 +41,7 @@ import org.triplea.java.PredicateBuilder;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashMap;
 
 /**
  * Responsible for moving units on the board.
@@ -449,7 +450,7 @@ public class MoveDelegate extends AbstractMoveDelegate {
     final Predicate<Unit> damagedUnits =
         Matches.unitHasMoreThanOneHitPointTotal().and(Matches.unitHasTakenSomeDamage());
     final Predicate<Unit> damagedUnitsOwned = damagedUnits.and(Matches.unitIsOwnedBy(player));
-    final Map<Territory, Set<Unit>> damagedMap = new HashMap<>();
+    final Map<Territory, Set<Unit>> damagedMap = new LinkedHashMap<>();
     for (final Territory current : data.getMap().getTerritories()) {
       final Set<Unit> damaged;
       if (!Properties.getTwoHitPointUnitsRequireRepairFacilities(data.getProperties())) {
@@ -469,7 +470,7 @@ public class MoveDelegate extends AbstractMoveDelegate {
     if (damagedMap.isEmpty()) {
       return;
     }
-    final Map<Unit, Territory> fullyRepaired = new HashMap<>();
+    final Map<Unit, Territory> fullyRepaired = new LinkedHashMap<>();
     final IntegerMap<Unit> newHitsMap = new IntegerMap<>();
     final var territoriesToNotify = new HashSet<Territory>();
     for (final Entry<Territory, Set<Unit>> entry : damagedMap.entrySet()) {
@@ -732,3 +733,4 @@ public class MoveDelegate extends AbstractMoveDelegate {
     pusLost.add(t, amt);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/MovePerformer.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/MovePerformer.java
index 5460b985d..edf03ac10 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/MovePerformer.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/MovePerformer.java
@@ -37,6 +37,8 @@ import java.util.Set;
 import java.util.function.Predicate;
 import org.triplea.java.PredicateBuilder;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Used to move units and make changes to game state. */
 public class MovePerformer implements Serializable {
@@ -121,7 +123,7 @@ public class MovePerformer implements Serializable {
           @Override
           public void execute(final ExecutionStack stack, final IDelegateBridge bridge) {
             final Collection<Unit> aaCasualties = fireAa(route, units);
-            final Set<Unit> aaCasualtiesWithDependents = new HashSet<>();
+            final Set<Unit> aaCasualtiesWithDependents = new LinkedHashSet<>();
             // need to remove any dependents here
             if (aaCasualties != null) {
               aaCasualtiesWithDependents.addAll(aaCasualties);
@@ -232,8 +234,8 @@ public class MovePerformer implements Serializable {
                   }
                   if (target != null) {
                     targetedAttack = true;
-                    final Map<Unit, Set<Unit>> targets = new HashMap<>();
-                    targets.put(target, new HashSet<>(arrived));
+                    final Map<Unit, Set<Unit>> targets = new LinkedHashMap<>();
+                    targets.put(target, new LinkedHashSet<>(arrived));
                     // createdBattle = true;
                     getBattleTracker()
                         .addBattle(
@@ -389,7 +391,7 @@ public class MovePerformer implements Serializable {
     final boolean paratroopsLanding =
         arrived.stream().anyMatch(paratroopNAirTransports)
             && MoveValidator.allLandUnitsHaveAirTransport(arrived);
-    final Map<Unit, Collection<Unit>> dependentAirTransportableUnits = new HashMap<>();
+    final Map<Unit, Collection<Unit>> dependentAirTransportableUnits = new LinkedHashMap<>();
     for (final Unit unit : arrived) {
       Unit transport = unit.getTransportedBy();
       if (transport != null) {
@@ -436,7 +438,7 @@ public class MovePerformer implements Serializable {
       }
     }
     if (route.isUnload() || paratroopsLanding) {
-      final Set<Unit> units = new HashSet<>();
+      final Set<Unit> units = new LinkedHashSet<>();
       units.addAll(transporting.values());
       units.addAll(transporting.keySet());
       // if there are multiple units on a single transport, the transport will be in units list
@@ -499,3 +501,4 @@ public class MovePerformer implements Serializable {
     return unitsToRemove;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/PoliticsDelegate.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/PoliticsDelegate.java
index 2e91807d8..97a9d9916 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/PoliticsDelegate.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/PoliticsDelegate.java
@@ -602,7 +602,7 @@ public class PoliticsDelegate extends BaseTripleADelegate implements IPoliticsDe
     // first do alliances. then, do war (since we don't want to declare war on a potential ally).
     final Collection<GamePlayer> players = data.getPlayerList().getPlayers();
     for (final GamePlayer p1 : players) {
-      final Set<GamePlayer> p1NewAllies = new HashSet<>();
+      final Set<GamePlayer> p1NewAllies = new LinkedHashSet<>();
       final Collection<GamePlayer> p1AlliedWith =
           CollectionUtils.getMatches(players, Matches.isAlliedAndAlliancesCanChainTogether(p1));
       for (final GamePlayer p2 : p1AlliedWith) {
@@ -635,7 +635,7 @@ public class PoliticsDelegate extends BaseTripleADelegate implements IPoliticsDe
       return;
     }
     for (final GamePlayer p1 : players) {
-      final Set<GamePlayer> p1NewWar = new HashSet<>();
+      final Set<GamePlayer> p1NewWar = new LinkedHashSet<>();
       final Collection<GamePlayer> p1WarWith =
           CollectionUtils.getMatches(players, Matches.isAtWar(p1));
       final Collection<GamePlayer> p1AlliedWith =
@@ -692,3 +692,4 @@ public class PoliticsDelegate extends BaseTripleADelegate implements IPoliticsDe
     }
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/RandomStartDelegate.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/RandomStartDelegate.java
index e089c3da5..168f79693 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/RandomStartDelegate.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/RandomStartDelegate.java
@@ -22,6 +22,7 @@ import java.util.function.Predicate;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashSet;
 
 /**
  * This delegate sets up the game according to Risk rules, with a few allowed customizations. Either
@@ -105,7 +106,7 @@ public class RandomStartDelegate extends BaseTripleADelegate {
         final List<Unit> units = new ArrayList<>(currentPickingPlayer.getUnits());
 
         units.sort(Comparator.comparingInt(unit -> costs.getInt(unit.getType())));
-        final Set<Unit> unitsToPlace = new HashSet<>();
+        final Set<Unit> unitsToPlace = new LinkedHashSet<>();
         unitsToPlace.add(units.get(0));
         picked = allPickableTerritories.get(pos % allPickableTerritories.size());
         final CompositeChange change = new CompositeChange();
@@ -279,3 +280,4 @@ public class RandomStartDelegate extends BaseTripleADelegate {
     return null;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/RocketsFireHelper.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/RocketsFireHelper.java
index 428aaf99a..fa805036b 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/RocketsFireHelper.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/RocketsFireHelper.java
@@ -33,11 +33,12 @@ import org.triplea.java.PredicateBuilder;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.sound.SoundPath;
+import java.util.LinkedHashSet;
 
 /** Logic to fire rockets. */
 public class RocketsFireHelper implements Serializable {
   private static final long serialVersionUID = -3694482580987118444L;
-  private final Set<Territory> attackingFromTerritories = new HashSet<>();
+  private final Set<Territory> attackingFromTerritories = new LinkedHashSet<>();
   private final Map<Territory, Territory> attackedTerritories = new LinkedHashMap<>();
   private final Map<Territory, Unit> attackedUnits = new LinkedHashMap<>();
   private boolean needToFindRocketTargets;
@@ -76,7 +77,7 @@ public class RocketsFireHelper implements Serializable {
       return;
     }
     final Set<Territory> rocketTerritories = getTerritoriesWithRockets(data, player);
-    final Set<Territory> targets = new HashSet<>();
+    final Set<Territory> targets = new LinkedHashSet<>();
     for (final Territory territory : rocketTerritories) {
       targets.addAll(getTargetsWithinRange(territory, data, player));
     }
@@ -131,7 +132,7 @@ public class RocketsFireHelper implements Serializable {
         if (Properties.getDamageFromBombingDoneToUnitsInsteadOfTerritories(data.getProperties())) {
           final Collection<Unit> rocketTargets =
               CollectionUtils.getMatches(attackFrom.getUnits(), rocketMatch(player));
-          final HashSet<UnitType> legalTargetsForTheseRockets = new HashSet<>();
+          final HashSet<UnitType> legalTargetsForTheseRockets = new LinkedHashSet<>();
           // a hack for now, we let the rockets fire at anyone who could be targeted by any rocket
           // Not sure if that comment is still current
           for (final Unit r : rocketTargets) {
@@ -194,7 +195,7 @@ public class RocketsFireHelper implements Serializable {
   }
 
   static Set<Territory> getTerritoriesWithRockets(final GameData data, final GamePlayer player) {
-    final Set<Territory> territories = new HashSet<>();
+    final Set<Territory> territories = new LinkedHashSet<>();
     final Predicate<Unit> ownedRockets = rocketMatch(player);
     final BattleTracker tracker = AbstractMoveDelegate.getBattleTracker(data);
     for (final Territory current : data.getMap()) {
@@ -220,7 +221,7 @@ public class RocketsFireHelper implements Serializable {
   private static Set<Territory> getTargetsWithinRange(
       final Territory territory, final GameState data, final GamePlayer player) {
     final int maxDistance = data.getTechTracker().getRocketDistance(player);
-    final Set<Territory> hasFactory = new HashSet<>();
+    final Set<Territory> hasFactory = new LinkedHashSet<>();
     final Predicate<Territory> allowed =
         PredicateBuilder.of(Matches.territoryAllowsRocketsCanFlyOver(player))
             .andIf(
@@ -544,3 +545,4 @@ public class RocketsFireHelper implements Serializable {
     return bridge.getRemotePlayer();
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TechnologyDelegate.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TechnologyDelegate.java
index e367f44f7..6fbcd1676 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TechnologyDelegate.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TechnologyDelegate.java
@@ -35,6 +35,8 @@ import javax.annotation.Nullable;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.sound.SoundPath;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /**
  * Logic for dealing with player tech rolls. This class requires the TechActivationDelegate which
@@ -51,7 +53,7 @@ public class TechnologyDelegate extends BaseTripleADelegate implements ITechDele
   @Override
   public void initialize(final String name, final String displayName) {
     super.initialize(name, displayName);
-    techs = new HashMap<>();
+    techs = new LinkedHashMap<>();
     techCost = -1;
   }
 
@@ -85,7 +87,7 @@ public class TechnologyDelegate extends BaseTripleADelegate implements ITechDele
                 toFirePossible, AbstractTriggerAttachment.isSatisfiedMatch(testedConditions));
         // now list out individual types to fire, once for each of the matches above.
         TriggerAttachment.triggerAvailableTechChange(
-            new HashSet<>(toFireTestedAndSatisfied),
+            new LinkedHashSet<>(toFireTestedAndSatisfied),
             bridge,
             new FireTriggerParams(null, null, true, true, true, true));
       }
@@ -439,3 +441,4 @@ public class TechnologyDelegate extends BaseTripleADelegate implements ITechDele
     return ITechDelegate.class;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TerritoryEffectHelper.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TerritoryEffectHelper.java
index 874389342..4d59262f0 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TerritoryEffectHelper.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TerritoryEffectHelper.java
@@ -14,6 +14,7 @@ import java.util.Collection;
 import java.util.HashSet;
 import java.util.Map;
 import java.util.Set;
+import java.util.LinkedHashSet;
 
 /** Placeholder for all calculations to do with TerritoryEffects. */
 public final class TerritoryEffectHelper {
@@ -55,7 +56,7 @@ public final class TerritoryEffectHelper {
   }
 
   public static Set<UnitType> getUnitTypesThatLostBlitz(final Collection<Territory> steps) {
-    final Set<UnitType> unitTypes = new HashSet<>();
+    final Set<UnitType> unitTypes = new LinkedHashSet<>();
     for (final Territory location : steps) {
       for (final TerritoryEffect effect : getEffects(location)) {
         unitTypes.addAll(TerritoryEffectAttachment.get(effect).getNoBlitz());
@@ -66,7 +67,7 @@ public final class TerritoryEffectHelper {
 
   public static Set<UnitType> getUnitTypesForUnitsNotAllowedIntoTerritory(
       final Territory location) {
-    final Set<UnitType> unitTypes = new HashSet<>();
+    final Set<UnitType> unitTypes = new LinkedHashSet<>();
     for (final TerritoryEffect effect : getEffects(location)) {
       unitTypes.addAll(TerritoryEffectAttachment.get(effect).getUnitsNotAllowed());
     }
@@ -75,7 +76,7 @@ public final class TerritoryEffectHelper {
 
   public static Set<UnitType> getUnitTypesForUnitsNotAllowedIntoTerritory(
       final Collection<Territory> steps) {
-    final Set<UnitType> unitTypes = new HashSet<>();
+    final Set<UnitType> unitTypes = new LinkedHashSet<>();
     for (final Territory location : steps) {
       unitTypes.addAll(getUnitTypesForUnitsNotAllowedIntoTerritory(location));
     }
@@ -114,3 +115,4 @@ public final class TerritoryEffectHelper {
     return map.getOrDefault(unitType, BigDecimal.ZERO);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TransportTracker.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TransportTracker.java
index c19a822a0..2c5fcf93f 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TransportTracker.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/TransportTracker.java
@@ -26,6 +26,7 @@ import javax.annotation.Nullable;
 import lombok.Value;
 import lombok.experimental.UtilityClass;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
 
 /**
  * Tracks which transports are carrying which units. Also tracks the capacity that has been
@@ -49,7 +50,7 @@ public class TransportTracker {
   private static Map<Unit, Collection<Unit>> transporting(
       final Collection<Unit> units,
       final Function<Unit, Collection<Unit>> getUnitsTransportedByTransport) {
-    final Map<Unit, Collection<Unit>> returnVal = new HashMap<>();
+    final Map<Unit, Collection<Unit>> returnVal = new LinkedHashMap<>();
     for (final Unit transported : units) {
       final Unit transport = transported.getTransportedBy();
       Collection<Unit> transporting = null;
@@ -359,3 +360,4 @@ public class TransportTracker {
     return change;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UndoableMove.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UndoableMove.java
index 16a8d7dee..bb3efa5a7 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UndoableMove.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UndoableMove.java
@@ -25,6 +25,8 @@ import java.util.Set;
 import lombok.Getter;
 import lombok.Setter;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Contains all the data to describe a move and to undo it. */
 public class UndoableMove extends AbstractUndoableMove {
@@ -34,16 +36,16 @@ public class UndoableMove extends AbstractUndoableMove {
   @Setter private String description;
   // this move is dependent on these moves
   // while these moves can't be undone until this one has been
-  private final Set<UndoableMove> dependencies = new HashSet<>();
+  private final Set<UndoableMove> dependencies = new LinkedHashSet<>();
   // these moves depend on me,
   // we can't be undone until this is empty
-  private final Set<UndoableMove> dependents = new HashSet<>();
+  private final Set<UndoableMove> dependents = new LinkedHashSet<>();
   // list of countries we took over
-  private final Set<Territory> conquered = new HashSet<>();
+  private final Set<Territory> conquered = new LinkedHashSet<>();
   // transports loaded by this move
-  private final Set<Unit> loaded = new HashSet<>();
+  private final Set<Unit> loaded = new LinkedHashSet<>();
   // transports unloaded by this move
-  private final Set<Unit> unloaded = new HashSet<>();
+  private final Set<Unit> unloaded = new LinkedHashSet<>();
   @Getter private final Route route;
 
   public UndoableMove(final Collection<Unit> units, final Route route) {
@@ -140,7 +142,7 @@ public class UndoableMove extends AbstractUndoableMove {
               target = CollectionUtils.getAny(enemyTargets);
             }
             if (target != null) {
-              targets = new HashMap<>();
+              targets = new LinkedHashMap<>();
               targets.put(target, new HashSet<>(Set.of(unit)));
             }
             final Change change =
@@ -219,3 +221,4 @@ public class UndoableMove extends AbstractUndoableMove {
     return new MoveDescription(units, route);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UndoablePlacement.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UndoablePlacement.java
index da4c93b7e..51279cbd5 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UndoablePlacement.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UndoablePlacement.java
@@ -11,6 +11,7 @@ import java.util.Collection;
 import java.util.HashMap;
 import java.util.Map;
 import lombok.Getter;
+import java.util.LinkedHashMap;
 
 /** Contains all the data to describe a placement and to undo it. */
 @Getter
@@ -45,7 +46,7 @@ public class UndoablePlacement extends AbstractUndoableMove {
     if (units.isEmpty()) {
       produced.remove(producerTerritory);
     }
-    currentDelegate.setProduced(new HashMap<>(produced));
+    currentDelegate.setProduced(new LinkedHashMap<>(produced));
   }
 
   @Override
@@ -74,3 +75,4 @@ public class UndoablePlacement extends AbstractUndoableMove {
     return getMoveLabel(" produces in ") + ": " + MyFormatter.unitsToTextNoOwner(units);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UnitComparator.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UnitComparator.java
index 62e4fd3ca..63f566f48 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UnitComparator.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/UnitComparator.java
@@ -13,13 +13,14 @@ import java.util.HashMap;
 import java.util.List;
 import java.util.Map;
 import lombok.experimental.UtilityClass;
+import java.util.LinkedHashMap;
 
 /** A collection of comparators for sorting units based on various heuristics. */
 @UtilityClass
 public final class UnitComparator {
 
   public static Comparator<Unit> getLowestToHighestMovementComparator() {
-    final Map<Unit, BigDecimal> cache = new HashMap<>();
+    final Map<Unit, BigDecimal> cache = new LinkedHashMap<>();
     return Comparator.comparing(u -> cache.computeIfAbsent(u, Unit::getMovementLeft));
   }
 
@@ -37,7 +38,7 @@ public final class UnitComparator {
    * @return A {@code Comparator<Unit>} that compares Units by transport capacity
    */
   public static Comparator<Unit> getIncreasingCapacityComparator() {
-    final Map<Unit, Integer> cache = new HashMap<>();
+    final Map<Unit, Integer> cache = new LinkedHashMap<>();
     return Comparator.comparingInt(
         u -> cache.computeIfAbsent(u, k -> TransportUtils.getTransportCost(u.getTransporting())));
   }
@@ -69,7 +70,7 @@ public final class UnitComparator {
   /** Return a Comparator that will order the specified units in preferred move order. */
   public static Comparator<Unit> getMovableUnitsComparator(
       final List<Unit> units, final Route route) {
-    final Map<Unit, BigDecimal> cache = new HashMap<>();
+    final Map<Unit, BigDecimal> cache = new LinkedHashMap<>();
     final Comparator<Unit> decreasingCapacityComparator = getDecreasingCapacityComparator();
     return (u1, u2) -> {
       // Ensure units have enough movement
@@ -141,3 +142,4 @@ public final class UnitComparator {
         Unit::getUnitAttachment, Comparator.comparingInt(UnitAttachment::getBombard).reversed());
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/AbstractBattle.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/AbstractBattle.java
index e33625f41..487340005 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/AbstractBattle.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/AbstractBattle.java
@@ -32,6 +32,7 @@ import org.triplea.java.ObjectUtils;
 import org.triplea.java.RemoveOnNextMajorRelease;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
 
 abstract class AbstractBattle implements IBattle {
   private static final long serialVersionUID = 871090498661731337L;
@@ -54,7 +55,7 @@ abstract class AbstractBattle implements IBattle {
   final BattleType battleType;
   boolean isOver = false;
 
-  @RemoveOnNextMajorRelease final Map<Unit, Collection<Unit>> dependentUnits = new HashMap<>();
+  @RemoveOnNextMajorRelease final Map<Unit, Collection<Unit>> dependentUnits = new LinkedHashMap<>();
 
   List<Unit> attackingUnits = new ArrayList<>();
   List<Unit> defendingUnits = new ArrayList<>();
@@ -336,3 +337,4 @@ abstract class AbstractBattle implements IBattle {
     return bridge.getRemotePlayer(player);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/AirBattle.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/AirBattle.java
index c42316e09..a7641a5a2 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/AirBattle.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/AirBattle.java
@@ -49,6 +49,7 @@ import org.triplea.java.PredicateBuilder;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.sound.SoundPath;
+import java.util.LinkedHashMap;
 
 /** Battle class used for air battles and interception before a standard battle. */
 @Slf4j
@@ -372,7 +373,7 @@ public class AirBattle extends AbstractBattle {
               target = CollectionUtils.getAny(enemyTargets);
             }
             if (target != null) {
-              targets = new HashMap<>();
+              targets = new LinkedHashMap<>();
               targets.put(target, new HashSet<>(Set.of(unit)));
             }
             battleTracker.addBattle(
@@ -966,3 +967,4 @@ public class AirBattle extends AbstractBattle {
   public void unitsLostInPrecedingBattle(
       final Collection<Unit> units, final IDelegateBridge bridge, final boolean withdrawn) {}
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/BattleDelegate.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/BattleDelegate.java
index c71db37a7..34570ed8d 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/BattleDelegate.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/BattleDelegate.java
@@ -59,6 +59,8 @@ import lombok.Getter;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Delegate to track and fight all battles. */
 @AutoSave(beforeStepStart = true, afterStepEnd = true)
@@ -306,7 +308,7 @@ public class BattleDelegate extends BaseTripleADelegate implements IBattleDelega
    * Return map of adjacent territories along attack routes in battles where fighting will occur.
    */
   private Map<Territory, Collection<IBattle>> getPossibleBombardingTerritories() {
-    final Map<Territory, Collection<IBattle>> possibleBombardingTerritories = new HashMap<>();
+    final Map<Territory, Collection<IBattle>> possibleBombardingTerritories = new LinkedHashMap<>();
     for (final IBattle battle : battleTracker.getPendingBattles(BattleType.NORMAL)) {
       // we only care about battles where we must fight
       // this check is really to avoid implementing getAttackingFrom() in other battle subclasses
@@ -347,7 +349,7 @@ public class BattleDelegate extends BaseTripleADelegate implements IBattleDelega
       return CollectionUtils.getAny(battles);
     }
     final List<Territory> territories = new ArrayList<>();
-    final Map<Territory, IBattle> battleTerritories = new HashMap<>();
+    final Map<Territory, IBattle> battleTerritories = new LinkedHashMap<>();
     for (final IBattle battle : battles) {
       // If Restricted & # of bombarding units => landing units, don't add territory to list to
       // bombard
@@ -430,7 +432,7 @@ public class BattleDelegate extends BaseTripleADelegate implements IBattleDelega
       // dependencies
       final Map<Unit, Collection<Unit>> transportMap =
           TransportTracker.transporting(territory.getUnitCollection());
-      final Set<Unit> dependants = new HashSet<>();
+      final Set<Unit> dependants = new LinkedHashSet<>();
       for (final Entry<Unit, Collection<Unit>> entry : transportMap.entrySet()) {
         // only consider those transports that we are attacking with. allied and enemy transports
         // are not added.
@@ -762,7 +764,7 @@ public class BattleDelegate extends BaseTripleADelegate implements IBattleDelega
         }
 
         // Validate players have enough fuel to move there and back
-        final Map<GamePlayer, ResourceCollection> playerFuelCost = new HashMap<>();
+        final Map<GamePlayer, ResourceCollection> playerFuelCost = new LinkedHashMap<>();
         for (final Entry<Territory, Collection<Unit>> entry : toScramble.entrySet()) {
           final Map<GamePlayer, ResourceCollection> map =
               Route.getScrambleFuelCostCharge(entry.getValue(), entry.getKey(), to, data);
@@ -877,7 +879,7 @@ public class BattleDelegate extends BaseTripleADelegate implements IBattleDelega
           if (attackingUnits.stream().anyMatch(Matches.unitIsAir().negate())) {
             // TODO: for now, we will hack and say that the attackers came from Everywhere, and hope
             // the user will choose the correct place to retreat to! (TODO: Fix this)
-            final Map<Territory, Collection<Unit>> attackingFromMap = new HashMap<>();
+            final Map<Territory, Collection<Unit>> attackingFromMap = new LinkedHashMap<>();
             final Predicate<Territory> predicate =
                 to.isWater() ? Matches.territoryIsWater() : Matches.territoryIsLand();
             final Collection<Territory> neighbors = data.getMap().getNeighbors(to, predicate);
@@ -1216,7 +1218,7 @@ public class BattleDelegate extends BaseTripleADelegate implements IBattleDelega
     final Collection<Territory> pendingBattles =
         battleTracker.getPendingBattleSitesWithoutBombing();
     // create a list of all kamikaze zones, listed by enemy
-    final Map<GamePlayer, Collection<Territory>> kamikazeZonesByEnemy = new HashMap<>();
+    final Map<GamePlayer, Collection<Territory>> kamikazeZonesByEnemy = new LinkedHashMap<>();
     for (final Territory t : data.getMap().getTerritories()) {
       final Optional<TerritoryAttachment> optionalTerritoryAttachment = TerritoryAttachment.get(t);
       if (optionalTerritoryAttachment.isEmpty()
@@ -1306,7 +1308,7 @@ public class BattleDelegate extends BaseTripleADelegate implements IBattleDelega
       }
       // now let the enemy decide if they will do attacks
       final Collection<Territory> kamikazeZones = entry.getValue();
-      final Map<Territory, Collection<Unit>> possibleUnitsToAttack = new HashMap<>();
+      final Map<Territory, Collection<Unit>> possibleUnitsToAttack = new LinkedHashMap<>();
       for (final Territory t : kamikazeZones) {
         final List<Unit> validTargets = t.getUnitCollection().getMatches(canBeAttacked);
         if (!validTargets.isEmpty()) {
@@ -1483,7 +1485,7 @@ public class BattleDelegate extends BaseTripleADelegate implements IBattleDelega
     final boolean areNeutralsPassableByAir =
         (Properties.getNeutralFlyoverAllowed(data.getProperties())
             && !Properties.getNeutralsImpassable(data.getProperties()));
-    final Set<Territory> canNotLand = new HashSet<>();
+    final Set<Territory> canNotLand = new LinkedHashSet<>();
     canNotLand.addAll(battleTracker.getPendingBattleSitesWithoutBombing());
     canNotLand.addAll(
         CollectionUtils.getMatches(
@@ -1520,7 +1522,7 @@ public class BattleDelegate extends BaseTripleADelegate implements IBattleDelega
                 possibleTerrs,
                 Matches.isTerritoryAllied(alliedPlayer).and(Matches.territoryIsLand())));
     availableLand.removeAll(canNotLand);
-    final Set<Territory> whereCanLand = new HashSet<>(availableLand);
+    final Set<Territory> whereCanLand = new LinkedHashSet<>(availableLand);
     // now for carrier-air-landing validation
     if (Matches.unitCanLandOnCarrier().test(strandedAir)) {
       final Set<Territory> availableWater =
@@ -1572,3 +1574,4 @@ public class BattleDelegate extends BaseTripleADelegate implements IBattleDelega
     }
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/BattleTracker.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/BattleTracker.java
index bfa2abe37..ec6e3793d 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/BattleTracker.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/BattleTracker.java
@@ -60,6 +60,8 @@ import org.triplea.java.collections.IntegerMap;
 import org.triplea.sound.ISound;
 import org.triplea.sound.SoundPath;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Used to keep track of where battles have occurred. */
 @Slf4j
@@ -70,28 +72,28 @@ public class BattleTracker implements Serializable {
       "Bombing Raids should be dealt with first! Be sure the battle has dependencies set correctly!";
 
   // List of pending battles
-  private final Set<IBattle> pendingBattles = new HashSet<>();
+  private final Set<IBattle> pendingBattles = new LinkedHashSet<>();
   // List of battle dependencies
   // maps blocked -> Collection of battles that must precede
-  private final Map<IBattle, Set<IBattle>> dependencies = new HashMap<>();
+  private final Map<IBattle, Set<IBattle>> dependencies = new LinkedHashMap<>();
   // enemy and neutral territories that have been conquered
   // blitzed is a subset of this
-  @Getter private final Set<Territory> conquered = new HashSet<>();
+  @Getter private final Set<Territory> conquered = new LinkedHashSet<>();
   // blitzed territories
-  private final Set<Territory> blitzed = new HashSet<>();
+  private final Set<Territory> blitzed = new LinkedHashSet<>();
   // territories where a battle occurred
-  private final Set<Territory> foughtBattles = new HashSet<>();
+  private final Set<Territory> foughtBattles = new LinkedHashSet<>();
 
   // list of territory we have conquered in a FinishedBattle and where from and if amphibious
   @Getter
   private final Map<Territory, Map<Territory, Collection<Unit>>> finishedBattlesUnitAttackFromMap =
-      new HashMap<>();
+      new LinkedHashMap<>();
 
   // things like kamikaze suicide attacks disallow bombarding from that sea zone for that turn
-  private final Set<Territory> noBombardAllowed = new HashSet<>();
+  private final Set<Territory> noBombardAllowed = new LinkedHashSet<>();
 
   @Getter
-  private final Map<Territory, Collection<Unit>> defendingAirThatCanNotLand = new HashMap<>();
+  private final Map<Territory, Collection<Unit>> defendingAirThatCanNotLand = new LinkedHashMap<>();
 
   private BattleRecords battleRecords = null;
   // to keep track of all relationships that have changed this turn
@@ -144,7 +146,7 @@ public class BattleTracker implements Serializable {
 
   public boolean didAllThesePlayersJustGoToWarThisTurn(
       final GamePlayer p1, final Collection<Unit> enemyUnits) {
-    final Set<GamePlayer> enemies = new HashSet<>();
+    final Set<GamePlayer> enemies = new LinkedHashSet<>();
     for (final Unit u : CollectionUtils.getMatches(enemyUnits, Matches.unitIsEnemyOf(p1))) {
       enemies.add(u.getOwner());
     }
@@ -1205,7 +1207,7 @@ public class BattleTracker implements Serializable {
   }
 
   public void addDependency(final IBattle blocked, final IBattle blocking) {
-    dependencies.computeIfAbsent(blocked, k -> new HashSet<>()).add(blocking);
+    dependencies.computeIfAbsent(blocked, k -> new LinkedHashSet<>()).add(blocking);
   }
 
   private void removeDependency(final IBattle blocked, final IBattle blocking) {
@@ -1383,3 +1385,4 @@ public class BattleTracker implements Serializable {
         + pendingBattles;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/DependentBattle.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/DependentBattle.java
index 3cb04b145..5d1284e36 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/DependentBattle.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/DependentBattle.java
@@ -16,6 +16,7 @@ import java.util.Map;
 import java.util.Set;
 import lombok.Getter;
 import org.triplea.java.RemoveOnNextMajorRelease;
+import java.util.LinkedHashMap;
 
 /** Battle with possible dependencies Includes MustFightBattle and NonFightingBattle. */
 public abstract class DependentBattle extends AbstractBattle {
@@ -33,7 +34,7 @@ public abstract class DependentBattle extends AbstractBattle {
       final BattleTracker battleTracker,
       final GameData data) {
     super(battleSite, attacker, battleTracker, BattleType.NORMAL, data);
-    attackingFromMap = new HashMap<>();
+    attackingFromMap = new LinkedHashMap<>();
     amphibiousAttackFrom = new ArrayList<>();
   }
 
@@ -77,3 +78,4 @@ public abstract class DependentBattle extends AbstractBattle {
     return amphibiousAttackFrom;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/FinishedBattle.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/FinishedBattle.java
index 99f35d703..845571879 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/FinishedBattle.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/FinishedBattle.java
@@ -22,6 +22,7 @@ import java.util.Map;
 import java.util.Set;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
 
 /**
  * A sort of scripted battle made for blitzed/conquered territories without a fight. TODO: expand to
@@ -33,7 +34,7 @@ public class FinishedBattle extends AbstractBattle {
   private final Collection<Territory> amphibiousAttackFrom = new ArrayList<>();
   // maps Territory-> units (stores a collection of who is attacking from where, needed for undoing
   // moves)
-  private final Map<Territory, Collection<Unit>> attackingFromMap = new HashMap<>();
+  private final Map<Territory, Collection<Unit>> attackingFromMap = new LinkedHashMap<>();
 
   FinishedBattle(
       final Territory battleSite,
@@ -156,3 +157,4 @@ public class FinishedBattle extends AbstractBattle {
     return attackingFromMap;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/FireAa.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/FireAa.java
index ed16b4880..e9bf8da5b 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/FireAa.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/FireAa.java
@@ -33,6 +33,8 @@ import java.util.Set;
 import java.util.UUID;
 import org.triplea.java.RemoveOnNextMajorRelease;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /**
  * Maintains the state of a group of AA units firing during a {@link
@@ -118,7 +120,7 @@ public class FireAa implements IExecutable {
                         TechTracker.getCurrentTechAdvances(
                             hitPlayer, bridge.getData().getTechnologyFrontier()))
                     .get(aaType)
-                : new HashSet<>();
+                : new LinkedHashSet<>();
         final Collection<Unit> validTargets =
             CollectionUtils.getMatches(
                 attackableUnits,
@@ -208,7 +210,7 @@ public class FireAa implements IExecutable {
   static List<Collection<Unit>> newFiringUnitGroups(final Collection<Unit> units) {
 
     // Sort suicide on hit units by type
-    final Map<UnitType, Collection<Unit>> map = new HashMap<>();
+    final Map<UnitType, Collection<Unit>> map = new LinkedHashMap<>();
     for (final Unit unit : CollectionUtils.getMatches(units, Matches.unitIsSuicideOnHit())) {
       final UnitType type = unit.getType();
       if (map.containsKey(type)) {
@@ -230,3 +232,4 @@ public class FireAa implements IExecutable {
     return result;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/MustFightBattle.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/MustFightBattle.java
index fa1038190..d4276e5da 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/MustFightBattle.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/MustFightBattle.java
@@ -84,6 +84,7 @@ import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.sound.SoundPath;
 import org.triplea.sound.SoundUtils;
+import java.util.LinkedHashMap;
 
 /** Handles logic for battles in which fighting actually occurs. */
 @Slf4j
@@ -243,7 +244,7 @@ public class MustFightBattle extends DependentBattle
    */
   @Override
   public Collection<Unit> getRemainingAttackingUnits() {
-    final Set<Unit> remaining = new HashSet<>(attackingUnitsRetreated);
+    final Set<Unit> remaining = new LinkedHashSet<>(attackingUnitsRetreated);
     final Collection<Unit> unitsLeftInTerritory = new ArrayList<>(battleSite.getUnits());
     unitsLeftInTerritory.removeAll(killed);
     remaining.addAll(
@@ -264,7 +265,7 @@ public class MustFightBattle extends DependentBattle
    */
   @Override
   public Collection<Unit> getRemainingDefendingUnits() {
-    final Set<Unit> remaining = new HashSet<>(defendingUnitsRetreated);
+    final Set<Unit> remaining = new LinkedHashSet<>(defendingUnitsRetreated);
     remaining.addAll(defendingUnits);
     if (getWhoWon() != WhoWon.ATTACKER || attackingUnits.stream().allMatch(Matches.unitIsAir())) {
       final var unitsLeftInTerritory =
@@ -457,6 +458,26 @@ public class MustFightBattle extends DependentBattle
 
   @Override
   public void endBattle(final WhoWon whoWon, final IDelegateBridge bridge) {
+    if (Boolean.getBoolean("plan") && !headless) {
+      java.util.function.Function<java.util.Collection<Unit>, String> fmt = us -> {
+        java.util.TreeMap<String, Integer> m = new java.util.TreeMap<>();
+        for (Unit u : us) m.merge(u.getType().getName(), 1, Integer::sum);
+        StringBuilder b = new StringBuilder();
+        boolean first = true;
+        for (var e : m.entrySet()) { if (!first) b.append(','); b.append(e.getKey()).append(':').append(e.getValue()); first=false; }
+        return b.toString();
+      };
+      java.util.List<Unit> killedAtk = new java.util.ArrayList<>();
+      java.util.List<Unit> killedDef = new java.util.ArrayList<>();
+      for (Unit u : killed) {
+        if (u.getOwner() != null && u.getOwner().getName().equals(attacker.getName())) killedAtk.add(u);
+        else killedDef.add(u);
+      }
+      System.out.println(String.format(
+        "BATTLE t=%s atk=%s def=%s won=%s atk_alive=[%s] def_alive=[%s] killed_atk=[%s] killed_def=[%s]",
+        battleSite.getName(), attacker.getName(), defender == null ? "<nil>" : defender.getName(), whoWon,
+        fmt.apply(attackingUnits), fmt.apply(defendingUnits), fmt.apply(killedAtk), fmt.apply(killedDef)));
+    }
     switch (whoWon) {
       case ATTACKER:
         attackerWins(bridge);
@@ -784,7 +805,7 @@ public class MustFightBattle extends DependentBattle
   @VisibleForTesting
   public List<String> determineStepStrings() {
     stepStrings = new ArrayList<>();
-    stepFiringUnits = new HashMap<>();
+    stepFiringUnits = new LinkedHashMap<>();
     for (var details : BattleSteps.builder().battleState(this).battleActions(this).build().get()) {
       String stepName = details.getName();
       stepStrings.add(stepName);
@@ -1536,3 +1557,4 @@ public class MustFightBattle extends DependentBattle
         + attackingUnits;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/ScrambleLogic.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/ScrambleLogic.java
index 03e0f458d..1a9d242f3 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/ScrambleLogic.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/ScrambleLogic.java
@@ -21,6 +21,8 @@ import lombok.Getter;
 import org.triplea.java.PredicateBuilder;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.util.Tuple;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /**
  * ScrambeLogic encapsulates the logic for finding possible units that can scramble to defend a
@@ -128,18 +130,18 @@ public class ScrambleLogic {
         CollectionUtils.getMatches(territoriesWithBattles, Matches.territoryIsWater());
     final Collection<Territory> territoriesWithBattlesLand =
         CollectionUtils.getMatches(territoriesWithBattles, Matches.territoryIsLand());
-    final Map<Territory, Set<Territory>> scrambleTerrs = new HashMap<>();
+    final Map<Territory, Set<Territory>> scrambleTerrs = new LinkedHashMap<>();
     for (final Territory battleTerr : territoriesWithBattlesWater) {
       final Collection<Territory> canScrambleFrom = getCanScrambleFromTerritories(battleTerr);
       if (!canScrambleFrom.isEmpty()) {
-        scrambleTerrs.put(battleTerr, new HashSet<>(canScrambleFrom));
+        scrambleTerrs.put(battleTerr, new LinkedHashSet<>(canScrambleFrom));
       }
     }
     for (final Territory battleTerr : territoriesWithBattlesLand) {
       if (!toSeaOnly) {
         final Collection<Territory> canScrambleFrom = getCanScrambleFromTerritories(battleTerr);
         if (!canScrambleFrom.isEmpty()) {
-          scrambleTerrs.put(battleTerr, new HashSet<>(canScrambleFrom));
+          scrambleTerrs.put(battleTerr, new LinkedHashSet<>(canScrambleFrom));
         }
       }
       final IBattle battle = battleTracker.getPendingBattle(battleTerr, BattleType.NORMAL);
@@ -152,9 +154,9 @@ public class ScrambleLogic {
         for (final Territory amphibFrom : amphibFromTerrs) {
           if (toAnyAmphibious) {
             final Collection<Territory> territories = getCanScrambleFromTerritories(amphibFrom);
-            scrambleTerrs.computeIfAbsent(amphibFrom, key -> new HashSet<>()).addAll(territories);
+            scrambleTerrs.computeIfAbsent(amphibFrom, key -> new LinkedHashSet<>()).addAll(territories);
           } else if (canScrambleFromPredicate.test(battleTerr)) {
-            scrambleTerrs.computeIfAbsent(amphibFrom, key -> new HashSet<>()).add(battleTerr);
+            scrambleTerrs.computeIfAbsent(amphibFrom, key -> new LinkedHashSet<>()).add(battleTerr);
           }
         }
       }
@@ -170,9 +172,9 @@ public class ScrambleLogic {
             .and(Matches.unitWasScrambled().negate());
 
     final Map<Territory, Map<Territory, Tuple<Collection<Unit>, Collection<Unit>>>>
-        scramblersByTerritory = new HashMap<>();
+        scramblersByTerritory = new LinkedHashMap<>();
     for (final Territory to : scrambleTerrs.keySet()) {
-      final Map<Territory, Tuple<Collection<Unit>, Collection<Unit>>> scramblers = new HashMap<>();
+      final Map<Territory, Tuple<Collection<Unit>, Collection<Unit>>> scramblers = new LinkedHashMap<>();
       for (final Territory from : scrambleTerrs.get(to)) {
         // find how many is the max this territory can scramble
         final UnitCollection fromUnits = from.getUnitCollection();
@@ -227,3 +229,4 @@ public class ScrambleLogic {
     return maxScrambled;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/StrategicBombingRaidBattle.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/StrategicBombingRaidBattle.java
index 4f28f9d24..020189375 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/StrategicBombingRaidBattle.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/StrategicBombingRaidBattle.java
@@ -58,6 +58,8 @@ import org.triplea.java.Interruptibles;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.sound.SoundPath;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** A strategic bombing raid (SBR) battle. */
 public class StrategicBombingRaidBattle extends AbstractBattle implements BattleStepStrings {
@@ -65,7 +67,7 @@ public class StrategicBombingRaidBattle extends AbstractBattle implements Battle
   private static final String RAID = "Strategic bombing raid";
 
   // these would be the factories or other targets. does not include aa.
-  private final Map<Unit, Set<Unit>> targets = new HashMap<>();
+  private final Map<Unit, Set<Unit>> targets = new LinkedHashMap<>();
   private final ExecutionStack stack = new ExecutionStack();
   private List<String> steps;
   private List<Unit> defendingAa;
@@ -176,7 +178,7 @@ public class StrategicBombingRaidBattle extends AbstractBattle implements Battle
     targets.forEach(
         (target, targetAttackers) -> {
           final Set<Unit> currentAttackers =
-              this.targets.computeIfAbsent(target, i -> new HashSet<>());
+              this.targets.computeIfAbsent(target, i -> new LinkedHashSet<>());
           currentAttackers.addAll(targetAttackers);
         });
     return ChangeFactory.EMPTY_CHANGE;
@@ -827,7 +829,7 @@ public class StrategicBombingRaidBattle extends AbstractBattle implements Battle
           Properties.getWW2V2(gameData.getProperties())
               || Properties.getLimitRocketAndSbrDamageToProduction(gameData.getProperties());
       final List<Die> bombingDice = new ArrayList<>();
-      final Map<Unit, List<Die>> targetToDiceMap = new HashMap<>();
+      final Map<Unit, List<Die>> targetToDiceMap = new LinkedHashMap<>();
       // limit to maxDamage
       for (final Unit attacker : attackingUnits) {
         final UnitAttachment ua = attacker.getUnitAttachment();
@@ -1006,3 +1008,4 @@ public class StrategicBombingRaidBattle extends AbstractBattle implements Battle
     // should never happen
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/UnitBattleComparator.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/UnitBattleComparator.java
index 0b1e8d513..5e6b0ba77 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/UnitBattleComparator.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/UnitBattleComparator.java
@@ -14,6 +14,7 @@ import lombok.AccessLevel;
 import lombok.Getter;
 import lombok.Value;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashSet;
 
 // TODO: make the Comparator be serializable. To get there all class members need to be serializable
 // or removed.
@@ -28,7 +29,7 @@ public class UnitBattleComparator implements Comparator<Unit> {
   IntegerMap<UnitType> costs;
   boolean bonus;
   boolean ignorePrimaryPower;
-  Collection<UnitType> multiHitpointCanRepair = new HashSet<>();
+  Collection<UnitType> multiHitpointCanRepair = new LinkedHashSet<>();
   CombatValue combatValueCalculator;
   CombatValue reversedCombatValueCalculator;
 
@@ -176,3 +177,4 @@ public class UnitBattleComparator implements Comparator<Unit> {
     return ua1.getMovement(u1.getOwner()) - ua2.getMovement(u2.getOwner());
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/AaCasualtySelector.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/AaCasualtySelector.java
index d07716e47..911f13b27 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/AaCasualtySelector.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/AaCasualtySelector.java
@@ -26,6 +26,7 @@ import java.util.function.Function;
 import java.util.stream.Collectors;
 import java.util.stream.IntStream;
 import lombok.experimental.UtilityClass;
+import java.util.LinkedHashSet;
 
 @UtilityClass
 public class AaCasualtySelector {
@@ -177,7 +178,7 @@ public class AaCasualtySelector {
             DiceType.ENGINE,
             "Deciding which planes should die due to AA fire");
     // turn the random numbers into a unique set of targets
-    final Set<Integer> hitTargets = new HashSet<>();
+    final Set<Integer> hitTargets = new LinkedHashSet<>();
     int index = 0;
     for (final int randomIndex : hitRandom) {
       index = (index + randomIndex) % availableTargets.size();
@@ -219,3 +220,4 @@ public class AaCasualtySelector {
         .collect(Collectors.toList());
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/CasualtyOrderOfLosses.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/CasualtyOrderOfLosses.java
index 23e4eb29f..277807159 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/CasualtyOrderOfLosses.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/CasualtyOrderOfLosses.java
@@ -26,6 +26,8 @@ import lombok.Builder;
 import lombok.Value;
 import lombok.experimental.UtilityClass;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 @UtilityClass
 class CasualtyOrderOfLosses {
@@ -132,12 +134,12 @@ class CasualtyOrderOfLosses {
     Collections.reverse(sortedUnitsList);
     final List<Unit> sortedWellEnoughUnitsList = new ArrayList<>();
     final Map<Unit, UnitPowerStrengthAndRolls> originalUnitPowerAndRollsMap =
-        new HashMap<>(unitPowerAndRollsMap);
+        new LinkedHashMap<>(unitPowerAndRollsMap);
     for (int i = 0; i < sortedUnitsList.size(); ++i) {
       // Loop through all target units to find the best unit to take as casualty
       Unit worstUnit = null;
       int minPower = Integer.MAX_VALUE;
-      final Set<UnitType> unitTypes = new HashSet<>();
+      final Set<UnitType> unitTypes = new LinkedHashSet<>();
       for (final Unit u : sortedUnitsList) {
         if (unitTypes.contains(u.getType())) {
           continue;
@@ -272,3 +274,4 @@ class CasualtyOrderOfLosses {
         + Objects.hashCode(targetTypes);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/CasualtyUtil.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/CasualtyUtil.java
index 4e0ea5fd6..eb6325e45 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/CasualtyUtil.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/casualty/CasualtyUtil.java
@@ -7,6 +7,7 @@ import java.util.Collection;
 import java.util.HashMap;
 import java.util.Map;
 import lombok.experimental.UtilityClass;
+import java.util.LinkedHashMap;
 
 @UtilityClass
 public class CasualtyUtil {
@@ -28,10 +29,11 @@ public class CasualtyUtil {
 
   public static Map<Unit, Collection<Unit>> getDependents(final Collection<Unit> targets) {
     // just worry about transports
-    final Map<Unit, Collection<Unit>> dependents = new HashMap<>();
+    final Map<Unit, Collection<Unit>> dependents = new LinkedHashMap<>();
     for (final Unit target : targets) {
       dependents.put(target, TransportTracker.transportingAndUnloaded(target));
     }
     return dependents;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/change/suicide/RemoveUnits.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/change/suicide/RemoveUnits.java
index b2a488218..384c442d2 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/change/suicide/RemoveUnits.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/change/suicide/RemoveUnits.java
@@ -17,6 +17,7 @@ import java.util.Map;
 import java.util.function.Predicate;
 import lombok.AllArgsConstructor;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
 
 @AllArgsConstructor
 abstract class RemoveUnits implements BattleStep {
@@ -51,10 +52,11 @@ abstract class RemoveUnits implements BattleStep {
   }
 
   private Map<Unit, Collection<Unit>> getDependents(final Collection<Unit> units) {
-    final Map<Unit, Collection<Unit>> dependents = new HashMap<>();
+    final Map<Unit, Collection<Unit>> dependents = new LinkedHashMap<>();
     for (final Unit unit : units) {
       dependents.put(unit, battleState.getDependentUnits(List.of(unit)));
     }
     return dependents;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/FiringGroup.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/FiringGroup.java
index 1104269e8..dd4b1b072 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/FiringGroup.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/FiringGroup.java
@@ -16,6 +16,7 @@ import lombok.Getter;
 import lombok.Value;
 import org.triplea.java.RemoveOnNextMajorRelease;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashSet;
 
 /**
  * Group of units that are firing on targets
@@ -97,7 +98,7 @@ public class FiringGroup implements Serializable {
    * @param aliveUnits Units that need to be retained as targets
    */
   public void retainAliveTargets(final Collection<Unit> aliveUnits) {
-    targetUnits.retainAll(new HashSet<>(aliveUnits));
+    targetUnits.retainAll(new LinkedHashSet<>(aliveUnits));
   }
 
   /**
@@ -157,3 +158,4 @@ public class FiringGroup implements Serializable {
         : originalName;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/MarkCasualties.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/MarkCasualties.java
index fa6c015ca..a3adebf3a 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/MarkCasualties.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/MarkCasualties.java
@@ -28,6 +28,7 @@ import org.triplea.java.ChangeOnNextMajorRelease;
 import org.triplea.java.Interruptibles;
 import org.triplea.java.RemoveOnNextMajorRelease;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
 
 /** Step where the casualties are moved from ALIVE to CASUALTY */
 @RequiredArgsConstructor
@@ -98,7 +99,7 @@ public class MarkCasualties implements BattleStep {
   }
 
   private void notifyCasualties(final IDelegateBridge bridge) {
-    final Map<Unit, Collection<Unit>> dependentUnits = new HashMap<>();
+    final Map<Unit, Collection<Unit>> dependentUnits = new LinkedHashMap<>();
     for (final Unit unit : fireRoundState.getCasualties().getKilled()) {
       dependentUnits.put(unit, battleState.getDependentUnits(List.of(unit)));
     }
@@ -195,7 +196,7 @@ public class MarkCasualties implements BattleStep {
         firingGroup.getFiringUnits().stream()
             .limit(fireRoundState.getDice().getHits())
             .collect(Collectors.toList());
-    final Map<Unit, Collection<Unit>> dependentUnits = new HashMap<>();
+    final Map<Unit, Collection<Unit>> dependentUnits = new LinkedHashMap<>();
     for (final Unit unit : suicidedUnits) {
       dependentUnits.put(unit, battleState.getDependentUnits(List.of(unit)));
     }
@@ -208,3 +209,4 @@ public class MarkCasualties implements BattleStep {
     battleActions.removeUnits(suicidedUnits, bridge, battleState.getBattleSite(), side);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/SelectMainBattleCasualties.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/SelectMainBattleCasualties.java
index 5799a707f..a05078232 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/SelectMainBattleCasualties.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/fire/SelectMainBattleCasualties.java
@@ -23,6 +23,7 @@ import java.util.stream.Collectors;
 import lombok.NoArgsConstructor;
 import lombok.Value;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
 
 /** Selects casualties for normal (basically, anything that isn't AA) hits */
 @NoArgsConstructor
@@ -130,7 +131,7 @@ public class SelectMainBattleCasualties
    */
   private List<Unit> limitTransportsToSelect(
       final Collection<Unit> restrictedTransports, final int hitsLeftForTransports) {
-    final Map<GamePlayer, Collection<Unit>> alliedHitPlayer = new HashMap<>();
+    final Map<GamePlayer, Collection<Unit>> alliedHitPlayer = new LinkedHashMap<>();
     for (final Unit unit : restrictedTransports) {
       alliedHitPlayer.computeIfAbsent(unit.getOwner(), (owner) -> new ArrayList<>()).add(unit);
     }
@@ -181,3 +182,4 @@ public class SelectMainBattleCasualties
     }
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/retreat/RetreaterGeneral.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/retreat/RetreaterGeneral.java
index e3b9e7ebe..61f04992f 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/retreat/RetreaterGeneral.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/retreat/RetreaterGeneral.java
@@ -22,6 +22,7 @@ import java.util.HashSet;
 import java.util.List;
 import lombok.AllArgsConstructor;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashSet;
 
 @AllArgsConstructor
 class RetreaterGeneral implements Retreater {
@@ -77,7 +78,7 @@ class RetreaterGeneral implements Retreater {
       historyChildren.add(RetreatHistoryChild.of(transcriptText, new ArrayList<>(airRetreating)));
     }
 
-    final Collection<Unit> nonAirRetreating = new HashSet<>(retreatUnits);
+    final Collection<Unit> nonAirRetreating = new LinkedHashSet<>(retreatUnits);
     nonAirRetreating.removeAll(airRetreating);
     nonAirRetreating.addAll(battleState.getDependentUnits(nonAirRetreating));
 
@@ -139,3 +140,4 @@ class RetreaterGeneral implements Retreater {
     return change;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/retreat/RetreaterPartialAmphibious.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/retreat/RetreaterPartialAmphibious.java
index 01d01e670..9e4adc44a 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/retreat/RetreaterPartialAmphibious.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/battle/steps/retreat/RetreaterPartialAmphibious.java
@@ -17,6 +17,7 @@ import java.util.HashSet;
 import java.util.List;
 import lombok.AllArgsConstructor;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashSet;
 
 @AllArgsConstructor
 class RetreaterPartialAmphibious implements Retreater {
@@ -60,7 +61,7 @@ class RetreaterPartialAmphibious implements Retreater {
       historyChildren.add(RetreatHistoryChild.of(transcriptText, new ArrayList<>(airRetreating)));
     }
 
-    final Collection<Unit> nonAirRetreating = new HashSet<>(retreatUnits);
+    final Collection<Unit> nonAirRetreating = new LinkedHashSet<>(retreatUnits);
     nonAirRetreating.removeAll(airRetreating);
     nonAirRetreating.addAll(battleState.getDependentUnits(nonAirRetreating));
 
@@ -76,3 +77,4 @@ class RetreaterPartialAmphibious implements Retreater {
     return RetreatChanges.of(change, historyChildren);
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/data/BattleListing.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/data/BattleListing.java
index 965ea4189..0a4d9dcfd 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/data/BattleListing.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/data/BattleListing.java
@@ -13,6 +13,7 @@ import java.util.Set;
 import java.util.function.BiConsumer;
 import java.util.function.Predicate;
 import lombok.Getter;
+import java.util.LinkedHashSet;
 
 /**
  * Sent by the battle delegate to the game player to indicate which battles are left to be fought.
@@ -29,17 +30,21 @@ public class BattleListing implements Serializable {
    */
   public BattleListing(final Set<IBattle> battles) {
     this.battlesMap = new EnumMap<>(BattleType.class);
-    battles.stream()
-        .filter(b -> !b.isEmpty())
-        .forEach(
-            b -> {
-              Collection<Territory> territories = battlesMap.get(b.getBattleType());
-              if (territories == null) {
-                territories = new HashSet<>();
-              }
-              territories.add(b.getTerritory());
-              battlesMap.put(b.getBattleType(), territories);
-            });
+    // triplea-port-bootstrap: sort battles: sort each per-BattleType territory bucket by
+    // territory name so iteration is stable. Mirrors the Odin port's
+    // battle_listing_new sort. Without this, ProAi.battle iteration
+    // order depends on HashSet<IBattle> hashCode order (allocation
+    // address) which is not reproducible across JVMs / Odin runs.
+    final java.util.List<IBattle> sortedBattles =
+        battles.stream()
+            .filter(b -> !b.isEmpty())
+            .sorted(java.util.Comparator.comparing(b -> b.getTerritory().getName()))
+            .collect(java.util.stream.Collectors.toList());
+    for (final IBattle b : sortedBattles) {
+      battlesMap
+          .computeIfAbsent(b.getBattleType(), k -> new java.util.ArrayList<>())
+          .add(b.getTerritory());
+    }
   }
 
   public Set<Territory> getNormalBattlesIncludingAirBattles() {
@@ -51,7 +56,7 @@ public class BattleListing implements Serializable {
   }
 
   private Set<Territory> getBattlesWith(Predicate<BattleType> predicate) {
-    final Set<Territory> territories = new HashSet<>();
+    final Set<Territory> territories = new LinkedHashSet<>();
     for (final Entry<BattleType, Collection<Territory>> entry : battlesMap.entrySet()) {
       if (predicate.test(entry.getKey())) {
         territories.addAll(entry.getValue());
@@ -74,3 +79,4 @@ public class BattleListing implements Serializable {
     }
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/data/BattleRecords.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/data/BattleRecords.java
index a4f494325..ac0ef534a 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/data/BattleRecords.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/data/BattleRecords.java
@@ -13,6 +13,7 @@ import java.util.HashMap;
 import java.util.Map;
 import java.util.Map.Entry;
 import java.util.UUID;
+import java.util.LinkedHashMap;
 
 /**
  * The Purpose of this class is to record various information about combat, in order to use it for
@@ -24,7 +25,7 @@ public class BattleRecords implements Serializable {
   private final Map<GamePlayer, Map<UUID, BattleRecord>> records;
 
   public BattleRecords() {
-    this.records = new HashMap<>();
+    this.records = new LinkedHashMap<>();
   }
 
   public BattleRecords(final Map<GamePlayer, Map<UUID, BattleRecord>> records) {
@@ -33,11 +34,11 @@ public class BattleRecords implements Serializable {
 
   // Create copy
   public BattleRecords(final BattleRecords records) {
-    this.records = new HashMap<>();
+    this.records = new LinkedHashMap<>();
     for (final Entry<GamePlayer, Map<UUID, BattleRecord>> entry : records.records.entrySet()) {
       final GamePlayer p = entry.getKey();
       final Map<UUID, BattleRecord> record = entry.getValue();
-      final Map<UUID, BattleRecord> map = new HashMap<>();
+      final Map<UUID, BattleRecord> map = new LinkedHashMap<>();
       for (final Entry<UUID, BattleRecord> entry2 : record.entrySet()) {
         map.put(entry2.getKey(), new BattleRecord(entry2.getValue()));
       }
@@ -216,7 +217,7 @@ public class BattleRecords implements Serializable {
       final BattleType battleType) {
     Map<UUID, BattleRecord> current = records.get(currentPlayerAndAttacker);
     if (current == null) {
-      current = new HashMap<>();
+      current = new LinkedHashMap<>();
     }
     final BattleRecord initial = new BattleRecord(battleSite, currentPlayerAndAttacker, battleType);
     current.put(battleId, initial);
@@ -274,3 +275,4 @@ public class BattleRecords implements Serializable {
     return sb.toString().replaceFirst(", ", "");
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/move/validation/AirMovementValidator.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/move/validation/AirMovementValidator.java
index 8613c8e35..802480ac2 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/move/validation/AirMovementValidator.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/move/validation/AirMovementValidator.java
@@ -77,7 +77,7 @@ public final class AirMovementValidator {
             .and(Matches.isUnitAllied(player))
             .and(Matches.unitIsAir())
             .and(Matches.unitCanLandOnCarrier());
-    final Set<Unit> airThatMustLandOnCarriersHash = new HashSet<>();
+    final Set<Unit> airThatMustLandOnCarriersHash = new LinkedHashSet<>();
     airThatMustLandOnCarriersHash.addAll(
         CollectionUtils.getMatches(routeEnd.getUnits(), airAlliedNotOwned));
     airThatMustLandOnCarriersHash.addAll(CollectionUtils.getMatches(units, airAlliedNotOwned));
@@ -110,7 +110,7 @@ public final class AirMovementValidator {
     carriersAtEnd.addAll(movingCarriersAtStartLocationBeingMoved);
     // to keep track of all carriers, and their fighters, that have moved, so that we do not move
     // them again.
-    final Map<Unit, Collection<Unit>> movedCarriersAndTheirFighters = new HashMap<>();
+    final Map<Unit, Collection<Unit>> movedCarriersAndTheirFighters = new LinkedHashMap<>();
     for (final Unit carrier : carriersAtEnd) {
       movedCarriersAndTheirFighters.put(carrier, new ArrayList<>());
     }
@@ -149,7 +149,7 @@ public final class AirMovementValidator {
     potentialCarrierOrigins.addAll(
         data.getMap()
             .getNeighbors(
-                new HashSet<>(landingSpots),
+                new LinkedHashSet<>(landingSpots),
                 maxMovementLeftForAllOwnedCarriers,
                 Matches.seaCanMoveOver(player)));
     potentialCarrierOrigins.remove(routeEnd);
@@ -455,7 +455,7 @@ public final class AirMovementValidator {
         allCarriers.addAll(carriersThatCanReach);
         // now we want to make a map of the carriers to the units they must carry with them (both
         // allied and owned)
-        final Map<Unit, Collection<Unit>> carriersToMove = new HashMap<>();
+        final Map<Unit, Collection<Unit>> carriersToMove = new LinkedHashMap<>();
         final List<Unit> carrierFull = new ArrayList<>();
         for (final Unit carrier : allCarriers) {
           final Collection<Unit> airMovingWith = new ArrayList<>();
@@ -728,7 +728,7 @@ public final class AirMovementValidator {
         return false;
       }
       // when doing the calculation, make sure to include the units in the territory
-      final Set<Unit> friendly = new HashSet<>();
+      final Set<Unit> friendly = new LinkedHashSet<>();
       friendly.addAll(getFriendly(territory, player, data));
       friendly.addAll(airUnits);
       // make sure we have the carrier capacity
@@ -844,3 +844,4 @@ public final class AirMovementValidator {
     return numberOfTerritories * Properties.getNeutralCharge(data.getProperties());
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/move/validation/MoveValidator.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/move/validation/MoveValidator.java
index 2ae270d60..75185dd69 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/move/validation/MoveValidator.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/move/validation/MoveValidator.java
@@ -56,6 +56,8 @@ import org.triplea.java.PredicateBuilder;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
 import org.triplea.util.Triple;
+import java.util.LinkedHashMap;
+import java.util.LinkedHashSet;
 
 /** Responsible for validating unit movement. */
 @AllArgsConstructor
@@ -245,8 +247,8 @@ public class MoveValidator {
       result.setErrorReturnResult("Not enough units in starting territory");
     }
     // this should never happen
-    else if (new HashSet<>(units).size() != units.size()) {
-      result.setError("Not all units unique, units: " + units + " unique: " + new HashSet<>(units));
+    else if (new LinkedHashSet<>(units).size() != units.size()) {
+      result.setError("Not all units unique, units: " + units + " unique: " + new LinkedHashSet<>(units));
     } else {
       units.forEach(
           unit -> {
@@ -308,7 +310,7 @@ public class MoveValidator {
       @Nullable final Collection<Unit> units,
       final Map<Unit, Collection<Unit>> airTransportDependents,
       final GamePlayer player) {
-    Map<Territory, Collection<CanalAttachment>> territoryCanals = new HashMap<>();
+    Map<Territory, Collection<CanalAttachment>> territoryCanals = new LinkedHashMap<>();
     int numCanals = 0;
     for (Territory t : route.getAllTerritories()) {
       // Only check canals that are on the route
@@ -324,8 +326,8 @@ public class MoveValidator {
 
     // Check each unit 1 by 1 to see if they can move through necessary canals on route
     String result = null;
-    final Set<Unit> unitsThatFailCanal = new HashSet<>();
-    final Set<Unit> setWithNull = new HashSet<>();
+    final Set<Unit> unitsThatFailCanal = new LinkedHashSet<>();
+    final Set<Unit> setWithNull = new LinkedHashSet<>();
     setWithNull.add(null);
     final Collection<Unit> unitsWithoutDependents =
         (units == null) ? setWithNull : findNonDependentUnits(units, route, airTransportDependents);
@@ -895,7 +897,7 @@ public class MoveValidator {
       final GamePlayer player,
       final Collection<Unit> possibleLandTransports,
       final Collection<Unit> unitsToLandTransport) {
-    final Set<Unit> disallowedUnits = new HashSet<>();
+    final Set<Unit> disallowedUnits = new LinkedHashSet<>();
 
     try (GameData.Unlocker ignored = data.acquireReadLock()) {
       int numLandTransportsWithoutCapacity =
@@ -1477,7 +1479,7 @@ public class MoveValidator {
 
   private Map<Unit, Unit> convertTransportKeyedMapToLoadedUnitKeyedMap(
       final Map<Unit, Collection<Unit>> airTransportDependents, final MoveValidationResult result) {
-    Map<Unit, Unit> unitsToTransport = new HashMap<>();
+    Map<Unit, Unit> unitsToTransport = new LinkedHashMap<>();
     for (Unit transport : airTransportDependents.keySet()) {
       int capacity = TransportTracker.getAvailableCapacity(transport);
       for (Unit beingTransported : airTransportDependents.get(transport)) {
@@ -1566,7 +1568,7 @@ public class MoveValidator {
 
   private static Map<Unit, Collection<Unit>> transportsMustMoveWith(
       final Territory start, final Collection<Unit> units) {
-    final Map<Unit, Collection<Unit>> mustMoveWith = new HashMap<>();
+    final Map<Unit, Collection<Unit>> mustMoveWith = new LinkedHashMap<>();
     final Collection<Unit> transports =
         CollectionUtils.getMatches(units, Matches.unitIsSeaTransport());
     for (final Unit transport : transports) {
@@ -1582,7 +1584,7 @@ public class MoveValidator {
       final Territory start,
       final Collection<Unit> units,
       final Map<Unit, Collection<Unit>> airTransportDependents) {
-    final Map<Unit, Collection<Unit>> mustMoveWith = new HashMap<>();
+    final Map<Unit, Collection<Unit>> mustMoveWith = new LinkedHashMap<>();
     final Collection<Unit> airTransports =
         CollectionUtils.getMatches(units, Matches.unitIsAirTransport());
     // Then check those that have already had their transportedBy set
@@ -1634,7 +1636,7 @@ public class MoveValidator {
     if (alliedAir.isEmpty()) {
       return Map.of();
     }
-    final Map<Unit, Collection<Unit>> mapping = new HashMap<>();
+    final Map<Unit, Collection<Unit>> mapping = new LinkedHashMap<>();
     // get air that must be carried by our carriers
     final Collection<Unit> ownedCarrier =
         CollectionUtils.getMatches(
@@ -2008,3 +2010,4 @@ public class MoveValidator {
         .and(Matches.unitIsAirborne().negate());
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/AaPowerStrengthAndRolls.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/AaPowerStrengthAndRolls.java
index 00475a4da..cc3ccb277 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/AaPowerStrengthAndRolls.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/AaPowerStrengthAndRolls.java
@@ -14,6 +14,7 @@ import lombok.AccessLevel;
 import lombok.Getter;
 import lombok.Value;
 import org.triplea.java.collections.CollectionUtils;
+import java.util.LinkedHashMap;
 
 /**
  * Computes the total power, total strength, and total roll for a collection of AA units
@@ -37,7 +38,7 @@ public class AaPowerStrengthAndRolls implements TotalPowerAndTotalRolls {
 
   CombatValue calculator;
 
-  Map<Unit, UnitPowerStrengthAndRolls> totalStrengthAndTotalRollsByUnit = new HashMap<>();
+  Map<Unit, UnitPowerStrengthAndRolls> totalStrengthAndTotalRollsByUnit = new LinkedHashMap<>();
 
   int targetCount;
 
@@ -277,3 +278,4 @@ public class AaPowerStrengthAndRolls implements TotalPowerAndTotalRolls {
         == 1;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/AvailableSupports.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/AvailableSupports.java
index 9125c6f11..922452439 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/AvailableSupports.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/AvailableSupports.java
@@ -14,6 +14,7 @@ import lombok.Data;
 import lombok.Getter;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
 
 /**
  * Tracks the available support that a collection of units can give to other units.
@@ -27,8 +28,8 @@ class AvailableSupports {
 
   static final AvailableSupports EMPTY_RESULT =
       AvailableSupports.builder()
-          .supportRules(new HashMap<>())
-          .supportUnits(new HashMap<>())
+          .supportRules(new LinkedHashMap<>())
+          .supportUnits(new LinkedHashMap<>())
           .build();
 
   final Map<UnitSupportAttachment.BonusType, List<UnitSupportAttachment>> supportRules;
@@ -56,7 +57,7 @@ class AvailableSupports {
    * Keeps track of the units that have provided support in {@link
    * AvailableSupports#giveSupportToUnit} and whom they are providing it to
    */
-  @Getter private final Map<Unit, IntegerMap<Unit>> unitsGivingSupport = new HashMap<>();
+  @Getter private final Map<Unit, IntegerMap<Unit>> unitsGivingSupport = new LinkedHashMap<>();
 
   /** Sorts 'supportsAvailable' lists based on unit support attachment rules. */
   static AvailableSupports getSortedSupport(final SupportCalculator supportCalculator) {
@@ -94,7 +95,7 @@ class AvailableSupports {
   AvailableSupports filter(final Predicate<UnitSupportAttachment> ruleFilter) {
 
     final Map<UnitSupportAttachment.BonusType, List<UnitSupportAttachment>> supportRules =
-        new HashMap<>();
+        new LinkedHashMap<>();
     for (final Map.Entry<UnitSupportAttachment.BonusType, List<UnitSupportAttachment>> entry :
         this.supportRules.entrySet()) {
       final List<UnitSupportAttachment> filteredSupportRules =
@@ -104,7 +105,7 @@ class AvailableSupports {
       }
     }
 
-    final Map<UnitSupportAttachment, SupportDetails> supportUnits = new HashMap<>();
+    final Map<UnitSupportAttachment, SupportDetails> supportUnits = new LinkedHashMap<>();
     for (final UnitSupportAttachment usa : this.supportUnits.keySet()) {
       if (ruleFilter.test(usa)) {
         supportUnits.put(usa, new SupportDetails(this.supportUnits.get(usa)));
@@ -176,3 +177,4 @@ class AvailableSupports {
     return u;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/PowerStrengthAndRolls.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/PowerStrengthAndRolls.java
index 3dd8697e2..5a305f370 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/PowerStrengthAndRolls.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/PowerStrengthAndRolls.java
@@ -12,6 +12,7 @@ import lombok.Getter;
 import lombok.Value;
 import org.triplea.java.collections.CollectionUtils;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
 
 /**
  * Computes the total power, total strength, and total roll for a collection of units
@@ -30,15 +31,15 @@ public class PowerStrengthAndRolls implements TotalPowerAndTotalRolls {
   int diceSides;
 
   @Getter(AccessLevel.PUBLIC)
-  Map<Unit, UnitPowerStrengthAndRolls> totalStrengthAndTotalRollsByUnit = new HashMap<>();
+  Map<Unit, UnitPowerStrengthAndRolls> totalStrengthAndTotalRollsByUnit = new LinkedHashMap<>();
 
   List<UnitPowerStrengthAndRolls> sortedStrengthAndRolls = new ArrayList<>();
 
   @Getter(AccessLevel.PUBLIC)
-  Map<Unit, IntegerMap<Unit>> unitSupportPowerMap = new HashMap<>();
+  Map<Unit, IntegerMap<Unit>> unitSupportPowerMap = new LinkedHashMap<>();
 
   @Getter(AccessLevel.PUBLIC)
-  Map<Unit, IntegerMap<Unit>> unitSupportRollsMap = new HashMap<>();
+  Map<Unit, IntegerMap<Unit>> unitSupportRollsMap = new LinkedHashMap<>();
 
   private PowerStrengthAndRolls(final Collection<Unit> units, final CombatValue calculator) {
     this.calculator = calculator;
@@ -159,3 +160,4 @@ public class PowerStrengthAndRolls implements TotalPowerAndTotalRolls {
     return totalStrengthAndTotalRollsByUnit.get(unit).getPower();
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/SupportCalculator.java b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/SupportCalculator.java
index 908718acb..b7a1cba84 100644
--- a/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/SupportCalculator.java
+++ b/game-app/game-core/src/main/java/games/strategy/triplea/delegate/power/calculator/SupportCalculator.java
@@ -17,6 +17,7 @@ import lombok.AllArgsConstructor;
 import lombok.Getter;
 import lombok.Value;
 import org.triplea.java.collections.IntegerMap;
+import java.util.LinkedHashMap;
 
 /** Calculates how much support units can give */
 @Value
@@ -40,8 +41,8 @@ public class SupportCalculator {
       final boolean allies) {
     this.side = side;
     this.allies = allies;
-    supportRules = new HashMap<>();
-    supportUnits = new HashMap<>();
+    supportRules = new LinkedHashMap<>();
+    supportUnits = new LinkedHashMap<>();
 
     if (unitsGivingTheSupport == null || unitsGivingTheSupport.isEmpty()) {
       return;
@@ -92,7 +93,7 @@ public class SupportCalculator {
 
   public static Map<Unit, IntegerMap<Unit>> getCombinedSupportGiven(
       AvailableSupports supportFromFriends, AvailableSupports supportFromEnemies) {
-    Map<Unit, IntegerMap<Unit>> support = new HashMap<>();
+    Map<Unit, IntegerMap<Unit>> support = new LinkedHashMap<>();
     for (var entry : supportFromFriends.getUnitsGivingSupport().entrySet()) {
       support.computeIfAbsent(entry.getKey(), u -> new IntegerMap<>()).add(entry.getValue());
     }
@@ -102,3 +103,4 @@ public class SupportCalculator {
     return support;
   }
 }
+// triplea-port-bootstrap: global LHM rewrite
diff --git a/game-app/smoke-testing/build.gradle.kts b/game-app/smoke-testing/build.gradle.kts
index 663b2490d..8dd864e57 100644
--- a/game-app/smoke-testing/build.gradle.kts
+++ b/game-app/smoke-testing/build.gradle.kts
@@ -11,11 +11,19 @@ dependencies {
     testImplementation(project(":game-app:game-headless"))
     testImplementation(project(":lib:java-extras"))
     testImplementation(project(":lib:test-common"))
+    // triplea-port-bootstrap: needed by GenericValueSerializer
+    testImplementation(libs.jackson.databind)
 }
 
 tasks.named<Test>("test") {
     // AiGameTest is memory intensive due to ConcurrentBattleCalculator threads deserializing GameData concurrently.
     maxHeapSize = "2G"
+    // triplea-port-bootstrap: Ww2v5JacocoRun#runWithSnapshots reseeds
+    // java.lang.Math$RandomNumberGeneratorHolder#randomNumberGenerator
+    // via reflection so AI decisions (Math.random() in ProPurchaseUtils,
+    // AbstractAi politics/casualties, WeakAi purchase) are deterministic
+    // across snapshot runs. Required so the Odin port can byte-match.
+    jvmArgs("--add-opens", "java.base/java.lang=ALL-UNNAMED")
 }
 
 // Gather all URLs in the same game list file
@@ -46,3 +54,105 @@ tasks.named<ProcessResources>("processTestResources") {
         into("save-games")
     }
 }
+
+// Added by triplea-port-bootstrap to make jacocoTestReport aggregate
+// production classes from all dependent modules. Without this, the smoke-
+// testing report is empty (this module has no production sources of its own).
+val coverageProjects = listOf(
+    ":game-app:domain-data",
+    ":game-app:game-core",
+    ":game-app:ai",
+    ":game-app:game-headless",
+    ":game-app:map-data",
+    ":lib:java-extras",
+    ":lib:xml-reader",
+    ":lib:swing-lib",
+    ":lib:websocket-client",
+    ":lib:websocket-server",
+    ":lib:feign-common",
+    ":http-clients:lobby-client",
+)
+tasks.named<org.gradle.testing.jacoco.tasks.JacocoReport>("jacocoTestReport") {
+    coverageProjects.forEach { p ->
+        val proj = project.findProject(p)
+        if (proj != null) {
+            sourceSets(proj.the<SourceSetContainer>()["main"])
+        }
+    }
+    reports {
+        xml.required.set(true)
+        xml.outputLocation.set(layout.buildDirectory.file("jacoco.xml"))
+    }
+}
+
+// triplea-port-bootstrap: snapshot agent
+// When -PsnapshotAgent=<path-to-jar> is passed, attach the Byte Buddy
+// snapshot agent to the test JVM. Per-call config + output dir are passed
+// via -Dsnapshot.config and -Dsnapshot.outDir (set by
+// scripts/capture_proc_snapshot.py).
+tasks.withType<Test>().configureEach {
+    if (project.hasProperty("snapshotAgent")) {
+        val agentJar = project.property("snapshotAgent") as String
+        val methodsFile = (project.findProperty("snapshotMethods") as String?)
+            ?: "${rootProject.projectDir}/conversion/snapshot-agent/jfr-methods.txt"
+        val configFile = (project.findProperty("snapshotConfig") as String?)
+            ?: "${rootProject.projectDir}/conversion/snapshot-agent/snapshot.config"
+        val outDir = System.getProperty("snapshot.outDir")
+            ?: "${project.layout.buildDirectory.get()}/snapshots"
+        jvmArgs("-javaagent:${agentJar}=methods=${methodsFile},config=${configFile},outDir=${outDir}")
+        jvmArgs("-XX:+EnableDynamicAgentLoading")
+        systemProperty("snapshot.outDir", outDir)
+        System.getProperty("snapshot.rounds")?.let { systemProperty("snapshot.rounds", it) }
+    }
+}
+
+// triplea-port-bootstrap: golden-fixture proc-recorder agent
+// (golden_testing_plan.md Phase 1). Pass -PprocRecorderAgent=<jar>
+// (and optionally -PprocRecorderWhitelist=<file> / -DprocRecorder.outDir=<dir>)
+// to capture (args, return) per call for every method on every class
+// listed in the whitelist file. Independent of the snapshotAgent above.
+tasks.withType<Test>().configureEach {
+    if (project.hasProperty("procRecorderAgent")) {
+        val agentJar = project.property("procRecorderAgent") as String
+        val whitelist = (project.findProperty("procRecorderWhitelist") as String?)
+            ?: "${rootProject.projectDir}/conversion/proc-recorder-agent/agent.whitelist"
+        val outDir = System.getProperty("procRecorder.outDir")
+            ?: "${project.layout.buildDirectory.get()}/golden_fixtures"
+        val maxBytes = System.getProperty("procRecorder.maxBytes") ?: "209715200"
+        val perMethodCap = System.getProperty("procRecorder.perMethodCap") ?: "200"
+        val inlineClass = System.getProperty("procRecorder.class") ?: ""
+        val progressMs = System.getProperty("procRecorder.progressEveryMs") ?: "5000"
+        val classArg = if (inlineClass.isNotEmpty()) ",class=${inlineClass}" else ""
+        jvmArgs("-javaagent:${agentJar}=whitelist=${whitelist},outDir=${outDir}," +
+                "maxBytes=${maxBytes},perMethodCap=${perMethodCap}," +
+                "progressEveryMs=${progressMs}${classArg}")
+        jvmArgs("-XX:+EnableDynamicAgentLoading")
+        systemProperty("procRecorder.outDir", outDir)
+    }
+}
+
+// triplea-port-bootstrap: add-opens for Math.random reseed
+tasks.withType<Test>().configureEach {
+    jvmArgs("--add-opens", "java.base/java.lang=ALL-UNNAMED")
+    // Snapshot agent reflectively walks every field of every captured
+    // object (GenericValueSerializer.IdentityHintSerializer + Jackson
+    // default typing). Needed so f.setAccessible(true) succeeds for
+    // private fields on java.lang.reflect.Proxy and java.util internals
+    // when an AI receiver's transitive graph reaches a JDK Proxy.
+    jvmArgs("--add-opens", "java.base/java.lang.reflect=ALL-UNNAMED")
+    jvmArgs("--add-opens", "java.base/java.util=ALL-UNNAMED")
+    jvmArgs("--add-opens", "java.base/java.util.concurrent=ALL-UNNAMED")
+    // Propagate snapshot tuning system properties from the gradle invocation
+    // into the test JVM, so e.g. `./gradlew :smoke-testing:test
+    // -Dsnapshot.outDir=/tmp -Dsnapshot.rounds=1` actually reaches
+    // Ww2v5JacocoRun#runWithSnapshots. Without this, gradle's daemon JVM
+    // sees them but the forked test JVM does not. The snapshot-agent block
+    // above only forwards them when -PsnapshotAgent is also passed.
+    listOf("snapshot.outDir", "snapshot.rounds", "snapshot.rangeStart", "snapshot.rangeEnd",
+           "game.rounds", "game.outFile", "game.stopAfterStep", "digest", "digest.detail.r", "digest.detail.i", "plan",
+           "pro.ncm.trace", "pro.ncm.trace.dump", "pro.pur.trace", "pro.pur.trace.dump", "enemy.atk.dump",
+           "simbattle.trace", "mv.trace")
+        .forEach { key ->
+            System.getProperty(key)?.let { systemProperty(key, it) }
+        }
+}
diff --git a/gradle/libs.versions.toml b/gradle/libs.versions.toml
index fc7a8e973..78eb637a6 100644
--- a/gradle/libs.versions.toml
+++ b/gradle/libs.versions.toml
@@ -58,6 +58,7 @@ gson = { module = "com.google.code.gson:gson", version.ref = "gson" }
 guava = { module = "com.google.guava:guava", version.ref = "guava" }
 hamcrest-optional = { module = "com.github.npathai:hamcrest-optional", version.ref = "hamcrest-optional" }
 hamcrest = { module = "org.hamcrest:java-hamcrest", version.ref = "hamcrest" }
+jackson-databind = { module = "com.fasterxml.jackson.core:jackson-databind", version.ref = "jackson-datatype" }
 jackson-datatype-jsr310 = { module = "com.fasterxml.jackson.datatype:jackson-datatype-jsr310", version.ref = "jackson-datatype" }
 jakarta-mail = { module = "com.sun.mail:jakarta.mail", version.ref = "jakarta-mail" }
 java-websocket = { module = "org.java-websocket:Java-WebSocket", version.ref = "java-websocket" }
